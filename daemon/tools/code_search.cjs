// code_search tool — stub. Body lands in plan 02-03 (ripgrep spawn).

async function call() {
  const err = new Error('code_search not implemented yet (lands in plan 02-03)');
  err.code = 'not_implemented';
  throw err;
}

module.exports = { call };
