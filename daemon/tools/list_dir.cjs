// list_dir tool — stub. Body lands in plan 02-02.

async function call() {
  const err = new Error('list_dir not implemented yet (lands in plan 02-02)');
  err.code = 'not_implemented';
  throw err;
}

module.exports = { call };
