---
phase: 08-browser-automation
plan: 03
type: execute
wave: 3
depends_on: ["phase-08-browser-automation-08-02"]
files_modified:
  - src/renderer/components/MessageBlock.tsx
  - src/renderer/components/BrowserNavigateBlock.tsx
  - src/renderer/components/BrowserClickBlock.tsx
  - src/renderer/components/BrowserTypeBlock.tsx
  - src/renderer/components/BrowserFillFormBlock.tsx
  - src/renderer/components/BrowserScreenshotBlock.tsx
  - src/renderer/components/BrowserEvaluateBlock.tsx
  - src/renderer/components/BotSettingsPage.tsx
  - src/renderer/components/BotSettingsBrowserTab.tsx
  - src/renderer/state/browser.ts
  - src/renderer/styles/app.css
  - tests/playwright/fake-m3-server.ts
  - tests/playwright/browser-automation.test.ts
  - playwright.config.ts
autonomous: true
requirements:
  - TOOL-07
  - TOOL-08
  - TOOL-09
  - TOOL-10
  - TOOL-11
  - TOOL-12

estimate:
  tokens: 88000
  raw_tokens: 43000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "User can edit per-bot browserAllow + browserDeny + ssrfAllowInternal + the 6 browser tool allowlist checkboxes from BotSettingsPage's new 6th 'Browser' tab; changes debounce-save (250ms) via existing bot.update IPC; URL hash sync recognizes 'browser' as a valid tab"
    - "Renderer renders inline blocks for browser_navigate (url header + text content + status + bytes footer), browser_click (selector + text snippet), browser_type (selector + textBytes + submitted badge), browser_fill_form (fieldCount + submitted badge + text snippet), browser_screenshot (img with app:// src), browser_evaluate (expression bytes + result bytes + result text)"
    - "Playwright E2E: fake-m3-server streams a browser.navigate tool_use; renderer block renders url + content; audit row carries {hostname, path, status, duration_ms} only (no full URL, no query string, no HTML)"
    - "Playwright E2E: fake-m3-server streams browser.click + browser.type + browser.fill_form + browser.screenshot + browser.evaluate tool_use blocks; each renders the corresponding inline block; audit minimization asserted per case (no typed text, no expression source, no PNG bytes)"
  artifacts:
    - "src/renderer/components/BrowserNavigateBlock.tsx (NEW) - normalized url header + status badge + text content (collapsible > 500 bytes) + bytes footer"
    - "src/renderer/components/BrowserClickBlock.tsx (NEW) - selector header + text snippet (5KB cap) + duration"
    - "src/renderer/components/BrowserTypeBlock.tsx (NEW) - selector header + textBytes count + (submitted) badge + duration"
    - "src/renderer/components/BrowserFillFormBlock.tsx (NEW) - {fieldCount} fields header + (submitted) badge + text snippet (5KB cap)"
    - "src/renderer/components/BrowserScreenshotBlock.tsx (NEW) - {runId}/{n} header + bytes count + <img src={fileUri}> with cap-height CSS"
    - "src/renderer/components/BrowserEvaluateBlock.tsx (NEW) - expressionBytes + resultBytes header + <pre> result (collapsible > 500 bytes)"
    - "src/renderer/components/BotSettingsBrowserTab.tsx (NEW) - 6 browser-tool checkboxes + browserAllow/deny textareas + ssrfAllowInternal checkbox + save-status"
    - "src/renderer/state/browser.ts (NEW) - module-scope store + useBrowserConfig hook + browserActions object"
    - "src/renderer/components/MessageBlock.tsx (MODIFIED) - switch dispatches 6 new cases to new components"
    - "src/renderer/components/BotSettingsPage.tsx (MODIFIED) - TabId union extends with 'browser'; TAB_IDS + TAB_LABELS extended; URL hash sync to 'browser'; render <BotSettingsBrowserTab />"
    - "src/renderer/styles/app.css (MODIFIED) - .block-browser-* + .settings-tab-browser styles"
    - "tests/playwright/fake-m3-server.ts (MODIFIED) - 6 new helpers: streamBrowserNavigateToolUse, streamBrowserClickToolUse, streamBrowserTypeToolUse, streamBrowserFillFormToolUse, streamBrowserScreenshotToolUse, streamBrowserEvaluateToolUse"
    - "tests/playwright/browser-automation.test.ts (NEW) - 6-case E2E: navigate + click + type + screenshot + evaluate + fill_form; audit minimization asserted per case"
    - "playwright.config.ts (MODIFIED) - extends daemon-smoke project with browser-automation test"
  key_links:
    - "src/renderer/components/BotSettingsBrowserTab.tsx -> useEffect debounce (250ms) -> bots/update patch {browserAllow, browserDeny, ssrfAllowInternal, allowlist: <existing + browser tools>} -> api.bot.update -> daemon writeConfigPatch -> ALLOWED_CONFIG_KEYS accepts new fields"
    - "src/renderer/state/browser.ts -> useEffect subscribes to window.localbot.on('browser:config:updated', refresh) + initial fetch -> module-scope state + subscribers Set -> useBrowserConfig hook returns {config, refresh}"
    - "src/renderer/components/MessageBlock.tsx -> switch (block.kind) -> 6 new cases dispatch to BrowserXxxBlock components -> default: null + exhaustiveness check"
    - "tests/playwright/fake-m3-server.ts streamBrowser*ToolUse helpers -> SSE chunks with tool_use block name='browser.navigate' input={url} etc. -> renderer MessageBlock dispatch renders BrowserNavigateBlock"
    - "tests/playwright/browser-automation.test.ts -> spawn daemon with LOCALBOT_USER_DATA_DIR=<tmp> + LOCALBOT_ALLOW_INTERNAL_HOSTS=1 -> create <tmp>/screenshots/ -> start Node http.createServer for both the M3 mock AND the test page -> send initialize -> stream tool_use -> assert audit row + UI block + screenshot file at <userData>/screenshots/<runId>/<n>.png"
    - "BrowserScreenshotBlock <img src={block.fileUri}> where fileUri is app://localhost/screenshots/<runId>/<n>.png resolved by src/main/ipc/browser.ts protocol.handle('app') handler"
  prohibitions:
    - "BrowserNavigateBlock MUST display only the normalized url (hostname + pathname, NO query string per Pitfall 5)"
    - "BrowserTypeBlock MUST display only textBytes count + submitted badge, NEVER the typed text (Pitfall 5)"
    - "BrowserEvaluateBlock MUST display only expressionBytes + resultBytes counts, NEVER the expression source (Pitfall 5)"
    - "BrowserFillFormBlock MUST display only fieldCount + submitted badge + resulting text snippet, NEVER individual field values (Pitfall 5)"
    - "BotSettingsBrowserTab MUST debounce saves (250ms) to match Phase 7 Obsidian tab; immediate save could race concurrent edits"
    - "BotSettingsBrowserTab MUST parse newline-delimited globs into string[] before sending"
    - "fake-m3-server streamBrowser*ToolUse helpers MUST follow the same SSE envelope shape as the existing streamToolUseResponse (no custom protocol)"
    - "Playwright test MUST NOT depend on real external sites (use Node http.createServer for the test page; set LOCALBOT_ALLOW_INTERNAL_HOSTS=1 + ssrfAllowInternal:true in bot config to allow localhost navigation)"
    - "Playwright test MUST NOT depend on real Chromium download for the daemon-smoke project (handled via test setup OR local-bot-spawn which already has Chromium)"
    - "Playwright test MUST assert audit minimization for every case (no full URL, no typed text, no expression source, no PNG bytes)"
    - "BrowserScreenshotBlock <img> MUST use cap-height CSS (max-height: 400px; max-width: 100%) so the chat doesn't explode"
    - "Browser*Block components MUST handle errors gracefully (empty text / missing data / failed IPC fallback to placeholder)"

# Plan 08-03 artifact additions (full list lives in Plan 08-01's Artifacts section):
#
# New files (Plan 3):
#   src/renderer/state/browser.ts
#   src/renderer/components/BrowserNavigateBlock.tsx
#   src/renderer/components/BrowserClickBlock.tsx
#   src/renderer/components/BrowserTypeBlock.tsx
#   src/renderer/components/BrowserFillFormBlock.tsx
#   src/renderer/components/BrowserScreenshotBlock.tsx
#   src/renderer/components/BrowserEvaluateBlock.tsx
#   src/renderer/components/BotSettingsBrowserTab.tsx
#   tests/playwright/browser-automation.test.ts
#
# Modified files (Plan 3):
#   src/renderer/components/MessageBlock.tsx (6 new switch cases)
#   src/renderer/components/BotSettingsPage.tsx (6th 'browser' tab + URL hash sync)
#   src/renderer/styles/app.css (.block-browser-* + .settings-tab-browser styles)
#   tests/playwright/fake-m3-server.ts (6 streamBrowser*ToolUse helpers)
#   playwright.config.ts (extends daemon-smoke project)

<objective>
Phase 8 UI + E2E slice: render the 6 MessageBlock variants in chat (browser_navigate + browser_click + browser_type + browser_fill_form + browser_screenshot + browser_evaluate), add the 6th 'Browser' tab to BotSettingsPage with URL allowlist + tool allowlist + SSRF opt-out toggle, then prove the full vertical slice end-to-end via a new Playwright suite that spawns the daemon with a temp userData, starts a Node http.createServer test page, streams browser.* tool_use blocks via the fake-m3-server, and asserts the audit row minimization + UI block rendering + screenshot file on disk.

Purpose: close the user-facing surface so the renderer can configure
browser settings and the LLM can actually use all 6 browser tools in a
real Electron run.

Output: 6 new React components + 1 new renderer state module + 1 new
BotSettingsBrowserTab + 1 extended BotSettingsPage + 1 extended
MessageBlock dispatcher + 6 new fake-m3-server helpers + 1 new Playwright
E2E suite + 1 extended playwright.config + styles.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-browser-automation/08-RESEARCH.md
@.planning/phases/08-browser-automation/08-PATTERNS.md
@.planning/phases/08-browser-automation/08-VALIDATION.md
@.planning/phases/08-browser-automation/08-01-PLAN.md
@.planning/phases/08-browser-automation/08-01-SUMMARY.md (created during execution)
@.planning/phases/08-browser-automation/08-02-PLAN.md
@.planning/phases/07-obsidian-integration/07-03-PLAN.md (closest analog — UI + E2E)

# Source files for direct reference
@src/renderer/components/MessageBlock.tsx (existing switch dispatcher with vault_read/vault_search/vault_write cases — add 6 new)
@src/renderer/components/BotSettingsPage.tsx (existing 5-tab pattern with debounced save + URL hash sync; add 'browser' tab)
@src/renderer/components/BotSettingsObsidianTab.tsx (template for BotSettingsBrowserTab: debounced-save + parseGlobList + combined allowlist + 4 tool checkboxes shape)
@src/renderer/components/VaultReadBlock.tsx (template for BrowserNavigateBlock: path header + bytes footer + collapse)
@src/renderer/components/VaultWriteBlock.tsx (template for BrowserScreenshotBlock: small payload shape + <img> element)
@src/renderer/state/vault.ts (template for state/browser.ts: module-scope + 200ms throttle + ensureDaemonSubscription + EVENT_BROWSER_CONFIG_UPDATED)
@src/renderer/state/bots.ts (template for state/browser.ts updateBot pattern)
@src/shared/types.ts (BotConfig browserAllow/browserDeny/ssrfAllowInternal + 6 MessageBlock variants from Plans 1 + 2)
@src/main/preload/index.ts (api.browser.{getScreenshot, deleteContext} from Plan 1 + 2)
@src/shared/window.d.ts (LocalbotApi.browser namespace from Plan 1 + 2; LocalbotEvent union)
@src/renderer/components/AppModal.tsx (escape + focus trap primitive — NOT used here; no modal needed for browser tab)
@tests/playwright/fake-m3-server.ts (existing streamToolUseResponse at L165-234; SSE envelope pattern)
@tests/playwright/obsidian-integration.test.ts (template for browser-automation.test.ts spawn pattern)
@tests/playwright/daemon-tools.test.ts (template: spawn daemon with LOCALBOT_USER_DATA_DIR; NDJSON framing; tools/call JSON-RPC; audit JSONL read)
@tests/playwright/scheduler-notification.test.ts (template: 4-case E2E pattern from Phase 6 Plan 03)
@playwright.config.ts (daemon-smoke project config)
@src/renderer/styles/app.css (existing block styles for tool_result + .block-vault-* + .settings-tab-obsidian)
@08-PATTERNS.md (file-by-file pattern map with code excerpts for BrowserXxxBlock + BotSettingsBrowserTab + state/browser.ts)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 6 Browser*Block components + src/renderer/state/browser.ts + BotSettingsBrowserTab + extend MessageBlock + extend BotSettingsPage 6th tab + styles</name>
  <files>
    - src/renderer/components/BrowserNavigateBlock.tsx
    - src/renderer/components/BrowserClickBlock.tsx
    - src/renderer/components/BrowserTypeBlock.tsx
    - src/renderer/components/BrowserFillFormBlock.tsx
    - src/renderer/components/BrowserScreenshotBlock.tsx
    - src/renderer/components/BrowserEvaluateBlock.tsx
    - src/renderer/state/browser.ts
    - src/renderer/components/BotSettingsBrowserTab.tsx
    - src/renderer/components/MessageBlock.tsx
    - src/renderer/components/BotSettingsPage.tsx
    - src/renderer/styles/app.css
  </files>
  <read_first>
    - src/renderer/components/MessageBlock.tsx (switch dispatcher at L57-105; exhaustiveness check; pattern for adding 6 new cases)
    - src/renderer/components/BotSettingsPage.tsx (TabId union at L20-27; TAB_IDS + TAB_LABELS at L23-26; debounce save pattern at L195-200 with 250ms setTimeout; parseTabFromHash + useEffect URL sync; existing 5 tabs general/permissions/schedule/history/obsidian)
    - src/renderer/components/BotSettingsObsidianTab.tsx (template: debounced-save + parseGlobList + combined allowlist + tool checkboxes shape — mirror exactly for browser tab)
    - src/renderer/components/VaultReadBlock.tsx (template for BrowserNavigateBlock: path header + bytes footer + collapse pattern with utf8ByteLength + sliceAtUtf8Bytes)
    - src/renderer/components/VaultWriteBlock.tsx (template for BrowserScreenshotBlock: small payload shape; small payload shape for BrowserTypeBlock too)
    - src/renderer/components/VaultSearchBlock.tsx (template for BrowserClickBlock + BrowserFillFormBlock: match-list / collapse pattern)
    - src/renderer/state/vault.ts (template for state/browser.ts: module-scope + 200ms throttle + ensureDaemonSubscription + EVENT_BROWSER_CONFIG_UPDATED)
    - src/renderer/state/bots.ts (updateBot pattern from Phase 4)
    - src/shared/types.ts (BotConfig browserAllow/browserDeny/ssrfAllowInternal + 6 MessageBlock variants)
    - src/shared/window.d.ts (LocalbotApi.browser namespace; api.browser.getScreenshot + api.bot.update pattern)
    - src/renderer/styles/app.css (existing .block-tool-result + .block-result-error + .block-vault-read + .settings-tab-obsidian + .vault-global-modal styles)
    - 08-PATTERNS.md (file-by-file pattern map for BrowserXxxBlock + BotSettingsBrowserTab + state/browser.ts)
  </read_first>
  <action>
    Eleven coordinated changes:

    1) src/renderer/components/BrowserNavigateBlock.tsx (NEW, ~70 lines). React component rendering a browser.navigate result. Props: `{block: Extract<MessageBlock, {kind:'browser_navigate'}>}`. Mirrors VaultReadBlock pattern. Internal: COLLAPSE_THRESHOLD_BYTES = 500; utf8ByteLength + sliceAtUtf8Bytes helpers (or import from a shared utils). Destructure {url, status, title, text, bytes, durationMs} from block. Compute shouldCollapse = utf8ByteLength(text) > COLLAPSE_THRESHOLD_BYTES; useState for expanded; visible = shouldCollapse && !expanded ? sliceAtUtf8Bytes(text, COLLAPSE_THRESHOLD_BYTES) + '\n\n[... truncated]' : text. Render: <div className="block-browser-navigate" data-block-kind="browser_navigate" data-browser-url={url}> <div className="block-header"> <span className="block-tool-name">browser.navigate</span> <span className="block-browser-url">{url}</span> {status ? <span className={`block-browser-status block-status-${Math.floor(status / 100)}xx`}>{status}</span> : null} {bytes ? <span className="block-browser-bytes">({bytes} bytes)</span> : null} <span className="block-browser-duration">{durationMs}ms</span> </div> {title ? <div className="block-browser-title">{title}</div> : null} <pre className="block-browser-content">{visible}</pre> {shouldCollapse && <button type="button" className="collapse-toggle" onClick={() => setExpanded(v => !v)}>{expanded ? 'Show less' : `Show all ${bytes} bytes`}</button>} </div>.

    2) src/renderer/components/BrowserClickBlock.tsx (NEW, ~50 lines). Props: `{block: Extract<MessageBlock, {kind:'browser_click'}>}`. Destructure {selector, text, durationMs}. Render: <div className="block-browser-click" data-block-kind="browser_click" data-selector={selector}> <div className="block-header"> <span className="block-tool-name">browser.click</span> <span className="block-browser-selector">{selector}</span> <span className="block-browser-duration">{durationMs}ms</span> </div> <pre className="block-browser-content">{text}</pre> </div>. Collapse at COLLAPSE_THRESHOLD_BYTES = 200 (smaller cap; click result text is usually small).

    3) src/renderer/components/BrowserTypeBlock.tsx (NEW, ~50 lines). Props: `{block: Extract<MessageBlock, {kind:'browser_type'}>}`. Destructure {selector, textBytes, submitted, durationMs}. Render: <div className="block-browser-type" data-block-kind="browser_type" data-selector={selector}> <div className="block-header"> <span className="block-tool-name">browser.type</span> <span className="block-browser-selector">{selector}</span> <span className="block-browser-text-bytes">({textBytes} bytes)</span> {submitted ? <span className="block-browser-submitted">(submitted)</span> : null} <span className="block-browser-duration">{durationMs}ms</span> </div> </div>. NO <pre> body; one-line summary. NEVER include the typed text (Pitfall 5).

    4) src/renderer/components/BrowserFillFormBlock.tsx (NEW, ~70 lines). Props: `{block: Extract<MessageBlock, {kind:'browser_fill_form'}>}`. Destructure {fieldCount, submitted, text, durationMs}. Render: <div className="block-browser-fill-form" data-block-kind="browser_fill_form"> <div className="block-header"> <span className="block-tool-name">browser.fill_form</span> <span className="block-browser-field-count">{fieldCount} fields</span> {submitted ? <span className="block-browser-submitted">(submitted)</span> : null} <span className="block-browser-duration">{durationMs}ms</span> </div> <pre className="block-browser-content">{text}</pre> </div>. Collapse at COLLAPSE_THRESHOLD_BYTES = 500. NEVER include field values.

    5) src/renderer/components/BrowserScreenshotBlock.tsx (NEW, ~80 lines). Props: `{block: Extract<MessageBlock, {kind:'browser_screenshot'}>}`. Destructure {runId, n, fileUri, bytes, durationMs}. Render: <div className="block-browser-screenshot" data-block-kind="browser_screenshot"> <div className="block-header"> <span className="block-tool-name">browser.screenshot</span> <span className="block-browser-screenshot-ref">{runId}/{n}</span> <span className="block-browser-bytes">({bytes} bytes)</span> <span className="block-browser-duration">{durationMs}ms</span> </div> <img src={fileUri} alt={`screenshot ${runId}/${n}`} className="block-browser-screenshot-img" loading="lazy" /> </div>. fileUri is app://localhost/screenshots/<runId>/<n>.png; the renderer relies on the protocol.handle('app') handler in src/main/ipc/browser.ts (registered in Plan 2). Cap-height CSS in app.css.

    6) src/renderer/components/BrowserEvaluateBlock.tsx (NEW, ~60 lines). Props: `{block: Extract<MessageBlock, {kind:'browser_evaluate'}>}`. Destructure {expressionBytes, resultBytes, result, durationMs}. Render: <div className="block-browser-evaluate" data-block-kind="browser_evaluate"> <div className="block-header"> <span className="block-tool-name">browser.evaluate</span> <span className="block-browser-expression-bytes">{expressionBytes} byte expression</span> <span className="block-browser-result-bytes">{resultBytes} byte result</span> <span className="block-browser-duration">{durationMs}ms</span> </div> <pre className="block-browser-eval-result">{result}</pre> </div>. Collapse at COLLAPSE_THRESHOLD_BYTES = 500. NEVER include expression source.

    7) src/renderer/state/browser.ts (NEW, ~150 lines). Module-scope state + subscribers + ensureDaemonSubscription, mirrors src/renderer/state/vault.ts. Define `export interface BrowserConfigState { browserAllow: string[]; browserDeny: string[]; ssrfAllowInternal: boolean; loading: boolean; error: string | null; }`. Module-scope: `const state: BrowserConfigState = { browserAllow: [], browserDeny: [], ssrfAllowInternal: false, loading: false, error: null }; const subscribers = new Set<() => void>(); let subscribedToDaemon = false; let lastRefreshAt = 0; const REFRESH_MIN_INTERVAL_MS = 200;`. async function refresh(): read state; if Date.now() - lastRefreshAt < REFRESH_MIN_INTERVAL_MS return; lastRefreshAt = Date.now(); if (!window.localbot) return; try { state.loading = true; state.error = null; const res = await window.localbot.bot.list(); if (Array.isArray(res?.bots)) { /* extract current bot config if available */ } else { state.error = res?.error ?? 'failed'; } } catch (e) { state.error = (e as Error).message; } finally { state.loading = false; notify(); } (NOTE: per-bot config comes from the bot.update event; this hook mirrors the existing pattern). function notify(): for (const s of subscribers) s(); function subscribe(fn): return () => subscribers.delete(fn); function ensureDaemonSubscription(): subscribe to EVENT_BROWSER_CONFIG_UPDATED + EVENT_BROWSER_PAGE_CLOSED; call refresh() once on first mount. Export: `export function useBrowserConfig(): BrowserConfigState { const [s, setS] = useState<BrowserConfigState>(getState()); useEffect(() => { const off = subscribe(() => setS({...getState()})); return off; }, []); return s; }` plus `export const browserActions = { refresh, getScreenshot: async (req: BrowserScreenshotRequest): Promise<BrowserScreenshotResult> => { if (!window.localbot) return {ok: false, error: 'no_ipc'}; return await window.localbot.browser.getScreenshot(req); }, deleteContext: async (req: BrowserDeleteContextRequest): Promise<{ok: boolean; error?: string}> => { if (!window.localbot) return {ok: false, error: 'no_ipc'}; return await window.localbot.browser.deleteContext(req); } };`. Also export `__test__.reset()` for unit tests.

    8) src/renderer/components/BotSettingsBrowserTab.tsx (NEW, ~250 lines). React component for the Browser tab content. Props: `{bot: BotConfig; onPatch: (patch: Partial<BotConfig>) => void; saving: boolean; error: string | null}`. Internals:
       - useState for browserAllowText (joined from bot.browserAllow with \n), browserDenyText (joined from bot.browserDeny with \n), ssrfAllowInternal (initialized from bot.ssrfAllowInternal ?? false).
       - useState for toolCheckboxes: { 'browser.navigate': true/false, 'browser.click': true/false, 'browser.type': true/false, 'browser.fill_form': true/false, 'browser.screenshot': true/false, 'browser.evaluate': true/false } — initialized from bot.allowlist membership.
       - Debounce ref + scheduleSave() (mirror Phase 7 Obsidian tab at L195-200): parse browserAllowText into string[] (split \n, trim, filter empty); same for browserDenyText; nonBrowser = initialAllowlist.filter((t) => !t.startsWith('browser.')); nextBrowser = BROWSER_TOOLS.filter((t) => toolCheckboxes[t]); combinedAllowlist = [...nonBrowser, ...nextBrowser]; patch: {browserAllow: parsedAllow, browserDeny: parsedDeny, ssrfAllowInternal, allowlist: combinedAllowlist}; onPatch(patch) on debounce (250ms).
       - BROWSER_TOOLS constant = ['browser.navigate', 'browser.click', 'browser.type', 'browser.fill_form', 'browser.screenshot', 'browser.evaluate'] (6 entries).
       - Render: <div className="settings-tab settings-tab-browser"> with 4 sections:
         (a) <section><h3>URL allow globs</h3><textarea placeholder="https://example.com/**" value={browserAllowText} onChange={...} /></section>
         (b) <section><h3>URL deny globs</h3><textarea placeholder="https://example.com/admin/**" value={browserDenyText} onChange={...} /></section>
         (c) <section><h3>SSRF opt-out</h3><label><input type="checkbox" checked={ssrfAllowInternal} onChange={...} /> Allow internal IPs (localhost, RFC1918, 169.254). WARNING: enables bot to read local services.</label></section>
         (d) <section><h3>Browser tools</h3>6 checkboxes for the 6 browser.* tools</section>
         (e) <div className="save-status">{saving ? 'Saving...' : error ? `Error: ${error}` : 'Saved'}</div>

    9) src/renderer/components/MessageBlock.tsx (MODIFY): in the switch statement (L57-105), BEFORE the `default` case, add 6 new cases AFTER the existing vault_write case:
       - case 'browser_navigate': return <BrowserNavigateBlock block={block} />;
       - case 'browser_click': return <BrowserClickBlock block={block} />;
       - case 'browser_type': return <BrowserTypeBlock block={block} />;
       - case 'browser_fill_form': return <BrowserFillFormBlock block={block} />;
       - case 'browser_screenshot': return <BrowserScreenshotBlock block={block} />;
       - case 'browser_evaluate': return <BrowserEvaluateBlock block={block} />;
       Import all 6 new components at the top of the file (L11-18 alongside VaultReadBlock etc.). Verify exhaustiveness check `const _exhaustive: never = block;` still compiles (the new variants must extend MessageBlock).

    10) src/renderer/components/BotSettingsPage.tsx (MODIFY): four changes.
       (a) Extend TabId union (L20) with `'browser'`. Add 'browser' to TAB_IDS. Add `browser: 'Browser'` to TAB_LABELS.
       (b) parseTabFromHash must recognize 'browser' as a valid tab.
       (c) In the JSX render where the 5 existing tabs render (general/permissions/schedule/history/obsidian), add a sixth <button> + conditional render <BotSettingsBrowserTab .../> when activeTab === 'browser'. Pass bot + onPatch (existing persistPatch) + saving + error.
       (d) Import BotSettingsBrowserTab at top of file.

    11) src/renderer/styles/app.css (MODIFY): append at end of file:
       - .block-browser-navigate, .block-browser-click, .block-browser-type, .block-browser-fill-form, .block-browser-screenshot, .block-browser-evaluate { /* mirror .block-vault-read + monospace pre */ }
       - .block-browser-status-2xx { color: #2a8a2a; }
       - .block-browser-status-3xx { color: #b8860b; }
       - .block-browser-status-4xx, .block-browser-status-5xx { color: #d33; }
       - .block-browser-screenshot-img { max-height: 400px; max-width: 100%; object-fit: contain; display: block; margin: 8px 0; border: 1px solid #ddd; }
       - .block-browser-submitted { background: #e8f5e9; color: #2a8a2a; padding: 0 4px; border-radius: 3px; font-size: 11px; margin-left: 4px; }
       - .settings-tab-browser section { margin-bottom: 16px; }
       - .settings-tab-browser input, .settings-tab-browser textarea { width: 100%; padding: 6px; font-family: monospace; }
       - .settings-tab-browser textarea { min-height: 80px; resize: vertical; }
       - .settings-tab-browser label { display: flex; align-items: center; gap: 8px; }

    Avoid: hardcoding URLs in components (always read from block.url). Use loadState hooks (never window.localbot.browser directly in components — go through browserActions in state/browser.ts). Avoid: opening the BotSettingsBrowserTab automatically on app start (user must navigate to the tab). Avoid: per-bot ssrfAllowInternal defaulting to true (false by default; the checkbox explicitly toggles it). Avoid: missing aria-labels on the new inputs (use <label htmlFor> or aria-label). Avoid: emitting audit-leaking fields in any block component (browser_navigate displays ONLY the normalized url; never the raw args.url which may include a query string).

    Validation step (REQUIRED before Task 2): after all 11 changes, run `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` from D:/Claude/Grokbot. Must exit 0 (TS build clean; new components + BotSettingsPage + state/browser.ts + MessageBlock wiring all type-check). Then run `node -e "const r = require('./src/renderer/state/browser.ts')"` — wait, this is TS, so the validation is the build alone. Instead, run `grep -c 'BrowserNavigateBlock\|BrowserClickBlock\|BrowserTypeBlock\|BrowserFillFormBlock\|BrowserScreenshotBlock\|BrowserEvaluateBlock\|BotSettingsBrowserTab' src/renderer/components/MessageBlock.tsx src/renderer/components/BotSettingsPage.tsx src/renderer/state/browser.ts 2>&1` — must return >= 8 total matches.
  </action>
  <acceptance_criteria>
    - 6 new Browser*Block components exist on disk
    - src/renderer/state/browser.ts exists; exports useBrowserConfig hook + browserActions object with refresh + getScreenshot + deleteContext
    - src/renderer/components/BotSettingsBrowserTab.tsx exists; renders URL allow/deny textareas + SSRF opt-out checkbox + 6 browser-tool checkboxes; debounces saves via onPatch callback
    - src/renderer/components/MessageBlock.tsx switch covers 6 new browser_* variants; exhaustiveness check compiles
    - src/renderer/components/BotSettingsPage.tsx TabId union includes 'browser'; TAB_IDS includes 'browser'; URL hash sync handles 'browser'; renders BotSettingsBrowserTab when activeTab === 'browser'
    - src/renderer/styles/app.css has styles for .block-browser-* (6 selectors) + .block-browser-screenshot-img cap-height + .settings-tab-browser section styles
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean)
    - `grep -c 'BrowserNavigateBlock\|BrowserClickBlock\|BrowserTypeBlock\|BrowserFillFormBlock\|BrowserScreenshotBlock\|BrowserEvaluateBlock\|BotSettingsBrowserTab' src/renderer/components/MessageBlock.tsx src/renderer/components/BotSettingsPage.tsx src/renderer/state/browser.ts 2>&1 | awk '{ sum += $1 } END { if (sum < 8) exit 1 }'` succeeds (all symbols wired)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30 && grep -l 'settings-tab-browser\|block-browser-navigate\|block-browser-click\|block-browser-type\|block-browser-fill-form\|block-browser-screenshot\|block-browser-evaluate\|BotSettingsBrowserTab\|BrowserNavigateBlock' src/renderer/components/MessageBlock.tsx src/renderer/components/BotSettingsPage.tsx src/renderer/components/BotSettingsBrowserTab.tsx src/renderer/components/BrowserNavigateBlock.tsx src/renderer/styles/app.css 2>&1</automated>
    <fails_when>tsc --noEmit reports any TS error, or grep returns empty for any of the 5 files</fails_when>
  </verify>
  <done>
    All 11 UI changes committed. TS build clean. 6 Browser*Block components render path/content/bytes/text/result/img inline. state/browser.ts provides useBrowserConfig hook with EVENT_BROWSER_CONFIG_UPDATED subscription + browserActions.getScreenshot/deleteContext. BotSettingsBrowserTab renders 4 sections (URL allow/deny textareas + SSRF opt-out checkbox + 6 tool checkboxes) + debounced save. MessageBlock dispatcher covers all 6 new variants; exhaustiveness check compiles. BotSettingsPage extends TabId union with 'browser' and routes to BotSettingsBrowserTab. app.css has styles for all new components. Pre-existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: fake-m3-server streamBrowser*ToolUse helpers + Playwright browser-automation.test.ts (6 cases) + extend playwright.config.ts</name>
  <files>
    - tests/playwright/fake-m3-server.ts
    - tests/playwright/browser-automation.test.ts
    - playwright.config.ts
  </files>
  <read_first>
    - tests/playwright/fake-m3-server.ts (existing streamToolUseResponse at L165-234; streamVaultReadToolUse at L957-1056 as closest analog for the 6 new browser helpers; SSE envelope pattern with message_start + content_block_start + input_json_delta + content_block_stop + message_delta + message_stop; writeSse helper; SSE_HEADERS constant; CHUNK_SIZE = 16 input-JSON splitting)
    - tests/playwright/obsidian-integration.test.ts (template for browser-automation.test.ts spawn pattern)
    - tests/playwright/daemon-tools.test.ts (template: spawn daemon with LOCALBOT_USER_DATA_DIR; NDJSON framing; tools/call JSON-RPC; audit JSONL read)
    - tests/playwright/scheduler-notification.test.ts (template: 4-case E2E pattern from Phase 6 Plan 03)
    - playwright.config.ts (daemon-smoke project config + testDir pattern; add browser-automation.test.ts to the project's testMatch)
    - 08-RESEARCH.md §Validation Architecture (test framework + sample commands)
  </read_first>
  <action>
    Three coordinated changes:

    1) tests/playwright/fake-m3-server.ts (MODIFY): add 6 new helper functions that mirror streamVaultReadToolUse at L957-1056. Each helper takes (res, opts) where opts includes toolUseId (optional; default `toolu_browser_<action>_<Date.now().toString(36)>`), input (the tool input object), and any tool-specific extras. The helpers build the SSE chunk array in the same shape as streamVaultReadToolUse with hardcoded tool name. Export them at the bottom of the file. ~240 lines total (6 helpers x ~40 each).
       - streamBrowserNavigateToolUse(res, opts: { toolUseId?: string; input: { url: string } }): tool_use block name='browser.navigate' input=opts.input; tool_result on the next turn returns {url: hostname+pathname (strip query), status, title, text, bytes, durationMs}.
       - streamBrowserClickToolUse(res, opts: { toolUseId?: string; input: { selector: string }; resultText?: string }): tool_use block name='browser.click' input=opts.input; tool_result returns {selector, hostname, path, text: opts.resultText ?? 'click result text', durationMs}.
       - streamBrowserTypeToolUse(res, opts: { toolUseId?: string; input: { selector: string; text: string; submit?: boolean } }): tool_use block name='browser.type' input=opts.input; tool_result returns {selector, textBytes: Buffer.byteLength(input.text, 'utf8'), hostname, path, submitted: input.submit === true, durationMs}.
       - streamBrowserFillFormToolUse(res, opts: { toolUseId?: string; input: { fields: Array<{selector:string;value:string}>; submit?: {selector:string} } }): tool_use block name='browser.fill_form' input=opts.input; tool_result returns {fieldCount: input.fields.length, hostname, path, submitted: !!input.submit, text: 'form filled', durationMs}.
       - streamBrowserScreenshotToolUse(res, opts: { toolUseId?: string; input: { n?: string; fullPage?: boolean } }): tool_use block name='browser.screenshot' input=opts.input; tool_result returns {path: `<runId>/<input.n ?? timestamp>.png`, absolutePath: `<userData>/screenshots/<runId>/<input.n>.png`, bytes: 12345, hostname, path, fullPage: input.fullPage === true, durationMs}.
       - streamBrowserEvaluateToolUse(res, opts: { toolUseId?: string; input: { expression: string }; result?: string }): tool_use block name='browser.evaluate' input=opts.input; tool_result returns {expressionBytes: Buffer.byteLength(input.expression, 'utf8'), resultBytes: Buffer.byteLength(opts.result ?? 'eval result', 'utf8'), result: opts.result ?? 'eval result', hostname, path, durationMs}.
       Follow the EXACT SSE envelope shape from streamVaultReadToolUse (message_start → content_block_start with type:'tool_use' → input_json_delta with partial JSON chunks → content_block_stop → message_delta → message_stop; include tool_result in the NEXT message for each call so the renderer sees tool_use + tool_result in sequence).

    2) tests/playwright/browser-automation.test.ts (NEW, ~400 lines). Playwright test suite that exercises the full vertical slice end-to-end. 6 test cases covering all 6 TOOL-07..12 requirements.
       - Setup (beforeAll): create mkdtemp userDataDir + mkdtemp screenshotsDir (pre-create <userData>/screenshots/ so the daemon can write to it); start a Node http.createServer test page server on 127.0.0.1:<random_port>; the server responds to GET / with an HTML page containing <h1>Example Domain</h1><p>hello world</p> and a <input id="name"> + <button id="go">Go</button>; responds to GET /admin with HTTP 403; set env LOCALBOT_USER_DATA_DIR=<userDataDir> + LOCALBOT_ALLOW_INTERNAL_HOSTS=1; spawn daemon subprocess via existing helper.
       - bots/create alpha with allowlist: ['browser.navigate']; bots/update alpha with browserAllow: [`http://127.0.0.1:<port>/**`] and ssrfAllowInternal:true (the test relies on the SSRF opt-out to allow 127.0.0.1).
       - Case 1: browser_navigate happy path:
         * sendMessage('Navigate to the test page'); fake-m3-server streams streamBrowserNavigateToolUse with input.url=`http://127.0.0.1:<port>/`.
         * Assert: <div class="block-browser-navigate"> contains 'hello world' + the normalized url `<port>` (no query string); audit JSONL line for browser.navigate carries {tool:'browser.navigate', params:{hostname:'127.0.0.1', path:'/', status:200, duration_ms:<n>}}; no query string in audit; no full URL in audit; no HTML in audit.
       - Case 2: browser_click + resulting state:
         * bots/update alpha allowlist: ['browser.navigate', 'browser.click'].
         * sendMessage('Click the go button'); fake-m3-server streams streamBrowserClickToolUse with input.selector='#go'; tool_result returns {text: 'clicked go'}.
         * Assert: <div class="block-browser-click"> contains 'clicked go' + selector '#go'; audit row carries {tool:'browser.click', params:{hostname:'127.0.0.1', path:'/', duration_ms:<n>}}; no HTML in audit.
       - Case 3: browser_type + submitted:
         * bots/update alpha allowlist: ['browser.navigate', 'browser.type'].
         * sendMessage('Type into the name input'); fake-m3-server streams streamBrowserTypeToolUse with input.selector='#name', input.text='secret_password', input.submit=true; tool_result returns {textBytes: 15, submitted: true}.
         * Assert: <div class="block-browser-type"> contains '15 bytes' + '(submitted)' badge; NEVER contains 'secret_password' in the DOM; audit row carries {tool:'browser.type', params:{hostname:'127.0.0.1', path:'/', textBytes:15, duration_ms:<n>}}; audit NEVER contains 'secret_password'.
       - Case 4: browser_screenshot + app:// URI:
         * bots/update alpha allowlist: ['browser.navigate', 'browser.screenshot'].
         * sendMessage('Take a screenshot'); fake-m3-server streams streamBrowserScreenshotToolUse with input.n='test'; tool_result returns {path: 'test.png', absolutePath, bytes: 12345}.
         * Assert: <img src="app://localhost/screenshots/<runId>/test.png"> is rendered; <div class="block-browser-screenshot"> contains '12345 bytes' + 'test.png'; screenshot file exists at <userData>/screenshots/<runId>/test.png; audit row carries {tool:'browser.screenshot', params:{hostname:'127.0.0.1', path:'/', screenshotBytes:12345, duration_ms:<n>}}; never the PNG bytes in audit.
       - Case 5: browser_evaluate + result cap:
         * bots/update alpha allowlist: ['browser.navigate', 'browser.evaluate'].
         * sendMessage('Run JS in the page'); fake-m3-server streams streamBrowserEvaluateToolUse with input.expression='document.body.innerText.length'; tool_result returns {result: '12', expressionBytes: 28, resultBytes: 2}.
         * Assert: <div class="block-browser-evaluate"> contains '28 byte expression' + '2 byte result' + '12'; NEVER contains the expression source in the DOM (audit NEVER contains 'document.body.innerText'); audit row carries {tool:'browser.evaluate', params:{hostname:'127.0.0.1', path:'/', expressionBytes:28, duration_ms:<n>}}.
       - Case 6: browser_fill_form + multiple fields:
         * bots/update alpha allowlist: ['browser.navigate', 'browser.fill_form'].
         * sendMessage('Fill the form'); fake-m3-server streams streamBrowserFillFormToolUse with input.fields=[{selector:'#name', value:'Alice'},{selector:'#email', value:'alice@example.com'}]; tool_result returns {fieldCount: 2, submitted: false, text: 'form filled'}.
         * Assert: <div class="block-browser-fill-form"> contains '2 fields' + 'form filled'; NEVER contains 'Alice' or 'alice@example.com' in the DOM; audit row carries {tool:'browser.fill_form', params:{hostname:'127.0.0.1', path:'/', fieldCount:2, duration_ms:<n>}}; audit NEVER contains 'Alice' or 'alice@example.com'.
       - Teardown (afterAll): kill daemon + close test server; rmSync(userDataDir, {recursive: true, force: true}).
       - Mirror the NDJSON framing pattern from tests/playwright/daemon-tools.test.ts; use the daemon spawn helper from there.

    3) playwright.config.ts (MODIFY): in the daemon-smoke project's testMatch array, add 'browser-automation.test.ts'. Verify the project's testDir points to tests/playwright. Do NOT add to the headed project (LOCALBOT_SMOKE_OK-gated) — these are pure daemon smokes.

    Avoid: any headed-Electron assertion (LOCALBOT_SMOKE_OK-gated; strictly daemon-smoke project). Avoid: depending on real external sites (use Node http.createServer). Avoid: putting absolute paths or query strings in audit assertions (read JSONL, parse params, assert it does NOT contain the test URL absolute path or query string). Avoid: skipping the audit minimization check — every test case MUST read <userData>/audit.jsonl and verify the params shape. Avoid: real Chromium download in this CI run (assume Chromium is already installed at %LOCALAPPDATA%\ms-playwright\chromium-...; if not, the test setup spawns a one-time `npx playwright install chromium` once). Avoid: putting typed text in any DOM assertion (Pitfall 5).

    Validation step (REQUIRED): after all 3 changes, run `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts browser-automation.test.ts 2>&1 | tail -30` from D:/Claude/Grokbot. Exit 0 + all 6 cases passing is the gate. Then run `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` to confirm no TS regression. Then run `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot 2>&1 | tail -20` to confirm no Vitest regression.
  </action>
  <acceptance_criteria>
    - tests/playwright/fake-m3-server.ts exports 6 streamBrowser*ToolUse helpers
    - All 6 helpers follow the same SSE envelope shape as the existing streamVaultReadToolUse
    - tests/playwright/browser-automation.test.ts has 6 passing cases (browser.navigate happy + browser.click + browser.type + browser.screenshot + browser.evaluate + browser.fill_form)
    - Every case asserts audit row minimization (no full URL with query string; no typed text; no expression source; no PNG bytes; only normalized {hostname, path, status?, duration_ms, screenshotBytes?, expressionBytes?, fieldCount?})
    - playwright.config.ts daemon-smoke project includes browser-automation.test.ts in testMatch
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean)
    - `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot 2>&1 | tail -20` exits 0 (full Vitest suite green; Plan 08-01's 30 + Plan 08-02's 24 + all pre-existing tests unbroken)
    - `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts browser-automation.test.ts 2>&1 | tail -30` exits 0 with 6 passing cases
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts browser-automation.test.ts 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or "0 passed" or "failed" in summary, or fewer than 6 passing cases</fails_when>
  </verify>
  <done>
    All 3 E2E changes committed. fake-m3-server exposes 6 new browser tool_use streaming helpers matching the existing SSE envelope. browser-automation.test.ts has 6 passing cases covering all 6 browser tools; every case asserts audit minimization. playwright.config.ts daemon-smoke project includes the new test. TS build clean. Full Vitest suite green. Full Phase 8 vertical slice proven end-to-end (config + tools + IPC + UI + E2E).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to Main IPC | Renderer renders Browser*Block components from MessageBlock payloads; renderer never calls browser.* IPC directly except via state/browser.ts browserActions. |
| Renderer to Daemon (browser config) | BotSettingsBrowserTab sends browserAllow/browserDeny/ssrfAllowInternal via bots/update; main proxies; daemon validateConfig + writeConfigPatch enforce ALLOWED_CONFIG_KEYS. |
| Renderer to Daemon (screenshots) | BrowserScreenshotBlock renders <img src={app://localhost/screenshots/...}>; protocol.handle('app') in src/main/ipc/browser.ts resolves to <userData>/screenshots/<runId>/<n>.png with regex guards. |
| Playwright test to real daemon | Test uses mkdtemp userData + LOCALBOT_ALLOW_INTERNAL_HOSTS=1 + Node http.createServer for the test page; no real user sites at risk. |
| Playwright E2E to audit log | Test reads <userData>/audit.jsonl after each call and verifies the params shape (no full URL, no query string, no typed text, no expression source, no PNG bytes). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-8-16 | Information Disclosure | src/renderer/components/BrowserNavigateBlock.tsx | low | mitigate | Block displays normalized hostname+pathname; renderer never receives the full args.url (which may include a query string); Pitfall 5 |
| T-8-17 | Information Disclosure | src/renderer/components/BrowserTypeBlock.tsx | low | mitigate | Block displays textBytes count only; NEVER the typed text; Pitfall 5 |
| T-8-18 | Information Disclosure | src/renderer/components/BrowserEvaluateBlock.tsx | low | mitigate | Block displays expressionBytes + resultBytes counts only; NEVER the expression source; Pitfall 5 |
| T-8-19 | Information Disclosure | src/renderer/components/BrowserFillFormBlock.tsx | low | mitigate | Block displays fieldCount + resulting text only; NEVER individual field values; Pitfall 5 |
| T-8-20 | Spoofing | src/renderer/components/BotSettingsBrowserTab.tsx | low | mitigate | Renderer parses newline-delimited glob text into string[] before sending; daemon validateConfig enforces array-of-strings; ssrfAllowInternal is a single boolean checkbox (not a free text field) |
| T-8-21 | DoS | src/renderer/components/App.tsx | low | mitigate | BotSettingsBrowserTab is rendered only when the user navigates to the 'browser' tab; no auto-mount on app start |
| T-8-22 | Tampering | tests/playwright/browser-automation.test.ts | medium | mitigate | Test reads audit JSONL after each call and asserts params shape (no full URL with query string; no typed text; no expression source; no PNG bytes); regex /^[A-Za-z0-9_-]+$/ for hostname; /^\/[A-Za-z0-9/._-]*$/ for pathname |
| T-8-23 | Tampering | src/renderer/styles/app.css | low | accept | CSS-only changes; no security impact; visual regression risk only |
| T-8-24 | DoS | tests/playwright/browser-automation.test.ts | low | mitigate | Teardown rmSync(userDataDir, {recursive:true, force:true}); daemon subprocess killed in afterAll; test page server closed in afterAll |
| T-8-25 | Information Disclosure | src/main/ipc/browser.ts protocol.handle('app') | medium | mitigate | runId + n validated against safe regex /^[a-zA-Z0-9_-]{1,64}$/ + /^[a-zA-Z0-9._-]{1,32}$/ before resolving to <userData>/screenshots/<runId>/<n>.png; no path traversal possible |

## Threat-to-Test Mapping

| Threat | Test coverage |
|--------|---------------|
| T-8-16 (normalized URL display) | browser-automation.test.ts case 1 (audit shape + DOM assertion) |
| T-8-17 (typed text minimization) | browser-automation.test.ts case 3 (DOM never contains 'secret_password' + audit shape) |
| T-8-18 (expression minimization) | browser-automation.test.ts case 5 (DOM + audit) |
| T-8-19 (field values minimization) | browser-automation.test.ts case 6 (DOM never contains field values + audit shape) |
| T-8-20 (per-bot glob parse) | bot update via bots/update; daemon ALLOWED_CONFIG_KEYS enforcement |
| T-8-22 (audit minimization) | browser-automation.test.ts cases 1-6 (every case reads audit JSONL + asserts shape) |
| T-8-25 (app:// path traversal) | regex guards in src/main/ipc/browser.ts (manual review + browser-automation.test.ts case 4 screenshot rendering) |
</threat_model>

<verification>
Phase-level checks after Plan 3 execution (full Phase 8 gate):
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot 2>&1 | tail -30` exits 0 (full Vitest suite green; Plan 08-01's 30 + Plan 08-02's 24 + Plan 08-03 has 0 new Vitest tests + all pre-existing tests unbroken; total target >= 270 passing tests)
- `npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30` exits 0 (TS build clean across all renderer + main + shared)
- `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts browser-automation.test.ts 2>&1 | tail -30` exits 0 with 6 passing cases
- `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts 2>&1 | tail -30` exits 0 (full Playwright daemon-smoke suite green)
- `grep -c 'BrowserNavigateBlock\|BrowserClickBlock\|BrowserTypeBlock\|BrowserFillFormBlock\|BrowserScreenshotBlock\|BrowserEvaluateBlock\|BotSettingsBrowserTab\|streamBrowserNavigateToolUse\|streamBrowserClickToolUse\|streamBrowserTypeToolUse\|streamBrowserFillFormToolUse\|streamBrowserScreenshotToolUse\|streamBrowserEvaluateToolUse\|settings-tab-browser\|block-browser-navigate\|block-browser-click\|block-browser-type\|block-browser-fill-form\|block-browser-screenshot\|block-browser-evaluate' src/renderer/components/MessageBlock.tsx src/renderer/components/BotSettingsPage.tsx tests/playwright/fake-m3-server.ts tests/playwright/browser-automation.test.ts src/renderer/styles/app.css 2>&1 | awk '{ sum += $1 } END { if (sum < 30) exit 1 }'` succeeds (all symbols wired)
- `git ls-files -- src/renderer/components/BrowserNavigateBlock.tsx src/renderer/components/BrowserClickBlock.tsx src/renderer/components/BrowserTypeBlock.tsx src/renderer/components/BrowserFillFormBlock.tsx src/renderer/components/BrowserScreenshotBlock.tsx src/renderer/components/BrowserEvaluateBlock.tsx src/renderer/components/BotSettingsBrowserTab.tsx src/renderer/state/browser.ts tests/playwright/browser-automation.test.ts` exits 0 (9 new files tracked)
</verification>

<success_criteria>
- Phase 8 success criterion #1 (bot can navigate to a URL and return the rendered page contents) - PROVEN by browser-automation.test.ts case 1 + Plan 08-01's browser_navigate.cjs + browser_navigate.test.ts
- Phase 8 success criterion #2 (bot can click an element by CSS selector and observe resulting state) - PROVEN by browser-automation.test.ts case 2 + Plan 08-02's browser_actions.test.ts cases A-B
- Phase 8 success criterion #3 (bot can type into an input AND fill multiple form fields in a single tool call) - PROVEN by browser-automation.test.ts cases 3 + 6 + Plan 08-02's browser_actions.test.ts cases C-G
- Phase 8 success criterion #4 (bot can capture a viewport screenshot as a PNG and reference it in the chat) - PROVEN by browser-automation.test.ts case 4 + Plan 08-02's browser_screenshot.test.ts cases A-F + app:// protocol handler
- Phase 8 success criterion #5 (bot can execute arbitrary JavaScript in the page context and read the result) - PROVEN by browser-automation.test.ts case 5 + Plan 08-02's browser_evaluate.test.ts cases A-F

All 5 Phase 8 success criteria (covering TOOL-07..12) are now demonstrably TRUE.
</success_criteria>

<output>
Create `.planning/phases/08-browser-automation/08-03-SUMMARY.md` when done. After Plan 08-03 execution, run `/gsd-verify-work 8` to validate the phase against the success criteria.
</output>