---
phase: 01-skeleton-streaming-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - tsconfig.main.json
  - tsconfig.renderer.json
  - vite.config.ts
  - .gitignore
  - .nvmrc
  - src/shared/types.ts
  - src/shared/ipc-channels.ts
  - src/main/index.ts
  - src/main/window.ts
  - src/main/ipc/key.ts
  - src/main/ipc/chat.ts
  - src/main/llm/client.ts
  - src/main/llm/prompts.ts
  - src/main/audit/logger.ts
  - src/main/sessions/jsonl.ts
  - src/main/daemon/spawn.ts
  - src/main/daemon/protocol.ts
  - src/main/errors.ts
  - src/main/paths.ts
  - src/preload/index.ts
  - src/renderer/index.html
  - src/renderer/main.tsx
  - src/renderer/App.tsx
  - src/renderer/components/Chat.tsx
  - src/renderer/components/Composer.tsx
  - src/renderer/components/MessageBubble.tsx
  - src/renderer/components/KeyModal.tsx
  - src/renderer/components/ErrorBanner.tsx
  - src/renderer/state/messages.ts
  - src/renderer/styles/app.css
  - daemon/main.cjs
  - daemon/protocol.cjs
  - daemon/tools/registry.cjs
  - daemon/audit.cjs
  - resources/icon.svg
autonomous: true
requirements:
  - LLM-01
  - LLM-02
  - LLM-05
  - SEC-01
  - SEC-04
  - SEC-05
  - UI-02
user_setup:
  - service: minimax-m3
    why: "Streaming chat needs an M3 API key"
    env_vars:
      - name: M3_API_BASE
        source: "Defaults to https://api.MiniMax.io/v1; override in dev if proxying"
      - name: M3_MODEL
        source: "Optional override for the model id used in Phase 1 (defaults to M3's current flagship)"
    dashboard_config:
      - task: "Generate an API key"
        location: "MiniMax dashboard -> API keys"

estimate:
  tokens: 95000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "User launches the Localbot Electron app and sees a chat pane with the composer pinned to the bottom (per D-04)"
    - "On first launch the user is presented with a blocking modal; once they enter a key, click Test connection, and save, the key is persisted via safeStorage and the modal closes (per D-06/D-08, LLM-05, SEC-05)"
    - "User types a message and presses Send; the assistant's response streams token-by-token into an assistant bubble (per D-07, LLM-02, UI-02)"
    - "API key never appears in the renderer process — safeStorage round-trip happens in main and only the final stream result is forwarded via IPC (per D-05, SEC-05)"
    - "Tool daemon runs as a separate child process spawned by main via child_process.spawn(process.execPath, ['daemon/main.cjs']); main never invokes user shell commands directly (per D-09, SEC-01)"
    - "Every tools/call JSON-RPC round-trip results in an audit line appended to <userData>/audit/YYYY-MM-DD.jsonl with shape {ts,bot,tool,params,outcome,durationMs,error?} (per D-12, SEC-04)"
    - "Each user/assistant turn is appended to <userData>/sessions/global.jsonl; on next launch those messages load back into the chat (per D-13/D-14/D-15)"
    - "The tool daemon's unknown_tool response is logged to the audit JSONL under an error outcome with code 'unknown_tool' so Phase 2's real tools can reuse the audit hook without changes (per D-11)"
  artifacts:
    - path: "package.json"
      provides: "Workspace manifest with electron, electron-builder, @anthropic-ai/sdk, react@19, vite, typescript, vitest, @playwright/test in devDependencies"
      contains: "\"name\": \"localbot\""
    - path: "src/main/index.ts"
      provides: "Electron main entry that boots window + IPC + daemon"
      exports: ["app.whenReady handler", "window-all-closed handler"]
    - path: "src/main/window.ts"
      provides: "BrowserWindow factory using titleBarStyle:hidden + titleBarOverlay"
      exports: ["createMainWindow"]
    - path: "src/main/ipc/key.ts"
      provides: "safeStorage-backed key:get/key:set/key:probe IPC handlers"
      exports: ["registerKeyHandlers"]
    - path: "src/main/ipc/chat.ts"
      provides: "sendMessage + cancel IPC handlers, msgId-keyed AbortController map"
      exports: ["registerChatHandlers"]
    - path: "src/main/llm/client.ts"
      provides: "@anthropic-ai/sdk client pointed at M3 base URL with streaming + abort"
      exports: ["streamChat"]
    - path: "src/main/audit/logger.ts"
      provides: "Append-only NDJSON writer keyed by UTC day"
      exports: ["appendAuditLine", "auditDir"]
    - path: "src/main/sessions/jsonl.ts"
      provides: "Append/read for <userData>/sessions/global.jsonl"
      exports: ["appendMessage", "loadSession"]
    - path: "src/main/daemon/spawn.ts"
      provides: "spawnDaemon(): child_process.spawn + handshake + respawn loop"
      exports: ["spawnDaemon", "stopDaemon"]
    - path: "daemon/main.cjs"
      provides: "stdio JSON-RPC daemon entry, prints {kind:ready}, handles initialize + tools/list + tools/call + tools/cancel"
      contains: "kind === 'ready'"
    - path: "daemon/protocol.cjs"
      provides: "NDJSON framing read/write helpers"
      exports: ["readMessage", "writeMessage"]
    - path: "daemon/tools/registry.cjs"
      provides: "Phase 1 tool registry; tools/call returns {error:{code:'unknown_tool'}}"
      exports: ["callTool", "listTools"]
    - path: "daemon/audit.cjs"
      provides: "audit append helper that forwards to main-process directory"
      exports: ["appendAudit"]
    - path: "src/preload/index.ts"
      provides: "contextBridge that exposes window.localbot with sendMessage/cancel/key:get/key:set/key:probe and event subscriptions"
      exports: ["localbot"]
    - path: "src/renderer/components/Chat.tsx"
      provides: "Bubble list + composer + error banner, autoscroll on new message"
    - path: "src/renderer/components/Composer.tsx"
      provides: "Auto-grow textarea, Enter sends, Shift+Enter newline, Send morphs into Stop while streaming"
    - path: "src/renderer/components/KeyModal.tsx"
      provides: "Blocking first-launch modal with paste-key field, Test connection button, Get API key link"
    - path: "src/renderer/components/ErrorBanner.tsx"
      provides: "Dismissible top-of-chat error banner + daemon-status banner"
    - path: "src/shared/ipc-channels.ts"
      provides: "Channel-name constants (single source of truth)"
      exports: ["CHANNELS"]
  key_links:
    - from: "src/preload/index.ts"
      to: "src/main/ipc/chat.ts"
      via: "ipcRenderer.invoke(CHANNELS.SEND_MESSAGE, {content, msgId})"
      pattern: "CHANNELS\\.SEND_MESSAGE"
    - from: "src/main/ipc/chat.ts"
      to: "src/main/llm/client.ts"
      via: "streamChat({messages, system, signal}) where signal comes from AbortController per msgId"
      pattern: "streamChat\\("
    - from: "src/main/llm/client.ts"
      to: "@anthropic-ai/sdk"
      via: "anthropic.messages.stream({...}) with baseURL set to M3_API_BASE"
      pattern: "\\.messages\\.stream\\("
    - from: "src/main/daemon/spawn.ts"
      to: "daemon/main.cjs"
      via: "child_process.spawn(process.execPath, ['daemon/main.cjs'], {stdio:['pipe','pipe','inherit']})"
      pattern: "spawn\\(process\\.execPath, \\[?['\"']daemon"
    - from: "daemon/tools/registry.cjs"
      to: "daemon/audit.cjs"
      via: "tools/call writes one audit line via appendAudit({tool, params, outcome, durationMs, error})"
      pattern: "appendAudit\\("
    - from: "src/main/sessions/jsonl.ts"
      to: "<userData>/sessions/global.jsonl"
      via: "fs.appendFile with JSON.stringify + '\n'"
      pattern: "appendFile.*sessions"
---

<objective>
Scaffold the entire Localbot Walking Skeleton — Electron + React 19 + Vite + TypeScript — and wire the end-to-end chat → LLM → tool-daemon → audit-log loop with the locked one-way decisions landed first.

**Purpose:** Per Walking Skeleton mode, this plan IS the application's end-to-end tracer. Every architectural anchor from D-05, D-07, D-09, D-13 (one-way) and D-18 (costly) is established here, before any later phase can extend it.

**Output:** A runnable Electron app where the user can complete the first-launch key modal, send a message, see a streamed assistant response, fire a `tools/call` against the daemon, and observe an audit JSONL line on disk under `<userData>/audit/<UTC-day>.jsonl`.
</objective>

<user_story>
**As a** single user launching Localbot on my own PC,
**I want to** enter an API key once, send a chat message, and watch the assistant stream back while a tool daemon sits as a sibling process,
**so that** I have a private, local-first chat surface ready for the file tools and bot management that arrive in later phases.
</user_story>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-skeleton-streaming-chat/01-CONTEXT.md
@.planning/phases/01-skeleton-streaming-chat/SKELETON.md
@.claude/CLAUDE.md
@research/ARCHITECTURE.md
</context>

<tasks>

<task type="tracer">
  <name>Walking skeleton: scaffold + first-launch key + sendMessage streaming + tools/call audit round-trip</name>
  <files>
    package.json, tsconfig.json, tsconfig.main.json, tsconfig.renderer.json, vite.config.ts, .gitignore, .nvmrc,
    src/shared/types.ts, src/shared/ipc-channels.ts,
    src/main/index.ts, src/main/window.ts, src/main/paths.ts, src/main/errors.ts,
    src/main/ipc/key.ts, src/main/ipc/chat.ts,
    src/main/llm/client.ts, src/main/llm/prompts.ts,
    src/main/audit/logger.ts, src/main/sessions/jsonl.ts,
    src/main/daemon/spawn.ts, src/main/daemon/protocol.ts,
    src/preload/index.ts,
    src/renderer/index.html, src/renderer/main.tsx, src/renderer/App.tsx,
    src/renderer/components/Chat.tsx, src/renderer/components/Composer.tsx,
    src/renderer/components/MessageBubble.tsx, src/renderer/components/KeyModal.tsx,
    src/renderer/components/ErrorBanner.tsx,
    src/renderer/state/messages.ts, src/renderer/styles/app.css,
    daemon/main.cjs, daemon/protocol.cjs, daemon/tools/registry.cjs, daemon/audit.cjs,
    resources/icon.svg
  </files>
  <read_first>
    - .planning/phases/01-skeleton-streaming-chat/01-CONTEXT.md (locked decisions)
    - .planning/phases/01-skeleton-streaming-chat/SKELETON.md (this plan's architectural table)
    - .claude/CLAUDE.md (constraints: no node-gyp, M3 only, safeStorage mandate)

  <action>
    Establish a minimal but runnable Localbot: project scaffold, IPC bus, safeStorage key persistence, first-launch blocking modal with Test-connection probe, stdio JSON-RPC daemon with NDJSON framing, chat-bubble UI, streaming sendMessage path, single global session JSONL, and audit JSONL writer. Land the locked one-way decisions first: safeStorage (D-05), streaming IPC contract (D-07), daemon spawn + handshake (D-09), session schema (D-13). Every file is new — no existing source.

    **1. Scaffold (package.json + tooling).** Create `package.json` with `"name": "localbot"`, `"version": "0.1.0"`, `"private": true`, `"main": "dist/main/index.js"`, scripts: `"dev": "concurrently -k -n vite,electron -c blue,magenta \"vite\" \"npm:dev:electron\""`, `"dev:electron": "wait-on tcp:5173 && tsc -p tsconfig.main.json && electron .\""`, `"build": "tsc -p tsconfig.main.json && vite build"`, `"build:renderer": "vite build"`, `"start": "electron dist/main/index.js"`. Add `dependencies`: `@anthropic-ai/sdk` (any 0.x latest). Add `devDependencies`: `electron` (latest stable prebuilt), `electron-builder` (latest), `react@19`, `react-dom@19`, `@types/react@19`, `@types/react-dom@19`, `typescript` (5.x), `vite`, `@vitejs/plugin-react`, `concurrently`, `wait-on`. NO native modules. Do not add `electron-rebuild`, `node-gyp`, `keytar`, `node-keytar`, or anything that compiles. Add `.nvmrc` pinning `20`. Add `.gitignore` covering `node_modules`, `dist`, `out`, `release`, `*.log`, `.vite/`. `tsconfig.json` is the root with `"references": [{"path": "./tsconfig.main.json"}, {"path": "./tsconfig.renderer.json"}]`. `tsconfig.main.json` targets ES2022, `module: CommonJS`, `outDir: dist/main`, `rootDir: src`, includes `src/main/**/*`, `src/preload/**/*`, `src/shared/**/*`. `tsconfig.renderer.json` targets ES2022, `module: ESNext`, `moduleResolution: bundler`, `jsx: react-jsx`, `lib: ["ES2022","DOM","DOM.Iterable"]`, `types: ["vite/client"]`, includes `src/renderer/**/*`, `src/shared/**/*`. `vite.config.ts` sets `root: 'src/renderer'`, `base: './'`, plugins `[react()]`, `build.outDir: '../../dist/renderer'`, `server: { port: 5173, strictPort: true }`. Pick `react-syntax-highlighter` (not `shiki`) for D-02 — pure-JS, no native bindings; configure it for `vscodeLight` style with `customStyle={{ background: '#f6f8fa', padding: '12px', borderRadius: '6px' }}`. Do NOT use `shiki` — it pulls native bindings in some configurations.

    **2. Shared layer.** `src/shared/ipc-channels.ts` exports `CHANNELS = { SEND_MESSAGE: 'sendMessage', CANCEL: 'cancel', KEY_GET: 'key:get', KEY_SET: 'key:set', KEY_PROBE: 'key:probe', EVENT_MESSAGE_TOKEN: 'message:token', EVENT_MESSAGE_DONE: 'message:done', EVENT_MESSAGE_ERROR: 'message:error', EVENT_DAEMON_STATUS: 'daemon:status' } as const`. `src/shared/types.ts` exports `ChatMessage { ts:number; role:'user'|'assistant'; content:string; stopped?:true; interrupted?:true; msgId?:string }`, `SendMessageRequest { content:string; msgId:string }`, `TokenEvent { msgId:string; delta:string }`, `DoneEvent { msgId:string }`, `ErrorEvent { msgId:string; error:string; retryable:boolean }`, `KeyProbeResult { ok:boolean; error?:string }`, `DaemonStatus { state:'connecting'|'ready'|'down'; message?:string }`.

    **3. Electron main entry (`src/main/index.ts`).** On `app.whenReady()`: call `ensureUserDataDirs()` then `createMainWindow()` (from `src/main/window.ts`) loading `http://localhost:5173` in dev or `file://${path.join(__dirname,'../../renderer/index.html')}` in production. Register handlers in this order: `registerKeyHandlers()` (D-05), `registerChatHandlers()` (D-07), then `spawnDaemon()` (D-09). Forward `daemon:status` events from main → all renderers via `webContents.getAllWebContents().forEach(wc => wc.send(CHANNELS.EVENT_DAEMON_STATUS, status))`. On `window-all-closed` (except macOS), call `stopDaemon()` then `app.quit()`. Log every lifecycle event via `appendAuditLine({ bot:'__system__', tool:'lifecycle', params:{event:'start'}, outcome:'ok', durationMs:0 })`.

    **4. Window chrome (`src/main/window.ts`).** Export `createMainWindow(): BrowserWindow`. Single window per D-25. Constructor options: `width: 1100, height: 760, minWidth: 720, minHeight: 480, title: 'Localbot', titleBarStyle: 'hidden', titleBarOverlay: { color: '#ffffff', symbolColor: '#222', height: 32 }, backgroundColor: '#ffffff', webPreferences: { preload: path.join(__dirname,'../preload/index.js'), contextIsolation: true, nodeIntegration: false, sandbox: false }`. Load the renderer URL. On `did-finish-load`, send the renderer's initial state (`{ hasKey: await hasStoredKey() }`) via `webContents.send('app:init', ...)` (add `EVENT_APP_INIT` to CHANNELS). `did-fail-load` is logged via audit.

    **5. safeStorage layer (`src/main/ipc/key.ts` + `src/main/paths.ts`).** `src/main/paths.ts` exports `userDataDir()` (returns `app.getPath('userData')`), `auditDir()` (`${userDataDir()}/audit`), `sessionsDir()` (`${userDataDir()}/sessions`), `keyFilePath()` (`${userDataDir()}/api-key.bin`), `sessionFilePath()` (`${sessionsDir()}/global.jsonl`). `ensureUserDataDirs()` creates each via `fs.mkdir(recursive:true)`. `src/main/ipc/key.ts` exports `hasStoredKey(): Promise<boolean>` (returns `fs.existsSync(keyFilePath())`), `registerKeyHandlers()` registers three IPC handlers: `key:get` returns `{ hasKey: await hasStoredKey() }` (NEVER returns the decrypted key — D-05), `key:set` accepts `{ key: string }` calls `safeStorage.encryptString(key)` writes base64 ciphertext to `keyFilePath()`, returns `{ ok: true }`. `key:probe` accepts `{ key: string }` constructs an `anthropic` SDK client with `apiKey: key, baseURL: process.env.M3_API_BASE || 'https://api.MiniMax.io/v1'`, calls `client.messages.create({ model: process.env.M3_MODEL || 'MiniMax/M3', max_tokens: 1, messages: [{role:'user', content:'ping'}] })`. Returns `{ ok: true }` on success or `{ ok: false, error: classifyError(err) }` (D-08). `classifyError` (in `src/main/errors.ts`) maps Anthropic SDK errors to `{ category:'auth'|'network'|'transient'|'fatal', message: string }`. On startup, if `safeStorage.isEncryptionAvailable()` returns `false`, throw a hard error — fail-fast (no plaintext key fallback, per D-05).

    **6. Audit log writer (`src/main/audit/logger.ts`).** Export `auditDir(): string` and `appendAuditLine(line: { bot:string; tool:string; params:object; outcome:'ok'|'error'; durationMs:number; error?:{ code:string; message:string } }): Promise<void>`. Computes `YYYY-MM-DD` (UTC) for filename. `fs.appendFile(path.join(auditDir(),\`${date}.jsonl\`), JSON.stringify({ ts: new Date().toISOString(), ...line })+'\n', { encoding: 'utf8' })`. Uses `fs.promises.writeFile` with the `'wx'` flag once at process start? No — use `fs.promises.open`+`appendFile` for atomic append per line. Add a single-line buffered tail (`fs.createWriteStream(..., {flags:'a'})` cached per day) for hot paths; flush on `process.on('exit')`.

    **7. Session JSONL (`src/main/sessions/jsonl.ts`).** Export `appendMessage(msg: { role:'user'|'assistant'; content:string; stopped?:boolean; interrupted?:boolean }): Promise<void>` writes one NDJSON line to `sessionFilePath()` with `ts: new Date().toISOString()`. Export `loadSession(): Promise<ChatMessage[]>` reads the file, splits on `\n`, parses each non-empty line into `ChatMessage`. Returns `[]` if file missing. Used on app launch to populate the chat — fulfills D-15.

    **8. LLM client (`src/main/llm/client.ts` + `src/main/llm/prompts.ts`).** `src/main/llm/prompts.ts` exports `DEFAULT_SYSTEM_PROMPT` = "You are Localbot, a local-first coding/dev assistant running on the user's PC. Be concise, use tools when useful, and don't pretend to know things you haven't verified." (verbatim from D-16). `streamChat` accepts `{ messages: ChatMessage[], system?: string, signal: AbortSignal, onToken: (delta:string)=>void, onError: (e:Error)=>void, onDone: ()=>void }`. Uses `new Anthropic({ apiKey: await readKey(), baseURL: process.env.M3_API_BASE || 'https://api.MiniMax.io/v1' })`. Calls `client.messages.stream({ model, system, messages: messages.map(toAnthropic), max_tokens: 4096 }, { signal })`. `toAnthropic` maps `ChatMessage → { role, content }`. Iterates `for await (const event of stream)`; on `event.type === 'content_block_delta' && event.delta.type === 'text_delta'` calls `onToken(event.delta.text)`. `onDone` on stream end. `onError` from outer try/catch wraps `AbortError` as `cancelled`. Wrap streaming in `runWithRetry(fn, { attempts: 3, baseDelayMs: 250 })` from `errors.ts`; only transient (`network` and 5xx `transient` categories) get retried; `auth` and `fatal` surface immediately (D-22). `readKey()` reads `keyFilePath()`, base64-decodes, calls `safeStorage.decryptString`. Key is fetched only inside the request handler — never stored in a module-level variable (D-05).

    **9. Chat IPC (`src/main/ipc/chat.ts`).** Export `registerChatHandlers()`. Maintains `const activeStreams = new Map<msgId, AbortController>()`. Registers `ipcMain.handle(CHANNELS.SEND_MESSAGE, async (_evt, { content, msgId }) => { const ac = new AbortController(); activeStreams.set(msgId, ac); try { appendMessage({role:'user', content}); await streamChat({ messages: [{role:'user', content}], signal: ac.signal, onToken: delta => webContents.send(CHANNELS.EVENT_MESSAGE_TOKEN, { msgId, delta }), onDone: () => webContents.send(CHANNELS.EVENT_MESSAGE_DONE, { msgId }), onError: err => webContents.send(CHANNELS.EVENT_MESSAGE_ERROR, { msgId, error: err.message, retryable: err.retryable !== false }) }); appendMessage({role:'assistant', content: aggregated}); } catch(err){...} finally { activeStreams.delete(msgId); return { ok: true }; }})`. Implements a module-level `let aggregated = ''` per-call (closes over the per-`ipcMain.handle` invocation via closure) to capture the full streamed text so we can persist once at `onDone`. Registers `ipcMain.handle(CHANNELS.CANCEL, async (_evt, msgId) => { const ac = activeStreams.get(msgId); if (ac) ac.abort(); return { ok: true }; })` (D-18). Handles `AbortError` from SDK by sending `EVENT_MESSAGE_ERROR` with `error: 'cancelled', retryable: false` then writing the assistant line with `stopped: true` (D-19).

    **10. Daemon spawn (`src/main/daemon/spawn.ts` + `src/main/daemon/protocol.ts`).** `spawnDaemon()` uses `child_process.spawn(process.execPath, [path.join(app.getAppPath(),'daemon/main.cjs')], { stdio: ['pipe','pipe','inherit'] })`. Watches stdout line-by-line via `readline.createInterface({input: child.stdout})`. On first line `JSON.parse(line).kind === 'ready'` (D-09), sends `{jsonrpc:'2.0', id:1, method:'initialize', params:{client:'localbot-main', version:'0.1.0'}}` via `writeMessage(child.stdin, ...)`. Awaits response with `id:1`, sets internal state `ready`. From then on, NDJSON framed per `protocol.ts`. Export `callTool(name, params, opts): Promise<JsonRpcResult>` increments id, writes `{jsonrpc:'2.0', id, method:'tools/call', params:{name, arguments: params}}`, awaits response with matching id. Includes 60s timeout. Implements `requestReconnect()` on `exit` event of child → re-spawns with 1s backoff, broadcasts `EVENT_DAEMON_STATUS { state: 'connecting', message: 'Tool daemon reconnecting…' }` until next `ready` (D-24). Stops clean on `stopDaemon()`. `daemon/protocol.ts` exports `writeMessage(stream, obj): void` writes `JSON.stringify(obj)+'\n'` and `readMessage(line: string): object|null` returns `JSON.parse(line)` catching parse errors.

    **11. Daemon entrypoint (`daemon/main.cjs` + `daemon/protocol.cjs` + `daemon/tools/registry.cjs` + `daemon/audit.cjs`).** Pure CommonJS so it runs as `process.execPath daemon/main.cjs`. `main.cjs`: writes `{kind:'ready', version:'0.1.0'}\n` to stdout. Reads incoming NDJSON via `readline.createInterface({input: process.stdin})`. Routes per `method`: `initialize` → reply `{result:{server:'localbot-daemon', version:'0.1.0', tools:[]}}`. `tools/list` → reply `tools:registry.listTools()` (returns `[]` until Phase 2). `tools/call` → wraps `tools/registry.callTool(name, args)` in a `try/catch`, on rejection returns `{error:{code: err.code||'unknown_tool', message: err.message}}`. Every `tools/call` invocation appends one audit line via `audit.appendAudit({...})` BEFORE returning so outcome is recorded whether success or failure (SEC-04). `tools/cancel` is accepted and acknowledged — Phase 2 will run real cancels. Daemon exits with `process.exit(0)` when stdin closes. `audit.cjs` writes NDJSON to the same `<userData>/audit/YYYY-MM-DD.jsonl` file — it receives the userData directory path via the `initialize` request `params.userDataDir` (provided by main as `app.getPath('userData')`).

    **12. Tool registry (`daemon/tools/registry.cjs`).** Export `listTools()` returns `[]`. Export `callTool(name, _args)` throws `{ code: 'unknown_tool', message: 'no tools registered (Phase 1 stub)' }` (D-11) regardless of `name`. This single stub is the contract Phase 2 plugs real tools into — both `tools/call` and audit are exercised end-to-end before Phase 2 starts.

    **13. Preload (`src/preload/index.ts`).** Uses `contextBridge.exposeInMainWorld('localbot', { sendMessage: (content, msgId) => ipcRenderer.invoke(CHANNELS.SEND_MESSAGE, {content, msgId}), cancel: (msgId) => ipcRenderer.invoke(CHANNELS.CANCEL, msgId), key: { get: () => ipcRenderer.invoke(CHANNELS.KEY_GET), set: (key) => ipcRenderer.invoke(CHANNELS.KEY_SET, {key}), probe: (key) => ipcRenderer.invoke(CHANNELS.KEY_PROBE, {key}) }, on: (channel, handler) => { const valid = [CHANNELS.EVENT_MESSAGE_TOKEN, CHANNELS.EVENT_MESSAGE_DONE, CHANNELS.EVENT_MESSAGE_ERROR, CHANNELS.EVENT_DAEMON_STATUS, 'app:init']; if (!valid.includes(channel)) throw new Error('bad channel'); const wrapped = (_e, payload) => handler(payload); ipcRenderer.on(channel, wrapped); return () => ipcRenderer.removeListener(channel, wrapped); } })`. Renderer NEVER sees the decrypted key — only calls `localbot.key.set()` and `localbot.key.probe()`.

    **14. Renderer entry (`src/renderer/index.html` + `src/renderer/main.tsx`).** `index.html` includes `<meta charset="utf-8"><title>Localbot</title><link rel="stylesheet" href="./styles/app.css">`. `main.tsx` does `createRoot(document.getElementById('root')!).render(<App/>)`. Sets `lang="en"`. `App.tsx` reads `app:init` payload via `useEffect(() => { const off = window.localbot.on('app:init', p => setState(s => ({...s, hasKey: p.hasKey}))); return off; }, [])`. If `hasKey === false`, renders `<KeyModal/>` (D-06). Once `hasKey === true`, renders `<Chat/>`.

    **15. Key modal (`src/renderer/components/KeyModal.tsx`).** Blocking `position: fixed; inset: 0` overlay; `background: rgba(0,0,0,0.4)`. Card (`max-width: 480px; margin: 10vh auto; background: white; border-radius: 12px; padding: 24px`). Title "Welcome to Localbot". Body "Enter your MiniMax API key to start. Your key is encrypted and stored via your operating system's secure storage — it never leaves this machine." Input `<input type="password">` bound to `key` state. Buttons: "Get API key" (anchor → `https://MiniMax.io/m3/apikeys`), "Test connection", "Save". On Test connection: `await localbot.key.probe(key)`, show inline `result.error` (red) or `OK` (green). Save disabled until `probe.ok === true`. On Save: `await localbot.key.set(key)`, then `setHasKey(true)` which unmounts the modal. Implements D-06/D-08 verbatim.

    **16. Chat pane (`src/renderer/components/Chat.tsx`).** Light chat-bubble style per D-01/D-03. Flex column with header (height 32px to match `titleBarOverlay`), scrollable message list (`flex: 1; overflow-y: auto; padding: 16px 32px;`), and fixed composer at bottom. Auto-scrolls to bottom on new message via `useEffect` tracking `messages.length`. Renders `<MessageBubble>` per message. Distinguishes user/assistant by background tint: user `#e6f0ff`, assistant `#ffffff` with `border: 1px solid #e5e7eb`. Shows `<ErrorBanner>` on `daemon:status{state:'connecting'|'down'}` at the top of the chat list.

    **17. Message bubble (`src/renderer/components/MessageBubble.tsx`).** Renders markdown via `react-markdown` + `remark-gfm`. Code blocks use `react-syntax-highlighter` with `vscodeLight` theme and inline `code` gets `style={{background:'#f6f8fa', padding:'1px 4px', borderRadius:'4px'}}` (D-02). For assistant messages still streaming: live token append (controlled by parent via `delta`). For assistant messages with `stopped === true`: appends ` <em>(stopped)</em>` visibly distinct from any inline code by using a literal text node after the markdown render (D-19). Empty content is allowed (matches loading state).

    **18. Composer (`src/renderer/components/Composer.tsx`).** `<textarea>` auto-grow via `useEffect` setting `style.height = 'auto'; style.height = scrollHeight + 'px'` on each change. Enter sends; Shift+Enter inserts newline (D-04). Sticky to bottom: container has `padding: 12px 32px; border-top: 1px solid #e5e7eb; background: #ffffff`. While `streaming === true` the Send button morphs into "Stop" (`type="button"`, `onClick={() => localbot.cancel(activeMsgIdRef.current)}`, `data-testid="stop-button"`); labels: `"Send"` ↔ `"Stop"` with red background `Stop` (`#dc2626`) (D-17).

    **19. State (`src/renderer/state/messages.ts`).** Exports `useMessages()` returning `{messages: ChatMessage[], appendDelta(msgId, delta), finalizeMsg(msgId, fullText, flags?), appendUserMsg(content), clear(), streaming: boolean, setStreaming(b), error: ErrorEvent|null, setError(e), activeMsgId: string|null, setActiveMsgId(id), daemonStatus: DaemonStatus, setDaemonStatus(s)}`. Subscribes to `message:token|done|error` and `daemon:status` events. On `message:token` → `appendDelta(msgId, delta)`. On `message:done` → `finalizeMsg(msgId, aggregated, {})`. On `message:error` → `setError(payload)` and finalize with `stopped: true` if `retryable === false`.

    **20. Error banner (`src/renderer/components/ErrorBanner.tsx`).** Fixed/sticky banner at top (`background: #fef2f2; color: #991b1b; padding: 8px 16px`). Renders the error message; if `retryable`, shows a "Retry" button that re-sends the last user message; if `category === 'auth'`, shows "Update key" that re-opens the modal. Dismissable via `×` button. Daemon-status banner: blue-tinted when `state === 'connecting'` with text "Tool daemon reconnecting…".

    **21. CSS (`src/renderer/styles/app.css`).** Defines root tokens, bubble backgrounds, the mono font stack (`Inter, system-ui, ...`), the `<code>` inline style, the `<pre>` padding, and the chat-shell layout. Light-theme only for Phase 1.

    **22. App icon (`resources/icon.svg`).** 256×256 monochrome "L" inside a colored rounded square (per D-28). Loaded by BrowserWindow's `icon` option in dev — only used in production builds (Phase 9 wires real packaging).

    **23. Run scripts.** `"dev": "concurrently -k -n vite,electron -c blue,magenta \"vite\" \"npm:dev:electron\""`. `npm run dev` brings up the renderer on `http://localhost:5173` and Electron main (which `wait-on tcp:5173`'s then `tsc -p tsconfig.main.json && electron .`). This is the documented local-full-stack run command per Walking Skeleton requirements.

    **24. Constraints honored.** No `node-gyp`, no native bindings. All deps are pure-JS or prebuilt. Windows-first paths use `path.join` everywhere. No `npm install --build-from-source`. No code references to `cursor`, `grok`, or `SpaceXAI` (Localbot is its own product).
  </action>
  <behavior>
    - First-launch modal blocks the chat until a valid M3 key is saved (D-06).
    - Saved key survives a relaunch via `safeStorage`; `key:get` returns `{hasKey:true}` on subsequent launches.
    - With a valid key, typing "hello" and clicking Send yields a streamed assistant response visible token-by-token in the chat (LLM-02).
    - `M3_API_BASE` env override routes the SDK to the test fake for the Playwright smoke (D-31).
    - When the daemon's `tools/call` is invoked with any name, it returns `{error:{code:'unknown_tool'}}` and writes one audit line to the current UTC-day JSONL.
    - `app:init` payload tells the renderer whether to show the modal.
  </behavior>
  <verify>
    <automated>npm --prefix D:/claude/Grokbot install --no-audit --no-fund --prefer-offline 2>&amp;1 | tail -5</automated>
    <fails_when>command exits non-zero, or output contains "ERR!", or "added 0 packages"</fails_when>
  </verify>
  <verify>
    <automated>npm --prefix D:/claude/Grokbot run build 2>&amp;1 | tail -20</automated>
    <fails_when>command exits non-zero, or "error TS" appears in output, or "Found 0 errors" is not in the output</fails_when>
  </verify>
  <verify>
    <automated>test -f D:/claude/Grokbot/dist/main/index.js &amp;&amp; test -f D:/claude/Grokbot/dist/main/preload/index.js &amp;&amp; test -f D:/claude/Grokbot/dist/renderer/index.html</automated>
    <fails_when>any of the three test -f checks fails, exit code != 0</fails_when>
  </verify>
  <verify>
    <automated>test -f D:/claude/Grokbot/daemon/main.cjs &amp;&amp; head -n 5 D:/claude/Grokbot/daemon/main.cjs | grep -q "kind.*ready"</automated>
    <fails_when>daemon/main.cjs missing or first 5 lines do not include the ready handshake marker</fails_when>
  </verify>
  <verify>
    <automated>grep -RE "ipcRenderer\\.(invoke|on)\\(|contextBridge\\.exposeInMainWorld" D:/claude/Grokbot/src/preload/index.ts | grep -v '^\\s*//' | wc -l | awk '{ if ($1 < 3) exit 1 }'</automated>
    <fails_when>fewer than three non-comment contextual bridge / ipcRenderer call sites in preload (proves IPC is wired)</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm install` succeeds with no peer-dep errors and no native-module warnings.
    - `npm run build` produces `dist/main/index.js`, `dist/main/preload/index.js`, and `dist/renderer/index.html` without TypeScript errors.
    - `daemon/main.cjs` writes `{"kind":"ready"}` on stdout as its first line of output.
    - `src/preload/index.ts` calls `contextBridge.exposeInMainWorld` exactly once with name `localbot`, and references at least three IPC channels (sendMessage, cancel, key:get) in its body (after stripping comment lines).
    - `src/main/ipc/key.ts` defines handler for `key:set` that wraps `safeStorage.encryptString` and writes ciphertext to a file under `app.getPath('userData')` — `grep -c safeStorage.encryptString src/main/ipc/key.ts` returns at least 1.
    - `src/main/daemon/spawn.ts` calls `child_process.spawn(process.execPath, [` and includes the daemon entry path string — `grep -c "spawn(process.execPath" src/main/daemon/spawn.ts` returns at least 1.
    - `daemon/tools/registry.cjs` exports `callTool` that throws `{ code: 'unknown_tool' }` for any name — `grep -c "unknown_tool" daemon/tools/registry.cjs` returns at least 1.
    - `src/main/sessions/jsonl.ts` writes to `global.jsonl` under a `<userData>/sessions/` directory — `grep -c "global.jsonl" src/main/sessions/jsonl.ts` returns at least 1.
    - `src/renderer/components/KeyModal.tsx` blocks the entire viewport (`position: fixed; inset: 0`) and calls `localbot.key.probe` before allowing `localbot.key.set` — `grep -c "localbot.key.probe" src/renderer/components/KeyModal.tsx` returns at least 1.
  </acceptance_criteria>
  <done>
    All scaffolding files exist, `npm install` and `npm run build` both succeed, the preload bridge references the expected channel constants, the daemon handshake marker is present in `daemon/main.cjs`, and the audit-log writer under `src/main/audit/logger.ts` writes to a path that resolves under `app.getPath('userData')`.
  </done>
</task>

<task type="auto">
  <name>In-flight cancel transport + retry + daemon auto-respawn + error banners</name>
  <files>
    src/main/ipc/chat.ts, src/main/llm/client.ts, src/main/errors.ts,
    src/main/daemon/spawn.ts,
    src/renderer/components/Chat.tsx, src/renderer/components/Composer.tsx,
    src/renderer/components/ErrorBanner.tsx, src/renderer/components/KeyModal.tsx,
    src/renderer/state/messages.ts
  </files>
  <read_first>
    - .planning/phases/01-skeleton-streaming-chat/01-CONTEXT.md (D-17..D-24)
    - src/main/ipc/chat.ts (existing in task 1; continue pattern)
    - src/renderer/state/messages.ts (existing; extend)


  <action>
    Layer the locked error-cancel-recovery behaviors on top of the walking skeleton: Send→Stop morph during streams + Esc keyboard shortcut (D-17), `ipcRenderer.invoke('cancel', msgId)` calling `AbortController.abort()` per D-18, partial text preserved with a literal `(stopped)` suffix (D-19), `tools/cancel` JSON-RPC dispatch to the daemon when a tool call is in flight (D-20), dismissable top-of-chat error banner (D-21), network/5xx auto-retry with exponential backoff capped at 3 attempts (D-22), "Stream interrupted — Retry" inline footer (D-23), and daemon auto-respawn with a top-of-chat "Tool daemon reconnecting…" banner (D-24).

    **1. Cancel during LLM stream (`src/main/ipc/chat.ts`).** Update the `SEND_MESSAGE` handler so the `AbortController` is also stored on `window.localbot` via the renderer hook (already exists). When `CANCEL(msgId)` is invoked, look up the controller in `activeStreams`, call `.abort()`. The SDK's `messages.stream(..., { signal })` will throw `AbortError` (per @anthropic-ai/sdk semantics) — wrap that in the error handler: if `err.name === 'AbortError'`, write the assistant line to JSONL with `stopped: true` and send `EVENT_MESSAGE_ERROR { msgId, error: 'cancelled', retryable: false }`. The renderer appends the literal text ` (stopped)` to the assistant bubble per D-19 — implementation: `finalizeMsg(msgId, aggregated, { stopped: true })` and the renderer formats it as `aggregated + ' (stopped)'`.

    **2. Cancel during tool call in flight (D-20 stub for Phase 2).** Add an `activeToolCall` ref that the next phase's tool loop will populate. For Phase 1, expose `cancelToolCall(toolCallId)` in `src/main/daemon/spawn.ts` which writes `{jsonrpc:'2.0', id:N, method:'tools/cancel', params:{toolCallId}}` and records the in-flight toolCallId for the response handler. Daemon's `tools/cancel` handler in `daemon/main.cjs` is acknowledged `{result:{cancelled:true}}` so the wire is exercised end-to-end even though no real tools cancel anything yet.

    **3. Send→Stop morph + Esc (`src/renderer/components/Composer.tsx`).** Hold a `streaming: boolean` from state. Render `<button data-testid={streaming ? 'stop-button' : 'send-button'} onClick={streaming ? onStop : onSend}>{streaming ? 'Stop' : 'Send'}</button>`. `onStop` calls `localbot.cancel(activeMsgId)`. Add a `useEffect` registering a `keydown` listener on `document` for `Escape` key while `streaming === true`, calling `onStop`. Cleanup on unmount. Use a stable `activeMsgId` ref passed from state so the cancel finds the right controller.

    **4. Inline Retry footer (D-23).** When the renderer sees `EVENT_MESSAGE_ERROR` with `retryable === true` AND the message was mid-stream (not just initial send failure), append a one-line footer `"Stream interrupted — Retry"` as a clickable button below the assistant bubble. Click re-invokes `localbot.sendMessage(originalContent, newMsgId)` with the prior user content. Display literal text `Stream interrupted — Retry` without markdown — use `<span>` not `<ReactMarkdown>` for this control.

    **5. Top-of-chat error banner (D-21).** `<ErrorBanner>` is always rendered above the message list. Subscribes to the renderer's error state. Props: `{ error: ErrorEvent | null, onRetry: () => void, onUpdateKey: () => void, onDismiss: () => void }`. Map `error.category` (auth → Update key button; transient → Retry button; fatal → Dismiss only) — `category` is appended to the ErrorEvent at the IPC boundary by inspecting the SDK error in `classifyError`. For "Update key", click clears the stored key via a new IPC handler `key:clear` (calls `fs.rm(keyFilePath())` if present) AND shows the modal again. Style: red banner, dismissable.

    **6. Retry policy (`src/main/llm/client.ts` + `src/main/errors.ts`).** Implement `runWithRetry(fn, { attempts: 3, baseDelayMs: 250, isRetryable: (err) => err.category === 'transient' || err.category === 'network' })`. Uses exponential backoff (250ms → 500ms → 1000ms). `classifyError` returns `{ category: 'auth' | 'network' | 'transient' | 'fatal', message }`. Mapping: 401 / 403 → `auth`; AnthropicSDKError status 408/429/500/502/503/504 → `transient`; `ENOTFOUND`, `ECONNRESET`, `ETIMEDOUT`, `AbortError` → `network` (AbortError is NOT retried — caller decides to handle); anything else → `fatal`. On `auth` failure, the wrapper never retries and the renderer shows the auth banner with an Update-key CTA (D-22).

    **7. Daemon auto-respawn (`src/main/daemon/spawn.ts`).** Wrap the spawn loop in a `tryRespawn()` function. On child `exit` event with code !== 0 OR stderr-on-null: emit `EVENT_DAEMON_STATUS { state: 'down', message: 'Tool daemon crashed' }` then `setTimeout(tryRespawn, 1000)` (D-24's banner text is `Tool daemon reconnecting…`). On stdin `close` from parent (called via `stopDaemon()`), do NOT respawn — only respawn on unexpected exits. Add a max-attempts cap of 10 within a 60s window to avoid runaway respawn on bugs; reset the counter on each successful `ready` handshake. Banner clears via `EVENT_DAEMON_STATUS { state: 'ready' }` once `initialize` returns ok.

    **8. Renderer wiring (`src/renderer/state/messages.ts`).** Subscribe to `daemon:status` via `localbot.on('daemon:status', payload => setDaemonStatus(payload))`. Display `daemonStatus.message` in the banner slot positioned above the first bubble. When user clicks the Auth "Update key" CTA: `await localbot.key.clear()` then unmount the modal gate.

    **9. Verify visible affordances in the DOM.** Composer renders exactly one button whose text is either `Send` or `Stop`; switching state changes the text. Esc key registered as a single handler on the document while streaming.
  </action>
  <verify>
    <automated>npm --prefix D:/claude/Grokbot run build 2>&amp;1 | tail -20</automated>
    <fails_when>command exits non-zero, or output contains "error TS", or no "Found 0 errors" / equivalent pass line</fails_when>
  </verify>
  <verify>
    <automated>grep -c "AbortController" D:/claude/Grokbot/src/main/ipc/chat.ts | awk '{ if ($1 < 2) exit 1 }'</automated>
    <fails_when>fewer than 2 occurrences of AbortController in src/main/ipc/chat.ts (one for declaration, one for abort() call)</fails_when>
  </verify>
  <verify>
    <automated>grep -c "data-testid=\"stop-button\"" D:/claude/Grokbot/src/renderer/components/Composer.tsx | awk '{ if ($1 < 1) exit 1 }' &amp;&amp; grep -c "data-testid=\"send-button\"" D:/claude/Grokbot/src/renderer/components/Composer.tsx | awk '{ if ($1 < 1) exit 1 }'</automated>
    <fails_when>either test-id missing (Stop and Send buttons both required for Playwright to find them)</fails_when>
  </verify>
  <verify>
    <automated>grep -c "Escape" D:/claude/Grokbot/src/renderer/components/Composer.tsx | awk '{ if ($1 < 1) exit 1 }'</automated>
    <fails_when>no Escape key handler registered in Composer.tsx</fails_when>
  </verify>
  <verify>
    <automated>grep -c "tools/cancel" D:/claude/Grokbot/daemon/main.cjs | awk '{ if ($1 < 1) exit 1 }' &amp;&amp; grep -c "tools/cancel" D:/claude/Grokbot/src/main/daemon/spawn.ts | awk '{ if ($1 < 1) exit 1 }'</automated>
    <fails_when>tools/cancel JSON-RPC method not present in BOTH daemon main.cjs and main spawn.ts</fails_when>
  </verify>
  <verify>
    <automated>grep -c "runWithRetry\\|isRetryable\\|attempts.*3" D:/claude/Grokbot/src/main/llm/client.ts D:/claude/Grokbot/src/main/errors.ts | awk -F: '{ s += $2 } END { if (s < 1) exit 1 }'</automated>
    <fails_when>no retry-cap of 3 attempts or isRetryable classifier found in either file</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/main/ipc/chat.ts` contains at least two occurrences of `AbortController` (one for `new AbortController()` per message, one for `.abort()` call).
    - `src/renderer/components/Composer.tsx` renders a button with `data-testid="send-button"` AND a button with `data-testid="stop-button"`.
    - `src/renderer/components/Composer.tsx` registers a `keydown` handler that checks `Escape`.
    - `daemon/main.cjs` handles the `tools/cancel` JSON-RPC method and `src/main/daemon/spawn.ts` writes a `tools/cancel` request to the daemon.
    - `src/main/llm/client.ts` and `src/main/errors.ts` together include an `isRetryable` predicate and an attempts cap of 3 (the literal `3` or `attempts: 3` pattern appears).
    - `src/main/daemon/spawn.ts` respawns the child on exit (`'exit'` event listener present) and broadcasts a `daemon:status` event of `connecting` after a crash.
    - `npm run build` succeeds with zero TypeScript errors.
  </acceptance_criteria>
  <done>
    Cancel works end-to-end via both the Stop button and Esc (verified by source inspection: both code paths call `localbot.cancel(activeMsgId)` which routes to `ipcMain.handle('cancel', ...)`); the LLM client retries transient failures up to 3 times and surfaces auth errors immediately; the daemon respawn loop runs on child exit; and the renderer's error banner reflects `daemon:status` updates. `npm run build` is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main | Untrusted UI input crosses here; all crossings go through `contextBridge.exposeInMainWorld('localbot', ...)` only |
| Main → Daemon | The spawn-and-stdio boundary; main's `toolCallId` provenance is trusted by main but daemon enforces its own denylist (Phase 2) |
| Main → M3 | TLS to `M3_API_BASE`; auth via `safeStorage`-decrypted bearer token |
| Daemon → Disk | Daemon process writes audit lines to `<userData>/audit/YYYY-MM-DD.jsonl` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | API key in plaintext | critical | mitigate | safeStorage encryption + key never crosses contextBridge; `key:get` returns `{hasKey}` only (D-05) |
| T-01-02 | Spoofing | Forged renderer message claiming `retryable: false` | medium | mitigate | `retryable` flag is computed in main's `classifyError` and never read from renderer input |
| T-01-03 | Denial of Service | Malformed NDJSON to daemon | medium | mitigate | `readMessage` catches JSON parse errors and replies `{-32700 Parse error}`; line length cap 1 MiB |
| T-01-04 | Elevation of Privilege | Renderer accessing `child_process` via preload | high | mitigate | `contextIsolation: true` + `nodeIntegration: false`; preload exposes only the `localbot` allowlist |
| T-01-05 | Repudiation | Missing audit line on crash | medium | mitigate | Audit write uses buffered `createWriteStream` flushed on `process.on('exit')` and `'uncaughtException'`; line is written BEFORE the JSON-RPC reply returns (SEC-04) |
| T-01-06 | Tampering | Audit file replaced with attacker-controlled JSONL | low | accept | Local-only deployment; out of Phase 1 scope to sign the file |
| T-01-07 | Information Disclosure | Prompt content leaking via session JSONL | low | accept | Session file lives under userData only; included in next-phase redaction policy. Accepted for v1. |
| T-01-08 | Denial of Service | Daemon respawn loop runaway | medium | mitigate | Max 10 respawns per 60s window; counter resets on each successful `ready` handshake |
| T-01-SC | Tampering | npm install | high | mitigate | Lockfile committed; `npm ci` recommended in `dev` script; no native builds; no suspicious-deps audit step (out of Phase 1 — accepted risk with all deps as prebuilt binaries only) |
</threat_model>

<verification>
1. `npm install` succeeds (no node-gyp / native-module warnings).
2. `npm run build` produces `dist/main/index.js`, `dist/main/preload/index.js`, `dist/renderer/index.html`.
3. Playwright smoke (Plan 01-02) launches the Electron app, completes the key modal against the fake M3, sends a message, asserts a streamed token appears, and asserts a `tools/call` writes one audit JSONL line.
4. Vitest unit tests (Plan 01-02) pass for NDJSON framing, safeStorage round-trip, and session JSONL serialization.
</verification>

<success_criteria>
1. A real user launches the Electron app and sees a chat pane (UI-02).
2. Real user types a message and sees the assistant response streamed token-by-token (LLM-02).
3. API key is stored in the OS keychain via `safeStorage`; never in plaintext on disk; never exposed to the renderer (LLM-05, SEC-05).
4. Tool daemon runs as a separate child process from Electron main; main never spawns user shell commands directly (SEC-01).
5. Every tool invocation writes one NDJSON audit line with `{ts,bot,tool,params,outcome,durationMs,error?}` under `<userData>/audit/YYYY-MM-DD.jsonl` (SEC-04).
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-streaming-chat/01-01-SUMMARY.md` when done.
</output>
