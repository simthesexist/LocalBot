---
phase: 09-phone-reach-ship
plan: 01
type: tracer
wave: 1
depends_on: ["phase-08-browser-automation-08-03"]
files_modified:
  - package.json
  - package-lock.json
  - daemon/network/config.cjs
  - daemon/main.cjs
  - src/main/network/server.ts
  - src/main/network/handlers.ts
  - src/main/network/static.ts
  - src/main/network/index.ts
  - src/main/ipc/network.ts
  - src/main/index.ts
  - src/main/paths.ts
  - src/main/preload/index.ts
  - src/shared/types.ts
  - src/shared/ipc-channels.ts
  - src/shared/window.d.ts
  - tests/unit/ws_server.test.ts
  - tests/unit/ws_handlers.test.ts
  - tests/unit/network_config.test.ts
  - tests/unit/preload.test.ts
autonomous: true
requirements:
  - NET-01
  - NET-02

estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Localbot listens on configurable port (default 7878) for HTTP + WebSocket control (NET-01); server binds to host=127.0.0.1 by default (localhost only) and 0.0.0.0 when bindMode=='lan' (NET-03 partial via config persistence; rebind logic deferred to Wave 2)"
    - "Phone browser can open ws://<host>:7878 and chat with a bot via the same runAgenticLoop that src/main/ipc/chat.ts uses (NET-02); per-msgId AbortController map mirrors chat.ts cancel semantics; abort on WS close fires for in-flight runs"
    - "WS messages are JSON-parsed with shape validation before dispatch; unknown msg.type, missing msgId, or non-string content is dropped silently (no exception leaks to the client); malformed JSON is logged once and dropped"
    - "Phone bundle directory served via http.createServer; static files served under GET / (index.html) + GET /assets/* with path-traversal guard via path.join containment; 404 for any other path"
    - "Audit row for WS sendMessage carries ONLY {action:'ws.send_message', bot, msgId, durationMs, outcome}; never the message content, never the bot persona, never token deltas (specless-probe-fallback: no plaintext message text in audit)"
    - "Network config persists at <userData>/network.json with shape {port, bindMode, updateChannel}; atomic tmp+rename + persistQueue serialization mirrors daemon/vault/config.cjs:50-100; corrupt JSON returns defensive default {port:7878, bindMode:'localhost', updateChannel:'latest'}"
  artifacts:
    - "daemon/network/config.cjs (NEW) — exports loadNetworkConfig, saveNetworkConfig, networkConfigPath, defaultNetworkConfig, __test__"
    - "src/main/network/server.ts (NEW) — exports startNetworkServer() returning NetworkServerHandle {host, port, close, rebind}"
    - "src/main/network/handlers.ts (NEW) — exports dispatchWsMessage(ws, req); module-scope Map<msgId, AbortController>"
    - "src/main/network/static.ts (NEW) — exports servePhoneBundle(req, res, phoneBundleDir) with path-traversal guard"
    - "src/main/network/index.ts (NEW) — barrel + exports registerNetworkHandlers() and startNetworkServer()"
    - "src/main/ipc/network.ts (NEW) — exports registerNetworkHandlers() registering NETWORK_GET_CONFIG + NETWORK_SET_CONFIG + NETWORK_GET_REACH_INFO"
    - "src/shared/types.ts (EXTEND) — NetworkConfig interface {port:number, bindMode:'localhost'|'lan', updateChannel:'latest'}; ReachInfo interface; NetworkConfigResult interface; WsSendMessageRequest interface"
    - "src/shared/ipc-channels.ts (EXTEND) — NETWORK_GET_CONFIG, NETWORK_SET_CONFIG, NETWORK_GET_REACH_INFO"
    - "src/shared/window.d.ts (EXTEND) — LocalbotApi.network namespace {getConfig, setConfig, getReachInfo}; no event channels in this wave"
    - "src/main/paths.ts (EXTEND) — networkConfigPath() helper + phoneBundleDir() helper + ensurePhoneBundleDir()"
    - "src/main/preload/index.ts (EXTEND) — api.network namespace with getConfig/setConfig/getReachInfo"
    - "src/main/index.ts (MODIFY) — invoke registerNetworkHandlers() + startNetworkServer() AFTER spawnDaemon() resolves"
    - "daemon/main.cjs (MODIFY) — JSON-RPC cases network/get_config + network/set_config; currentNetworkConfig module-scope mirror of vault pattern"
    - "package.json + package-lock.json — ws@^8.21.3 added as runtime dependency"
    - "tests/unit/ws_server.test.ts (NEW, >= 8 cases) — bind modes, port collision rollback, 404, graceful close, static path traversal guard"
    - "tests/unit/ws_handlers.test.ts (NEW, >= 6 cases) — sendMessage round-trip, cancel, malformed JSON dropped, unknown type dropped, abort on ws close"
    - "tests/unit/network_config.test.ts (NEW, >= 6 cases) — atomic round-trip, default on missing/corrupt, concurrent save serialization"
    - "tests/unit/preload.test.ts (EXTEND) — api.network.{getConfig,setConfig,getReachInfo} surface"
  key_links:
    - "src/main/network/handlers.ts dispatchWsMessage -> receives {type:'sendMessage', msgId, content, bot?} -> registers AbortController in activeWsRuns map -> calls runAgenticLoop(...) reusing src/main/llm/loop.ts:150 -> on each onToken/onToolUse/onToolResult fires ws.send(JSON.stringify({type:'token'|'toolUse'|'toolResult', msgId, ...payload})) -> on completion ws.send({type:'messageDone', msgId}); on cancel ws.send({type:'messageError', msgId, error:'cancelled'}); finally activeWsRuns.delete(msgId)"
    - "src/main/network/server.ts startNetworkServer -> reads daemon/network/config.cjs loadNetworkConfig -> resolves host (127.0.0.1 vs 0.0.0.0) + port (7878 default) -> http.createServer wrapping servePhoneBundle + WebSocketServer({server}) -> listen(host, port) -> returns handle; rebind closes old + opens new (Wave 2 adds the bind-mode toggle UX)"
    - "src/main/index.ts -> after spawnDaemon resolves -> const networkHandle = await startNetworkServer() -> logs 'Network server bound on <host>:<port>' on stdout; audit log gets a 'lifecycle' entry with {event:'network.bind', host, port}"
    - "src/main/ipc/network.ts registerNetworkHandlers -> ipcMain.handle(NETWORK_GET_CONFIG) proxies callBot('network/get_config', {}) -> returns {ok:true, config:{port, bindMode, updateChannel}}; NETWORK_SET_CONFIG validates shape client-side BEFORE callBot to give inline errors"
    - "daemon/main.cjs network/get_config -> loadNetworkConfig(userDataDirState) -> replyResult(id, {ok:true, config}); network/set_config -> saveNetworkConfig(userDataDirState, cfg) with persistQueue -> updates currentNetworkConfig -> replyResult(id, {ok:true, config:written})"
  prohibitions:
    - "WS server MUST NOT bind to 0.0.0.0 when bindMode === 'localhost' (default); localhost-only invariant is the security baseline for NET-03 v1"
    - "WS handler MUST NOT log message content to stdout or audit (Pitfall 1: no plaintext message text); only metadata {msgId, bot, durationMs, outcome}"
    - "Static phone bundle server MUST reject paths containing '..' segments or paths that escape phoneBundleDir (path.join containment check BEFORE fs.existsSync)"
    - "saveNetworkConfig MUST serialize concurrent writes via persistQueue (mirrors daemon/vault/config.cjs:50-100); a renderer-driven set_config followed immediately by another must not interleave bytes"
    - "loadNetworkConfig MUST return defensive default {port:7878, bindMode:'localhost', updateChannel:'latest'} on missing file, corrupt JSON, or shape drift — never throw to renderer"
    - "Must NOT register bind-mode toggle or rebind-on-setting-change in this plan (Wave 2 scope); Wave 1 binds once at startup and exposes config via IPC; toggling requires Wave 2 server.ts.rebind logic"
    - "Must NOT add Tailwind, socket.io, Express, or any HTTP framework (RESEARCH §Alternatives Considered); use Node stdlib http + ws@8 only"
    - "Must NOT install the full `playwright` npm package; playwright-core (already in deps) + playwright-chromium (devDep) covers browser E2E (no Playwright needed for phone-reach WS test in Wave 3)"
    - "Must NOT add native modules that require building (CLAUDE.md constraint); `ws` is pure-JS with optional native helpers (bufferutil/utf-8-validate) — those are optionalDeps and not pulled in by default"

# Artifacts this phase produces (full list)
#
# Plan 1 (this file) NEW files:
#   daemon/network/config.cjs
#   src/main/network/server.ts
#   src/main/network/handlers.ts
#   src/main/network/static.ts
#   src/main/network/index.ts
#   src/main/ipc/network.ts
#   tests/unit/ws_server.test.ts
#   tests/unit/ws_handlers.test.ts
#   tests/unit/network_config.test.ts
# Plan 1 MODIFIED files:
#   package.json (deps: ws@^8.21.3)
#   package-lock.json (regenerated)
#   daemon/main.cjs (JSON-RPC cases network/get_config + network/set_config)
#   src/main/index.ts (registerNetworkHandlers + startNetworkServer wiring)
#   src/main/paths.ts (networkConfigPath + phoneBundleDir + ensurePhoneBundleDir helpers)
#   src/main/preload/index.ts (api.network namespace)
#   src/shared/types.ts (NetworkConfig + ReachInfo + NetworkConfigResult + WsSendMessageRequest)
#   src/shared/ipc-channels.ts (NETWORK_GET_CONFIG + NETWORK_SET_CONFIG + NETWORK_GET_REACH_INFO)
#   src/shared/window.d.ts (LocalbotApi.network namespace)
#   tests/unit/preload.test.ts (api.network surface cases)
#
# Plan 2 NEW files:
#   src/main/network/tailscale.ts
#   src/main/network/tailscale.test.ts (or tests/unit/tailscale.test.ts)
#   src/phone/index.html
#   src/phone/main.tsx
#   src/phone/vite.config.ts
#   src/phone/components/Composer.tsx
#   src/phone/components/MessageBubble.tsx
#   src/phone/components/Chat.tsx
#   src/phone/styles.css
#   src/renderer/components/NetworkSettingsModal.tsx
#   src/renderer/components/ReachInfoPill.tsx
#   src/renderer/state/network.ts
#   tests/unit/tailscale.test.ts
#   tests/playwright/phone-reach.test.ts (extended Wave 3; Wave 2 may scaffold)
# Plan 2 MODIFIED files:
#   src/main/network/server.ts (rebind + tailscale integration)
#   src/main/network/index.ts (expose rebind + ReachInfo subscription)
#   src/main/ipc/network.ts (NETWORK_GET_REACH_INFO + EVENT_REACH_INFO_UPDATED)
#   src/shared/types.ts (ReachInfo + ReachInfoEvent interfaces)
#   src/shared/ipc-channels.ts (EVENT_REACH_INFO_UPDATED)
#   src/shared/window.d.ts (LocalbotEvent 'network:reach:updated')
#   src/main/preload/index.ts (EVENT_CHANNELS extension + api.network.getReachInfo)
#   src/renderer/components/App.tsx (ReachInfoPill mount + NetworkSettingsModal mount)
#   src/renderer/styles/app.css (reach-pill + network-settings-modal styles)
#   package.json (scripts.build:phone; scripts.build chains)
#   vite.config.ts (no change; phone is separate vite.config)
#
# Plan 3 NEW files:
#   src/main/network/updater.ts
#   app-update.yml (repo root)
#   build/icon.ico (placeholder)
#   src/renderer/components/UpdateToast.tsx
#   tests/unit/updater.test.ts
# Plan 3 MODIFIED files:
#   package.json (deps: electron-updater@^6.8.9; devDeps: electron-builder upgrade to ^26.15.3 if peer-dep compatible else stay on 25.x)
#   package.json (scripts.dist: 'electron-builder --win --publish never')
#   package.json#build (electron-builder NSIS config: perMachine:false, oneClick:false, allowToChangeInstallationDirectory:true, files include dist/phone/**)
#   src/main/ipc/network.ts (NETWORK_CHECK_FOR_UPDATE + NETWORK_DOWNLOAD_UPDATE + NETWORK_INSTALL_UPDATE + EVENT_UPDATE_STATUS_CHANGED)
#   src/main/index.ts (initUpdater + registerUpdateIpcChannels)
#   src/shared/ipc-channels.ts (NETWORK_CHECK_FOR_UPDATE + NETWORK_DOWNLOAD_UPDATE + NETWORK_INSTALL_UPDATE + EVENT_UPDATE_STATUS_CHANGED)
#   src/shared/types.ts (UpdateStatusEvent)
#   src/shared/window.d.ts (LocalbotEvent 'network:update:status')
#   src/renderer/components/NetworkSettingsModal.tsx (Check-for-updates button)
#   src/main/preload/index.ts (api.network.checkForUpdate/downloadUpdate/installUpdate + EVENT_CHANNELS extension)
#   tests/playwright/phone-reach.test.ts (Wave 0 scaffold + 4 E2E cases)
#   playwright.config.ts (extend daemon-smoke project with phone-reach.test.ts)
#   README.md (document code-signing limitation + SmartScreen workaround)

<objective>
Wave 1 tracer slice: prove the WS control surface inside Electron main
binds on localhost:7878 by default, accepts a JSON `{type:'sendMessage',
msgId, content, bot}` envelope, and routes through the SAME
`runAgenticLoop` that `src/main/ipc/chat.ts` uses — so the phone chat
cycle is the desktop chat cycle (NET-02). The persistent network config
at `<userData>/network.json` carries `{port, bindMode, updateChannel}`
(mirroring `<userData>/vault.json` atomic-write pattern). Bind mode is
localhost only this wave; the LAN-bind opt-in (NET-03 full) lands in
Wave 2 alongside the rebind path + NetworkSettingsModal + ReachInfoPill.

Purpose: prove the security-critical invariants (localhost-only default,
per-msgId AbortController, audit minimization on WS sendMessage, atomic
config persistence, path-traversal guard on phone bundle serving)
BEFORE adding Tailscale detection (Wave 2) and the electron-builder NSIS
installer (Wave 3).

Output: 5 new CJS/TS modules + 4 new IPC channels + 1 new
`<userData>/network.json` file + 4 new Vitest suites (>= 20 passing
cases) + TypeScript build clean + manual `node ws-client.js` round-trip
succeeds against `localhost:7878`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-phone-reach-ship/09-RESEARCH.md
@.planning/phases/08-browser-automation/08-01-PLAN.md
@.planning/phases/08-browser-automation/08-01-SUMMARY.md
@.planning/phases/08-browser-automation/08-PATTERNS.md

# Source files for direct reference
@daemon/vault/config.cjs (atomic-JSON persistence pattern to mirror)
@daemon/main.cjs (vault/get_config + vault/set_config cases at L1148-1245 — direct template for network/get_config + network/set_config)
@src/main/ipc/chat.ts (sendMessage + cancel + per-msgId AbortController map at L41 + L145-296 — direct template for WS handlers.ts)
@src/main/llm/loop.ts (runAgenticLoop at L150-294 — both IPC and WS call sites consume this)
@src/main/index.ts (handler registration ordering at L63-106 — registerNetworkHandlers + startNetworkServer wiring)
@src/main/paths.ts (vaultConfigPath at L101-103 + ensureWorkspace at L36-40 — template for networkConfigPath + ensurePhoneBundleDir)
@src/main/preload/index.ts (api.* surface at L41-98 — extend with api.network namespace)
@src/shared/types.ts (BotConfig at L355-400 — template for NetworkConfig interface)
@src/shared/ipc-channels.ts (CHANNELS object at L3-84 — extend with NETWORK_* channels)
@src/shared/window.d.ts (LocalbotApi + LocalbotEvent type definitions)
@src/main/ipc/vault.ts (registerVaultHandlers template — same broadcast pattern)
@tests/unit/vault_config.test.ts (template for network_config.test.ts: mkdtemp + atomic round-trip + corrupt JSON defensive default)
@package.json (current deps; +ws@^8.21.3)
@09-RESEARCH.md §Pattern 1 (HTTP + WS server lifecycle); §Pattern 2 (WS message dispatch); §Architectural Responsibility Map; §Package Legitimacy Audit; §Pitfall 1 (no WS auth v1); §Pitfall 2 (port collision on rebind — Wave 2 fix); §Common Operation 1 (WS lifecycle)
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Install ws@^8.21.3 + daemon/network/config.cjs + daemon/main.cjs network/get_config + network/set_config + src/main/network/{server,handlers,static,index}.ts + src/main/ipc/network.ts + shared types + shared ipc-channels + shared window.d.ts + paths + preload + registerNetworkHandlers in src/main/index.ts</name>
  <files>
    - package.json
    - package-lock.json
    - daemon/network/config.cjs
    - daemon/main.cjs
    - src/main/network/server.ts
    - src/main/network/handlers.ts
    - src/main/network/static.ts
    - src/main/network/index.ts
    - src/main/ipc/network.ts
    - src/main/index.ts
    - src/main/paths.ts
    - src/main/preload/index.ts
    - src/shared/types.ts
    - src/shared/ipc-channels.ts
    - src/shared/window.d.ts
  </files>
  <read_first>
    - daemon/vault/config.cjs (atomic-JSON persistence pattern to mirror; defaultVaultConfig + loadVaultConfig + saveVaultConfig + persistQueue + __test__ at L1-131)
    - daemon/main.cjs (vault/get_config case at L1148-1176 + vault/set_config case at L1178-1245 — direct template; module-scope currentVaultConfig; audit.appendAudit + replyResult/replyError from daemon/protocol.cjs)
    - daemon/protocol.cjs (sendNotification helper; replyResult/replyError pattern)
    - src/main/ipc/chat.ts (sendMessage handler at L145-235 + cancel handler at L285-295 + activeStreams Map at L41 + broadcast helper at L50-54 — direct template for WS handlers.ts)
    - src/main/llm/loop.ts (runAgenticLoop signature at L25-48 + return AgenticLoopResult at L50-56 — both IPC and WS reuse this exact function)
    - src/main/index.ts (handler registration site at L63-106; spawnDaemon import at L18; protocol.registerSchemesAsPrivileged top-level at L29-45 — registerNetworkHandlers + startNetworkServer wiring goes AFTER spawnDaemon resolves)
    - src/main/paths.ts (vaultConfigPath at L101-103 + ensureWorkspace at L36-40 — template for networkConfigPath + ensurePhoneBundleDir)
    - src/main/preload/index.ts (EVENT_CHANNELS Set at L7-30 + api namespace at L41-98 + contextBridge.exposeInMainWorld pattern)
    - src/shared/types.ts (BotConfig interface at ~L355-400 — extend shape; MessageBlock union at L15-52 — no new variants in this plan)
    - src/shared/ipc-channels.ts (CHANNELS object at L3-84 — add NETWORK_GET_CONFIG + NETWORK_SET_CONFIG + NETWORK_GET_REACH_INFO)
    - src/shared/window.d.ts (LocalbotApi + LocalbotEvent type definitions — extend with network namespace)
    - src/main/ipc/vault.ts (registerVaultHandlers template — same ipcMain.handle + broadcast-on-success pattern)
    - src/main/daemon/spawn.ts (callBot signature used by vault IPC; mirror for network IPC bridge)
    - package.json (current deps at L20-30 + scripts at L8-19 + devDeps at L31-48; +ws@^8.21.3)
    - 09-RESEARCH.md §Pattern 1 (HTTP + WS server lifecycle with serveStatic path-traversal guard); §Pattern 2 (WS message dispatch with per-msgId AbortController map); §Package Legitimacy Audit (ws marked [OK], pure-JS); §Architectural Responsibility Map (WS server in main, NOT daemon; daemon only owns config persistence); §Common Operation 1 (WS lifecycle)
  </read_first>
  <action>
    Twelve coordinated changes:

    1) Install ws@^8.21.3 (runtime). Run `npm install ws@^8.21.3 --save` from D:/Claude/Grokbot. Verify with `npm ls ws` (returns ws@8.21.x). package.json gains 1 entry under dependencies; package-lock.json regenerates. No other dep changes. The package's optionalDependencies (bufferutil, utf-8-validate) are NOT pulled in by default — confirmed via `npm view ws dependencies` returning {} empty.

    2) daemon/network/config.cjs (NEW, ~120 lines). CJS module exporting `loadNetworkConfig(userDataDir)`, `saveNetworkConfig(userDataDir, cfg)`, `networkConfigPath(userDataDir)`, `defaultNetworkConfig()`, `__test__`. Mirror daemon/vault/config.cjs:1-131 verbatim — atomic tmp+rename, persistQueue serialization, corrupt JSON returns defensive default. Shape: `{port: number (default 7878), bindMode: 'localhost'|'lan' (default 'localhost'), updateChannel: 'latest'|'beta'|'nightly' (default 'latest')}`. `isValidShape(parsed)` checks: object; port is integer in [1, 65535]; bindMode ∈ {'localhost','lan'}; updateChannel ∈ {'latest','beta','nightly'}; else return defaultNetworkConfig(). `saveNetworkConfig` rejects with `{code:'invalid_network_config'}` on shape violation; persists via persistQueue.then chain.

    3) daemon/main.cjs (MODIFY): add `const network = require('./network/config.cjs');` near the existing `const vault = require('./vault/config.cjs');` import at the top. Module-scope: `let currentNetworkConfig = null;`. Two new JSON-RPC cases INSIDE the existing dispatch switch (mirror vault/get_config at L1148-1176 and vault/set_config at L1178-1245):
       - `case 'network/get_config':` -> loadNetworkConfig(userDataDirState || '') -> currentNetworkConfig = cfg -> audit.appendAudit({tool:'network.get_config', bot:currentBot, params:{}, outcome:'ok', durationMs: Date.now()-startedAt}) -> replyResult(id, {ok:true, config: cfg}).
       - `case 'network/set_config':` -> validate cfg.port integer [1,65535], cfg.bindMode ∈ {'localhost','lan'}, cfg.updateChannel ∈ {'latest','beta','nightly'} (throw {code:'invalid_network_config', message} on shape violation) -> saveNetworkConfig(userDataDirState, cfg).then(written => { currentNetworkConfig = written; audit.appendAudit({tool:'network.set_config', bot:currentBot, params:{}, outcome:'ok', durationMs: Date.now()-startedAt}); replyResult(id, {ok:true, config: written}); }).catch(err => replyError(id, err.code || 'network_set_config_failed', err.message)).
       Both cases gated behind `if (!userDataDirState)` -> throw {code:'daemon_not_initialized'}.

    4) src/main/network/server.ts (NEW, ~110 lines). TypeScript module exporting `startNetworkServer(opts: {phoneBundleDir: string}): Promise<NetworkServerHandle>`. Imports: `http` from 'node:http'; `WebSocketServer`, `WebSocket` from 'ws'; `path` from 'node:path'; `dispatchWsMessage` from './handlers'; `loadNetworkConfig` from '../../daemon/network/config.cjs' (note: require()-style for CJS, via createRequire); `userDataDir` from '../paths'; `appendAuditLine` from '../audit/logger'. Logic:
       - `config = await loadNetworkConfig(userDataDir())`; resolve `host = config.bindMode === 'lan' ? '0.0.0.0' : '127.0.0.1'` and `port = typeof config.port === 'number' ? config.port : 7878` (defensive default).
       - `const server = http.createServer((req, res) => servePhoneBundle(req, res, opts.phoneBundleDir));`
       - `const wss = new WebSocketServer({ server }); wss.on('connection', (ws, req) => { void dispatchWsMessage(ws, req); });`
       - `await new Promise<void>((resolve, reject) => { server.once('error', reject); server.listen(port, host, () => { server.removeListener('error', reject); resolve(); }); });`
       - `await appendAuditLine({bot: '__system__', tool: 'lifecycle', params: {event: 'network.bind', host, port}, outcome: 'ok', durationMs: 0});` (Pitfall 6: log bind event for "why can my neighbor hit my bot" diagnostics).
       - `console.info(`[network] bound on ${host}:${port}`);` (single line on bind for at-a-glance status).
       - Return `NetworkServerHandle { host, port, close: () => new Promise<void>(resolve => server.close(() => resolve())), rebind: async (newHost, newPort) => { /* Wave 2 closes old + opens new */ throw new Error('rebind not implemented in Wave 1'); } }`.
       Wave 1's `rebind` throws — the bind-mode toggle UX lands in Wave 2 with the rebind implementation. The handle shape is forward-compatible.

    5) src/main/network/handlers.ts (NEW, ~120 lines). TypeScript module exporting `dispatchWsMessage(ws: WebSocket, _req: http.IncomingMessage): Promise<void>`. Imports: `runAgenticLoop` from '../llm/loop'; `TOOL_SCHEMAS` from '../llm/tools'; `loadConfigIntoSystemPrompt` from '../bots/policy'; `readMemory, injectMemorySuffix` from '../bots/memory'; `callMemory` from '../daemon/spawn'; `type { ChatMessage }` from '../../shared/types'; `type { WebSocket }` from 'ws'. Module-scope: `const activeWsRuns = new Map<string, AbortController>();` (msgId -> controller; mirrors chat.ts activeStreams at L41).
       On `ws.on('message', async (raw) => {...})`:
       - `let msg: unknown; try { msg = JSON.parse(raw.toString()); } catch { return; }` (malformed JSON dropped silently — Pitfall 1).
       - Validate shape: `if (!msg || typeof msg !== 'object') return;`
       - `const m = msg as { type?: unknown; msgId?: unknown; content?: unknown; bot?: unknown };`
       - `if (m.type !== 'sendMessage') return;`
       - `if (typeof m.msgId !== 'string' || m.msgId.length === 0) return;`
       - `if (typeof m.content !== 'string') return;`
       - `if (m.content.length === 0 || m.content.length > 4096) return;` (V5 input validation cap; mirrors daemon/bots/loader.cjs prompt cap).
       - `const ac = new AbortController(); activeWsRuns.set(m.msgId, ac);`
       - `const bot = (typeof m.bot === 'string' && m.bot.length > 0) ? m.bot : 'default';`
       - `ws.send(JSON.stringify({type:'messageStarted', msgId: m.msgId, bot}));`
       - Wrap in try/catch/finally (mirror chat.ts L249-276):
         - `let personaSystem = DEFAULT_SYSTEM_PROMPT_BASE; try { personaSystem = (await loadConfigIntoSystemPrompt(bot)).system; } catch { /* ignore */ }`
         - `const mem = await readMemory(bot); const system = injectMemorySuffix(personaSystem, mem.markdown, mem.facts);`
         - `await runAgenticLoop({ messages: [{ts:Date.now(), role:'user', content: m.content}], system, tools: TOOL_SCHEMAS, signal: ac.signal, bot, onToken: (delta) => ws.send(JSON.stringify({type:'token', msgId: m.msgId, delta})), onToolUse: (b) => ws.send(JSON.stringify({type:'toolUse', msgId: m.msgId, toolUseId: b.id, name: b.name, input: b.input})), onToolResult: (r) => ws.send(JSON.stringify({type:'toolResult', msgId: m.msgId, toolUseId: r.toolUseId, content: r.content, isError: r.isError})), });`
         - `ws.send(JSON.stringify({type:'messageDone', msgId: m.msgId}));`
         - catch: `ws.send(JSON.stringify({type:'messageError', msgId: m.msgId, error: (err as Error).message}));`
         - finally: `activeWsRuns.delete(m.msgId);`
       Handle `m.type === 'cancel'` -> `activeWsRuns.get(m.msgId)?.abort()` (mirrors chat.ts cancel handler at L285-295).
       Handle `ws.on('close')` -> iterate `activeWsRuns.values()` and call `.abort()` on each (cancel on WS disconnect, mirrors chat.ts semantics — though chat.ts does not iterate on disconnect; this is the WS-specific addition).

    6) src/main/network/static.ts (NEW, ~50 lines). TypeScript module exporting `servePhoneBundle(req: http.IncomingMessage, res: http.ServerResponse, rootDir: string): void`. Imports: `path` from 'node:path'; `fs` from 'node:fs'.
       - `const urlPath = (req.url ?? '/').split('?')[0];`
       - `if (urlPath === '/' || urlPath === '/index.html') { fs.createReadStream(path.join(rootDir, 'index.html')).pipe(res); return; }`
       - `if (urlPath.startsWith('/assets/')) { const full = path.normalize(path.join(rootDir, urlPath)); if (!full.startsWith(path.normalize(rootDir) + path.sep) && full !== path.normalize(rootDir)) { res.writeHead(403).end(); return; } /* path traversal guard */ if (!fs.existsSync(full)) { res.writeHead(404).end(); return; } /* content-type by ext + createReadStream pipe */ }`
       - default: `res.writeHead(404).end();`
       Sets correct Content-Type: text/html for .html, application/javascript for .js, text/css for .css, image/svg+xml for .svg, image/png for .png, application/octet-stream fallback. (Wave 2 may extend with .svg + .png when the phone bundle adds icons.)

    7) src/main/network/index.ts (NEW, ~25 lines). Barrel + startup. Exports `registerNetworkHandlers(): void` (delegates to src/main/ipc/network.ts registerNetworkHandlers), `startNetworkServer(): Promise<NetworkServerHandle>` (delegates to ./server.ts), `__test__: { handlers: dispatchWsMessage, servePhoneBundle }`. Re-exports `NetworkServerHandle` type.

    8) src/main/ipc/network.ts (NEW, ~80 lines). TypeScript module exporting `registerNetworkHandlers(): void`. Imports: `ipcMain` from 'electron'; `CHANNELS` from '../../shared/ipc-channels'; `callBot` from '../daemon/spawn'; `appendAuditLine` from '../audit/logger'; `type { NetworkConfig, NetworkConfigResult }` from '../../shared/types'. Logic:
       - `ipcMain.handle(CHANNELS.NETWORK_GET_CONFIG, async () => { const r = await callBot<{ok:true, config:NetworkConfig}>('network/get_config', {}); return r; });`
       - `ipcMain.handle(CHANNELS.NETWORK_SET_CONFIG, async (_evt, cfg: NetworkConfig) => { /* client-side shape validation BEFORE callBot: port integer [1,65535]; bindMode ∈ {'localhost','lan'}; updateChannel ∈ {'latest','beta','nightly'} */ if invalid return {ok:false, error:'invalid_network_config'}; const r = await callBot<NetworkConfigResult>('network/set_config', cfg); if (r?.ok && r.config) { await appendAuditLine({bot:'__system__', tool:'lifecycle', params:{event:'network.bind_change', port:r.config.port, bindMode:r.config.bindMode}, outcome:'ok', durationMs:0}); } return r; });`
       - `ipcMain.handle(CHANNELS.NETWORK_GET_REACH_INFO, async () => { /* Wave 2 wires this; placeholder returns {tailscale:false, lanIps:[]} */ return {tailscale:false, lanIps:[]}; });`

    9) src/shared/types.ts (MODIFY): add three interfaces + one request interface:
       - `export interface NetworkConfig { port: number; bindMode: 'localhost' | 'lan'; updateChannel: 'latest' | 'beta' | 'nightly'; }` (place after BotConfig at ~L400).
       - `export interface NetworkConfigResult { ok: boolean; config?: NetworkConfig; error?: string; }`.
       - `export interface ReachInfo { tailscale: boolean; magicDnsName?: string; lanIps: string[]; error?: string; }` (Wave 2 fills the fields; Wave 1 declares the shape).
       - `export interface ReachInfoEvent extends ReachInfo { at: number; }`.
       - `export interface WsSendMessageRequest { msgId: string; content: string; bot?: string; }`.
       - `export interface UpdateStatusEvent { state: 'idle'|'checking'|'available'|'downloading'|'downloaded'|'error'; currentVersion?: string; availableVersion?: string; progress?: {percent:number}; error?: string; }` (Wave 3 fills the payload; Wave 1 declares for forward-compat).

    10) src/shared/ipc-channels.ts (MODIFY): add three constants to CHANNELS (place after EVENT_BROWSER_PAGE_CLOSED at L80): `NETWORK_GET_CONFIG: 'network:get_config'`, `NETWORK_SET_CONFIG: 'network:set_config'`, `NETWORK_GET_REACH_INFO: 'network:get_reach_info'`. (Wave 2 + 3 add more constants; this wave adds the 3 IPC bridge channels.)

    11) src/shared/window.d.ts (MODIFY): in LocalbotApi, add `network: { getConfig: () => Promise<NetworkConfigResult>; setConfig: (cfg: NetworkConfig) => Promise<NetworkConfigResult>; getReachInfo: () => Promise<ReachInfo>; }`. (ReachInfo subscription via EVENT_REACH_INFO_UPDATED lands in Wave 2; this wave declares the invoke surface.)

    12) src/main/paths.ts (MODIFY): add three helpers after screenshotPath:
       - `export function networkConfigPath(): string { return path.join(userDataDir(), 'network.json'); }`
       - `export function phoneBundleDir(): string { return path.join(userDataDir(), 'phone-bundle'); }`
       - `export async function ensurePhoneBundleDir(): Promise<string> { const dir = phoneBundleDir(); await fs.mkdir(dir, {recursive: true}); return dir; }`

    13) src/main/preload/index.ts (MODIFY): in api namespace (after `browser:` block), add `network: { getConfig: () => ipcRenderer.invoke(CHANNELS.NETWORK_GET_CONFIG), setConfig: (cfg: NetworkConfig) => ipcRenderer.invoke(CHANNELS.NETWORK_SET_CONFIG, cfg), getReachInfo: () => ipcRenderer.invoke(CHANNELS.NETWORK_GET_REACH_INFO) }`. Import NetworkConfig + NetworkConfigResult + ReachInfo from '../../shared/types'. No EVENT_CHANNELS additions in this wave.

    14) src/main/index.ts (MODIFY): three changes.
       (a) Add imports: `import { registerNetworkHandlers, startNetworkServer } from './network';` and `import { phoneBundleDir, ensurePhoneBundleDir } from './paths';`.
       (b) Inside `app.whenReady().then(async () => {...})` AFTER `registerBrowserHandlers()` (L91), add `registerNetworkHandlers();` (IPC channels are pure handler registration — no I/O side effects).
       (c) AFTER `void spawnDaemon();` (L96), wrap the network server startup in `void (async () => { await ensurePhoneBundleDir(); const handle = await startNetworkServer({phoneBundleDir: phoneBundleDir()}); app.on('before-quit', async () => { await handle.close(); }); })();`. (The before-quit hook closes the WS server cleanly on app exit.)

    Avoid: any renderer-side UI changes (Wave 2 scope for NetworkSettingsModal + ReachInfoPill). Avoid: rebind() implementation (Wave 2). Avoid: Tailscale detection (Wave 2). Avoid: electron-updater / electron-builder (Wave 3). Avoid: phone UI Vite target (Wave 2). Avoid: socket.io, Express, or any HTTP framework (RESEARCH §Alternatives Considered; use Node stdlib http + ws@8 only). Avoid: any audit row that includes message content (Pitfall 1: only metadata {msgId, bot, durationMs, outcome}). Avoid: importing ws into the renderer (preload never sees ws; only main + tests). Avoid: native modules (CLAUDE.md constraint; ws is pure-JS with optional native helpers in optionalDeps).

    Validation step (REQUIRED before Task 2): after all 14 changes, run `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` from D:/Claude/Grokbot. Exit 0 (TS build clean) is the gate. Then run `npm ls ws 2>&1 | grep -E '^ws@'` — must return 1 line. Then run `node -e "const r = require('./daemon/network/config.cjs'); console.log(Object.keys(r).sort());"` — must print a list including loadNetworkConfig, saveNetworkConfig, networkConfigPath, defaultNetworkConfig, __test__. Then run `node -e "const { WebSocketServer } = require('ws'); console.log(typeof WebSocketServer);"` — must print 'function'. Then run `npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30` — must exit 0.
  </action>
  <acceptance_criteria>
    - `npm ls ws` exits 0 with 1 line (ws@8.21.x)
    - package.json has ws@^8.21.3 under dependencies
    - daemon/network/config.cjs exists; exports loadNetworkConfig, saveNetworkConfig, networkConfigPath, defaultNetworkConfig, __test__
    - daemon/main.cjs has JSON-RPC cases 'network/get_config' and 'network/set_config' (grep matches `case 'network/get_config'\|case 'network/set_config'`)
    - daemon/main.cjs 'network/set_config' validates port integer [1,65535], bindMode ∈ {'localhost','lan'}, updateChannel ∈ {'latest','beta','nightly'} BEFORE saveNetworkConfig (throw {code:'invalid_network_config'})
    - src/main/network/server.ts exists; exports startNetworkServer returning NetworkServerHandle {host, port, close, rebind}
    - src/main/network/handlers.ts exists; exports dispatchWsMessage; module-scope activeWsRuns Map<msgId, AbortController>
    - src/main/network/handlers.ts drops malformed JSON silently (try/catch around JSON.parse; no throw to ws client)
    - src/main/network/handlers.ts validates msgId non-empty string, content non-empty string <= 4096 chars, bot optional non-empty string; unknown msg.type dropped
    - src/main/network/handlers.ts WS on close iterates activeWsRuns.values() and calls .abort() on each
    - src/main/network/static.ts exists; exports servePhoneBundle with path-traversal guard (path.normalize containment check BEFORE fs.existsSync)
    - src/main/network/index.ts exists; barrel exports startNetworkServer + registerNetworkHandlers
    - src/main/ipc/network.ts exists; registers NETWORK_GET_CONFIG + NETWORK_SET_CONFIG + NETWORK_GET_REACH_INFO handlers
    - src/main/ipc/network.ts NETWORK_SET_CONFIG does client-side shape validation BEFORE callBot (returns {ok:false, error:'invalid_network_config'} on invalid)
    - src/shared/types.ts exports NetworkConfig + NetworkConfigResult + ReachInfo + ReachInfoEvent + WsSendMessageRequest + UpdateStatusEvent interfaces
    - src/shared/ipc-channels.ts contains NETWORK_GET_CONFIG, NETWORK_SET_CONFIG, NETWORK_GET_REACH_INFO
    - src/shared/window.d.ts LocalbotApi.network namespace exists with getConfig/setConfig/getReachInfo
    - src/main/paths.ts exports networkConfigPath + phoneBundleDir + ensurePhoneBundleDir
    - src/main/preload/index.ts api.network namespace exists
    - src/main/index.ts registerNetworkHandlers invoked AFTER registerBrowserHandlers; startNetworkServer invoked AFTER spawnDaemon; before-quit hook closes the handle
    - src/main/index.ts calls ensurePhoneBundleDir BEFORE startNetworkServer
    - `node -e "require('./daemon/network/config.cjs')"` exits 0
    - `node -e "require('ws').WebSocketServer"` prints 'function' (or similar) without error
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && npm ls ws 2>&1 | grep -E '^ws@' && grep -l 'loadNetworkConfig\|saveNetworkConfig\|defaultNetworkConfig\|networkConfigPath' daemon/network/config.cjs daemon/main.cjs 2>&1 && grep -l 'startNetworkServer\|dispatchWsMessage\|servePhoneBundle\|NetworkServerHandle' src/main/network/server.ts src/main/network/handlers.ts src/main/network/static.ts src/main/network/index.ts 2>&1 && grep -l 'NETWORK_GET_CONFIG\|NETWORK_SET_CONFIG\|NETWORK_GET_REACH_INFO' src/shared/ipc-channels.ts src/main/ipc/network.ts src/main/preload/index.ts src/shared/window.d.ts 2>&1 && npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30</automated>
    <fails_when>npm ls missing ws, grep returns empty for any of the expected files, or tsc --noEmit reports any TS error</fails_when>
  </verify>
  <done>
    ws@^8.21.3 installed. daemon/network/config.cjs atomic <userData>/network.json persistence created (shape {port, bindMode, updateChannel}). daemon/main.cjs JSON-RPC cases network/get_config + network/set_config added with shape validation. src/main/network/{server,handlers,static,index}.ts created. WS server binds on 127.0.0.1:7878 by default; WS handler routes sendMessage through runAgenticLoop reusing src/main/llm/loop.ts; per-msgId AbortController map mirrors chat.ts; malformed JSON + unknown type dropped silently; content cap 4096 chars; abort on WS close. Static phone-bundle server with path-traversal guard. IPC bridge src/main/ipc/network.ts registers 3 channels. shared/types.ts + shared/ipc-channels.ts + shared/window.d.ts extended. paths.ts + preload updated. src/main/index.ts wires registerNetworkHandlers + startNetworkServer + before-quit cleanup. TS build clean. All wired symbols present in grep output. Wave 0 audit row added for 'lifecycle' params {event:'network.bind', host, port}.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests - ws_server + ws_handlers + network_config + preload extension (4 suites, >= 20 passing cases)</name>
  <files>
    - tests/unit/ws_server.test.ts
    - tests/unit/ws_handlers.test.ts
    - tests/unit/network_config.test.ts
    - tests/unit/preload.test.ts
  </files>
  <read_first>
    - tests/unit/vault_config.test.ts (template for network_config.test.ts: mkdtemp + atomic round-trip + corrupt JSON defensive default + concurrent save ordering)
    - tests/unit/vault_glob.test.ts (template for createRequire CJS import pattern)
    - tests/unit/scheduler_persistence.test.ts (template for JSON shape invariant assertions)
    - daemon/network/config.cjs (just created in Task 1 — loadNetworkConfig + saveNetworkConfig signatures)
    - src/main/network/server.ts (just created in Task 1 — startNetworkServer signature; test against the startNetworkServer factory NOT against the actual Electron-bound handler)
    - src/main/network/handlers.ts (just created in Task 1 — dispatchWsMessage signature; test by constructing a mock WebSocket + IncomingMessage)
    - src/main/network/static.ts (just created in Task 1 — servePhoneBundle signature; test with mock req/res + a temp phone bundle dir)
    - 09-RESEARCH.md §Pattern 1 (HTTP + WS server lifecycle with bind modes); §Pitfall 1 (no WS auth v1); §Pitfall 2 (port collision rollback — Wave 2 fix); §Wave 0 Gaps (test map)
  </read_first>
  <action>
    Create 3 new test suites + extend 1 existing suite. Total target: >= 20 new passing cases.

    1) tests/unit/ws_server.test.ts (NEW, >= 8 cases):
       - Use createRequire(import.meta.url) for CJS modules; `import { WebSocket, WebSocketServer } from 'ws'` for the client side.
       - Setup helper: `async function startTestServer(extraConfig?: Partial<NetworkConfig>) { const tmpUserData = await mkdtemp(...); writeFileSync(join(tmpUserData, 'network.json'), JSON.stringify({port:0, ...extraConfig})); /* port 0 lets the OS pick a free port */ const handle = await startNetworkServer({phoneBundleDir: tmpUserData}); return {handle, tmpUserData, close: handle.close}; }`.
       - Case A: bind to 127.0.0.1 with port 0 -> ws://127.0.0.1:<actualPort> connects; ws.onopen fires; ws.send('ping') returns.
       - Case B: bind mode 'lan' writes 0.0.0.0 -> startNetworkServer listens on 0.0.0.0; connect to ws://127.0.0.1:<port> still works (loopback accepted via 0.0.0.0 bind).
       - Case C: default config (no network.json) -> binds on 127.0.0.1:7878 OR fails with EADDRINUSE (test should be tolerant: assert bind either succeeds with 7878 OR fails with a known error code — both are acceptable in CI).
       - Case D: port collision -> start a test server on a known port, then start a second server on the SAME port -> second startNetworkServer throws (the server.listen error event rejects the start promise).
       - Case E: graceful close -> start server, connect ws, call handle.close(), then attempt ws.send -> ws error fires (server is closed).
       - Case F: rebind throws in Wave 1 -> handle.rebind('0.0.0.0', 7878) rejects with Error('rebind not implemented in Wave 1') (forward-compat for Wave 2).
       - Case G: static path-traversal guard -> GET /assets/../config.cjs (URL-encoded as %2e%2e or raw depending on test runner) -> servePhoneBundle returns 403 (path.join + path.normalize containment check rejects).
       - Case H: 404 fallback -> GET /unknown-path -> servePhoneBundle returns 404.
       - Case I: phone bundle GET / serves index.html -> create index.html in tmp dir with content '<html>phone</html>'; GET / reads the file content.

    2) tests/unit/ws_handlers.test.ts (NEW, >= 6 cases):
       - Mock `runAgenticLoop` from '../src/main/llm/loop' via vi.mock to avoid real LLM calls; record the onToken/onToolUse/onToolResult callbacks invoked during the test.
       - Mock `loadConfigIntoSystemPrompt` and `readMemory` to return deterministic strings.
       - Setup helper: `function makeMockWs() { const sent: any[] = []; let onMessage: ((data: any) => void) | null = null; let onClose: (() => void) | null = null; const ws = { send: (data: string) => sent.push(JSON.parse(data)), on: (event: string, cb: any) => { if (event === 'message') onMessage = cb; if (event === 'close') onClose = cb; } } as any; return { ws, sent, fireMessage: (raw: string) => onMessage?.(Buffer.from(raw)), fireClose: () => onClose?.() }; }`.
       - Case A: sendMessage round-trip -> fireMessage(JSON.stringify({type:'sendMessage', msgId:'m1', content:'hello', bot:'default'})) -> assert sent[] contains {type:'messageStarted', msgId:'m1', bot:'default'} -> mock runAgenticLoop invokes onToken('Hi') -> assert sent[] contains {type:'token', msgId:'m1', delta:'Hi'} -> runAgenticLoop resolves -> assert sent[] contains {type:'messageDone', msgId:'m1'}.
       - Case B: cancel mid-stream -> fire sendMessage; while runAgenticLoop is in-flight (use a deferred promise), fire {type:'cancel', msgId:'m1'} -> assert AbortController was aborted (verify by checking the signal.aborted flag in the mock).
       - Case C: malformed JSON dropped -> fireMessage('not-json') -> assert sent[] is empty (no throw, no leak to client).
       - Case D: unknown msg.type dropped -> fireMessage(JSON.stringify({type:'unknown', msgId:'m1'})) -> assert sent[] is empty.
       - Case E: missing msgId dropped -> fireMessage(JSON.stringify({type:'sendMessage', content:'hi'})) -> assert sent[] is empty.
       - Case F: content cap (4097 chars) dropped -> fireMessage with content.length=4097 -> assert sent[] is empty.
       - Case G: empty content dropped -> fireMessage with content='' -> assert sent[] is empty.
       - Case H: ws.on close aborts in-flight runs -> fire sendMessage; before runAgenticLoop resolves, fireClose() -> assert AbortController was aborted.

    3) tests/unit/network_config.test.ts (NEW, >= 6 cases):
       - Mirror tests/unit/vault_config.test.ts structure (mkdtemp + createRequire).
       - Case A: missing file -> loadNetworkConfig(tmpUserData) returns defaultNetworkConfig() {port:7878, bindMode:'localhost', updateChannel:'latest'}.
       - Case B: round-trip -> saveNetworkConfig(tmpUserData, {port:9999, bindMode:'lan', updateChannel:'beta'}) resolves; loadNetworkConfig(tmpUserData) returns the same object (deep equal).
       - Case C: corrupt JSON -> writeFileSync(join(tmpUserData, 'network.json'), '{not-json') -> loadNetworkConfig returns defaultNetworkConfig (defensive default).
       - Case D: shape drift (port is string) -> saveNetworkConfig with port='7878' rejects with {code:'invalid_network_config'}; the file is NOT written.
       - Case E: shape drift (bindMode invalid) -> saveNetworkConfig with bindMode='public' rejects with {code:'invalid_network_config'}; the file is NOT written.
       - Case F: concurrent saves serialize -> Promise.all([saveNetworkConfig(...{port:1001}), saveNetworkConfig(...{port:1002})]) -> both resolve; final file contains ONE of {1001, 1002} (no interleaved bytes); the other save's value is the LAST writer.
       - Case G: persistQueue is cleared on rejection -> saveNetworkConfig fails with bad shape -> next valid saveNetworkConfig still resolves (chain is not poisoned).

    4) tests/unit/preload.test.ts (EXTEND): add >= 3 cases for api.network surface.
       - Case A: getConfig() proxies ipcRenderer.invoke(CHANNELS.NETWORK_GET_CONFIG) -> mock ipcRenderer.invoke to capture the channel + return value; assert preload's api.network.getConfig() calls invoke with the right channel.
       - Case B: setConfig(cfg) proxies ipcRenderer.invoke(CHANNELS.NETWORK_SET_CONFIG, cfg) -> assert invoke called with channel + cfg payload.
       - Case C: getReachInfo() proxies ipcRenderer.invoke(CHANNELS.NETWORK_GET_REACH_INFO).

    Test infrastructure: createRequire(import.meta.url) for CJS module imports (daemon-side). mkdtemp workspace for any test that touches the filesystem. vi.fn() + vi.mock for runAgenticLoop + loadConfigIntoSystemPrompt + readMemory (no real LLM calls). vi.spyOn for any network IO. set 30s timeouts. The ws_server tests actually open real WebSocket connections on ephemeral ports — this is acceptable because the WS lifecycle is the contract under test; use `ws` client to drive the test.

    Avoid: any test that imports from src/renderer (Electron-coupled). Avoid: tests that depend on the Anthropic SDK. Avoid: tests that bind to port 7878 (port collision risk in CI; use port 0 + read actual port back). Avoid: testing the renderer UI (Wave 2 + 3 scope).
  </action>
  <acceptance_criteria>
    - All 4 test files exist on disk (3 NEW + 1 EXTENDED)
    - ws_server.test.ts has >= 8 passing cases (bind modes + port collision + default config + graceful close + rebind throw in Wave 1 + static path-traversal guard + 404 + GET / serves index.html)
    - ws_handlers.test.ts has >= 6 passing cases (sendMessage round-trip + cancel mid-stream + malformed JSON dropped + unknown type dropped + missing msgId dropped + content cap dropped + ws close aborts in-flight)
    - network_config.test.ts has >= 6 passing cases (missing file + round-trip + corrupt JSON + shape drift port + shape drift bindMode + concurrent saves serialize + persistQueue not poisoned)
    - preload.test.ts extended with >= 3 api.network cases (getConfig + setConfig + getReachInfo)
    - All new tests use createRequire(import.meta.url) for CJS imports OR pure TS imports where appropriate
    - Test files do NOT import from src/renderer
    - Total new passing tests: >= 23 (8 + 8 + 7 + 3 minimum)
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean — proves the new shared/types.ts additions do not break existing Phase 1-8 code)
  </acceptance_criteria>
  <verify>
    <automated>npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot tests/unit/ws_server.test.ts tests/unit/ws_handlers.test.ts tests/unit/network_config.test.ts tests/unit/preload.test.ts 2>&1 | tail -50</automated>
    <fails_when>non-zero exit, or "0 passed" or "failed" in summary, or fewer than 23 total passing tests across the 4 suites</fails_when>
  </verify>
  <done>
    All 4 test files committed. Vitest exits 0 with at least 23 passing tests across the 4 suites. WS server bind modes + port collision + path-traversal guard + 404 fallback + graceful close all proven without the renderer being involved. WS handler sendMessage round-trip + cancel + malformed-JSON drop + unknown-type drop + msgId validation + content cap + ws close abort propagation all proven. Network config atomic round-trip + defensive defaults + concurrent serialization all proven. Preload api.network surface extension proven. Pre-existing tests still pass. TS build clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to Main IPC | Renderer requests network config via NETWORK_GET_CONFIG + NETWORK_SET_CONFIG + NETWORK_GET_REACH_INFO; main proxies to daemon JSON-RPC; main never reads `<userData>/network.json` directly (daemon is the single writer). |
| Main to Daemon JSON-RPC | Main's spawn.ts proxies network/* requests. Daemon is the only writer of `<userData>/network.json`. |
| Phone Browser to Main WS | Phone connects via `ws://<host>:7878`; main's handlers.ts dispatches sendMessage to runAgenticLoop. The phone never imports `window.localbot` (no preload bridge). |
| LLM (Anthropic SDK) to Daemon | The LLM calls tools via tools/call; daemon enforces per-bot tool allowlist + URL allowlist (Phase 8) + vault glob (Phase 7) regardless of origin (phone or desktop). |
| WS to LAN | v1 ships WITHOUT WS auth (Pitfall 1). Tailscale ACL is the only access control. Documented in README threat model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-9-01 | Information Disclosure | src/main/network/handlers.ts | critical | mitigate | Localhost-only default bind (NET-03 invariant); per-msgId AbortController map; WS messages JSON-parsed with shape validation; content cap 4096 chars; unknown type + malformed JSON dropped silently (Pitfall 1 hardening) |
| T-9-02 | Tampering / EoP | src/main/network/static.ts | high | mitigate | Path-traversal guard via path.normalize + path.join containment check BEFORE fs.existsSync; tested in ws_server.test.ts case G |
| T-9-03 | Tampering | daemon/network/config.cjs | high | mitigate | Atomic tmp+rename + persistQueue serialization + shape validation BEFORE write; rejects {code:'invalid_network_config'} on shape drift; tested in network_config.test.ts cases D + E + F |
| T-9-04 | Information Disclosure | src/main/index.ts audit append | medium | mitigate | Audit row for 'network.bind' carries ONLY {event:'network.bind', host, port}; NEVER message content, bot persona, or token deltas (specless-probe-fallback: no plaintext in audit) |
| T-9-05 | Denial of Service | src/main/network/handlers.ts | medium | mitigate | Content cap 4096 chars; malformed JSON dropped silently (no infinite loop on bad frames); per-msgId AbortController map bounded by active WS connections |
| T-9-06 | Spoofing | daemon/main.cjs | low | mitigate | network/set_config validates port integer + bindMode enum + updateChannel enum BEFORE saveNetworkConfig; renderer cannot bypass |
| T-9-07 | Tampering | npm install ws | low | accept | ws marked [OK] in 09-RESEARCH.md §Package Legitimacy Audit; pure-JS, no native compile; MIT; 100M+ weekly downloads; no postinstall script |
| T-9-08 | Information Disclosure | src/main/network/server.ts | medium | accept | v1 ships WITHOUT WS auth; documented in README; Tailscale ACL is the recommended access control (Pitfall 1). v2 adds `networkAuthToken` field in network.json |
| T-9-SC | Tampering | npm/pip/cargo installs | high | mitigate | package-legitimacy gate + blocking human checkpoint for [ASSUMED]/[SUS] packages — N/A here, ws marked [OK] |

## Threat-to-Test Mapping

| Threat | Test coverage |
|--------|---------------|
| T-9-01 (localhost-only default + WS message validation) | ws_server.test.ts cases A + B + C + ws_handlers.test.ts cases C + D + E + F + G |
| T-9-02 (path-traversal guard) | ws_server.test.ts case G |
| T-9-03 (atomic config + shape validation + concurrent serialization) | network_config.test.ts cases B + D + E + F + G |
| T-9-05 (DoS hardening) | ws_handlers.test.ts cases C + F + G |
| T-9-06 (renderer cannot bypass shape validation) | network_config.test.ts cases D + E + ws_handlers.test.ts cases E + F |
</threat_model>

<verification>
Phase-level checks after Plan 1 execution:
- `npm ls ws 2>&1 | grep -E '^ws@'` exits 0 (dep installed)
- `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean)
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot tests/unit/ws_server.test.ts tests/unit/ws_handlers.test.ts tests/unit/network_config.test.ts tests/unit/preload.test.ts 2>&1 | tail -30` exits 0 with >= 23 new passing tests
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot 2>&1 | tail -30` exits 0 (full Vitest suite still green)
- `grep -c 'loadNetworkConfig\|saveNetworkConfig\|networkConfigPath\|defaultNetworkConfig' daemon/network/config.cjs daemon/main.cjs src/main/paths.ts 2>&1 | awk '{ sum += $1 } END { if (sum < 6) exit 1 }'` succeeds (config symbols wired)
- `grep -c 'startNetworkServer\|dispatchWsMessage\|servePhoneBundle\|NetworkServerHandle\|activeWsRuns' src/main/network/server.ts src/main/network/handlers.ts src/main/network/static.ts src/main/network/index.ts 2>&1 | awk '{ sum += $1 } END { if (sum < 8) exit 1 }'` succeeds (WS symbols wired)
- `git ls-files -- daemon/network/config.cjs src/main/network/server.ts src/main/network/handlers.ts src/main/network/static.ts src/main/network/index.ts src/main/ipc/network.ts tests/unit/ws_server.test.ts tests/unit/ws_handlers.test.ts tests/unit/network_config.test.ts` exits 0 (9 new files tracked)
</verification>

<success_criteria>
- Phase 9 success criterion #1 (Localbot listens on configurable port for HTTP + WS control) - PROVEN by ws_server.test.ts cases A + B + C (bind to 127.0.0.1:7878 default + LAN opt-in via config; rebind toggle deferred to Wave 2)
- Phase 9 success criterion #2 (Phone browser can chat via WS using same interface as desktop) - PROVEN by ws_handlers.test.ts case A (sendMessage round-trip reusing runAgenticLoop) + case B (cancel via WS mirrors chat.ts cancel semantics)
- Phase 9 success criterion #3 (Local-only binding by default; opt-in to LAN/Tailnet) - PARTIALLY PROVEN: localhost-only default in server.ts (T-9-01 mitigation); bindMode=='lan' config persistence + rebind toggle UX land in Wave 2
- WS server + handlers + static server + IPC bridge all wired and tested in isolation; phone UI Vite target + Tailscale detection + electron-builder installer all remain as Wave 2 + Wave 3 scope.
</success_criteria>

<output>
Create `.planning/phases/09-phone-reach-ship/09-01-SUMMARY.md` when done.
</output>
