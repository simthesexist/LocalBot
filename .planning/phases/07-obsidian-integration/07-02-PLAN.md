---
phase: 07-obsidian-integration
plan: 02
type: execute
wave: 2
depends_on: ["phase-07-obsidian-integration-07-01"]
files_modified:
  - daemon/tools/vault_search.cjs
  - daemon/tools/vault_list.cjs
  - daemon/tools/registry.cjs
  - daemon/vault/wikilink.cjs
  - daemon/vault/index.cjs
  - daemon/main.cjs
  - src/shared/types.ts
  - src/renderer/state/vault.ts
  - src/renderer/components/VaultReadBlock.tsx
  - src/renderer/components/VaultSearchBlock.tsx
  - src/renderer/components/VaultWriteBlock.tsx
  - src/renderer/components/MessageBlock.tsx
  - tests/unit/vault_search.test.ts
  - tests/unit/vault_wikilink.test.ts
autonomous: true
requirements:
  - OBS-02
  - OBS-06

estimate:
  tokens: 78000
  raw_tokens: 38000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Bot can search the vault by regex pattern (or literal query) via vault.search; ripgrep streams --json; each match is filtered through checkVaultAccess; result matches carry vault-relative path + line number + line text + truncated flag"
    - "Bot can list a vault directory via vault.list; entries sorted dirs-first then alphabetical; hidden dirs (.obsidian, .trash) skipped by default; glob pipeline applied to the listed dir's relative path"
    - "Wikilink parser handles all 4 Obsidian grammar variants [[Title]], [[Title|Alias]], [[Title#Section]], [[Title#Section|Alias]]; case-insensitive resolution via buildVaultIndex keyed on stem.toLowerCase()"
    - "Renderer renders inline blocks for vault_read (file header + bytes footer), vault_search (match list with collapse), and vault_write (path header + bytesWritten footer)"
    - "MessageBlock dispatch covers all 3 new variants; missing variants show fallback 'unsupported block' rather than crashing"
  artifacts:
    - "daemon/tools/vault_search.cjs (NEW) exporting call(args, ctx) - ripgrep --json stream + checkVaultAccess filter + max_results cap"
    - "daemon/tools/vault_list.cjs (NEW) exporting call(args, ctx) - readdir + sort + hidden skip"
    - "daemon/vault/wikilink.cjs (NEW) exporting parseWikilinks, buildVaultIndex, resolveWikilink"
    - "daemon/vault/index.cjs barrel re-exports parseWikilinks, buildVaultIndex, resolveWikilink"
    - "daemon/tools/registry.cjs TOOLS extended with 'vault.search' + 'vault.list'; SCHEMAS includes both"
    - "daemon/main.cjs tools/call ctx unchanged for these new tools (vaultRoot + globalDeny + vaultDeny + vaultAllow already threaded by Plan 07-01); new audit minimization entries for vault.search + vault.list"
    - "src/shared/types.ts MessageBlock union gains 'vault_search' variant {kind:'vault_search'; query:string; matches:Array<{path:string; line:number; text:string}>; truncated:boolean}"
    - "src/renderer/state/vault.ts (NEW) module-scope store for global config + EVENT_VAULT_CONFIG_UPDATED subscription; useVaultConfig() hook"
    - "src/renderer/components/VaultReadBlock.tsx (NEW) renders path header + content (pre-wrap, collapsible) + bytes footer + error tint"
    - "src/renderer/components/VaultSearchBlock.tsx (NEW) renders query header + truncated flag + match list (path + line + text); collapsible"
    - "src/renderer/components/VaultWriteBlock.tsx (NEW) renders path header + bytesWritten footer + content preview (optional)"
    - "src/renderer/components/MessageBlock.tsx (extended) switch dispatches 3 new cases to new components"
    - "tests/unit/vault_search.test.ts"
    - "tests/unit/vault_wikilink.test.ts"
  key_links:
    - "daemon/tools/vault_search.cjs -> spawn rgPath with --json --no-messages --no-config --regexp <query> [-g <glob>] <vaultRoot> --no-follow -> readline loop -> for each match evt compute path.relative(vaultRoot, abs).replace(/\\\\/g, '/') -> checkVaultAccess -> if allowed push {path, line, text} -> cap at max_results -> return {matches, truncated}"
    - "daemon/tools/vault_list.cjs -> safePath(vaultRoot, path) -> fs.readdir with withFileTypes -> map + sort dirs-first then alphabetical -> skip hidden unless includeHidden -> return {entries, path: relativePath}"
    - "daemon/vault/wikilink.cjs -> parseWikilinks(text) iterates WIKILINK_RE -> returns [{title, section, alias}]; buildVaultIndex(vaultRoot) walks vault tree skipping .obsidian/.trash/node_modules, indexes .md files by stem.toLowerCase() -> returns Map; resolveWikilink(index, name) does case-fold lookup"
    - "daemon/main.cjs tools/call audit minimization for vault.search: {query, glob, result_count, truncated}; for vault.list: {path: relativePath, entryCount}"
    - "src/renderer/state/vault.ts -> useEffect subscribes to window.localbot.on('vault:config:updated', refresh) + initial getConfig on mount -> module-scope state + subscribers Set -> useVaultConfig hook returns {config, refresh}"
    - "src/renderer/components/MessageBlock.tsx -> switch (block.kind) -> case 'vault_read': <VaultReadBlock /> -> case 'vault_search': <VaultSearchBlock /> -> case 'vault_write': <VaultWriteBlock /> -> default: null"
  prohibitions:
    - "vault.search MUST NOT include absolute vault paths in result matches or audit params"
    - "vault.search MUST pass --no-follow to ripgrep so symlinks outside vault are not followed (Pitfall Open Question #2)"
    - "vault.search MUST use ctx.registry.registerChild(toolCallId, child) for tools/cancel support (mirrors code_search)"
    - "vault.list MUST apply checkVaultAccess to the requested directory's relative path BEFORE returning entries (the entries themselves don't carry allow/deny individually)"
    - "Wikilink regex MUST be safe against ReDoS (uses [^\\]|]+ negated classes, no nested quantifiers)"
    - "vault.search max_results default MUST be 200 (mirrors code_search)"
    - "Renderer MUST NOT call api.vault.* on every keystroke; subscription is via EVENT_VAULT_CONFIG_UPDATED only (Pitfall 4)"
    - "Renderer block components MUST handle the isError / truncated flags distinctly (VaultReadBlock.error vs VaultSearchBlock.truncated)"
    - "Wikilink index MUST case-fold on stem (no .md extension) to handle Windows/macOS/Linux case-insensitive lookups (Pitfall 1)"

---

# Plan 07-02 artifact additions (full list lives in Plan 07-01's Artifacts section):
#
# New files (Plan 2):
#   daemon/tools/vault_search.cjs
#   daemon/tools/vault_list.cjs
#   daemon/vault/wikilink.cjs
#   src/renderer/state/vault.ts
#   src/renderer/components/VaultReadBlock.tsx
#   src/renderer/components/VaultSearchBlock.tsx
#   src/renderer/components/VaultWriteBlock.tsx
#   tests/unit/vault_search.test.ts
#   tests/unit/vault_wikilink.test.ts
#
# Modified files (Plan 2):
#   daemon/tools/registry.cjs (TOOLS + SCHEMAS additions for vault.search + vault.list)
#   daemon/vault/index.cjs (barrel adds wikilink exports)
#   daemon/main.cjs (audit minimization entries for vault.search + vault.list)
#   src/shared/types.ts (vault_search MessageBlock variant)
#   src/renderer/components/MessageBlock.tsx (switch dispatch for vault_read + vault_search + vault_write)

<objective>
Phase 7 expansion slice: complete the vault tool surface (vault.search +
vault.list), add the wikilink parser/index (OBS-02 follow-wikilinks), and
wire the renderer side so vault.read / vault.search / vault.write results
render as inline UI blocks in the chat. After this plan, the LLM can call
all 3 vault tools end-to-end (read + search + write), the renderer shows
the results inline with collapse/error tints, and the wikilink grammar is
parsed + indexed for future note-linking.

Purpose: complete OBS-02 (read + follow wikilinks) + OBS-06 (search) before
the renderer UI surface (Plan 3) adds the user-facing settings tabs.

Output: 3 new CJS modules + 1 barrel export addition + 1 new renderer state
module + 3 new UI components + 1 extended MessageBlock dispatcher + 2 new
unit test suites + TS build clean.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-obsidian-integration/07-RESEARCH.md
@.planning/phases/07-obsidian-integration/07-PATTERNS.md
@.planning/phases/07-obsidian-integration/07-VALIDATION.md
@.planning/phases/07-obsidian-integration/07-01-PLAN.md
@.planning/phases/07-obsidian-integration/07-01-SUMMARY.md (created during execution)
@.planning/phases/06-scheduler-notifications/06-03-PLAN.md

# Source files for direct reference
@daemon/main.cjs
@daemon/tools/code_search.cjs (template for vault_search.cjs)
@daemon/tools/list_dir.cjs (template for vault_list.cjs)
@daemon/tools/registry.cjs
@daemon/tools/vault_read.cjs (created in Plan 07-01 — ctx shape reference)
@daemon/tools/vault_write.cjs (created in Plan 07-01)
@daemon/vault/index.cjs (created in Plan 07-01 — barrel reference)
@daemon/vault/glob.cjs (created in Plan 07-01 — checkVaultAccess shape reference)
@src/shared/types.ts
@src/renderer/state/bots.ts (template for state/vault.ts ensureDaemonSubscription pattern)
@src/renderer/components/ToolResultBlock.tsx (template for VaultReadBlock / VaultWriteBlock collapse pattern)
@src/renderer/components/ToolUseBlock.tsx (template for VaultSearchBlock JSON-style rendering)
@src/renderer/components/MessageBlock.tsx (existing switch dispatcher)
@tests/unit/code_search.test.ts (template for vault_search.test.ts ripgrep spawn mocking)
</context>

<tasks>

<task type="auto">
  <name>Task 1: daemon/vault/wikilink.cjs + daemon/tools/vault_search.cjs + daemon/tools/vault_list.cjs + extend registry + extend barrel + extend main.cjs audit minimization</name>
  <files>
    - daemon/vault/wikilink.cjs
    - daemon/vault/index.cjs
    - daemon/tools/vault_search.cjs
    - daemon/tools/vault_list.cjs
    - daemon/tools/registry.cjs
    - daemon/main.cjs
  </files>
  <read_first>
    - daemon/tools/code_search.cjs (template for vault_search.cjs: spawn rgPath with --json --no-messages --no-config --regexp <pattern> --line-buffered --no-follow + readline + max_results cap + signal abort + registerChild)
    - daemon/tools/list_dir.cjs (template for vault_list.cjs: safePath + fs.readdir withFileTypes + sort dirs-first then alphabetical + ENOENT/ENOTDIR error mapping)
    - daemon/vault/glob.cjs (created in Plan 07-01 — checkVaultAccess shape; vault_search uses this on each match)
    - daemon/vault/index.cjs (created in Plan 07-01 — barrel to extend with wikilink exports)
    - daemon/tools/registry.cjs (TOOLS at L15-28 to extend; SCHEMAS at L37-168 to add vault.search + vault.list entries)
    - daemon/main.cjs (tools/call case audit append at L432-440; codeSearchAuditParams + execCommandAuditParams helpers — add vaultSearchAuditParams + vaultListAuditParams following same shape)
    - src/shared/types.ts (MessageBlock union at L11-28 to extend with vault_search case; declared in Plan 07-01 task list as deferred)
    - 07-RESEARCH.md §Pattern 4 (wikilink regex + index cache); §Pattern 5 (vault.search reusing @vscode/ripgrep); §Pattern 6 (wikilink grammar); §Pitfall 1 (case-fold lookup)
  </read_first>
  <action>
    Five coordinated changes:

    1) daemon/vault/wikilink.cjs (NEW, ~90 lines). CJS module exporting parseWikilinks, buildVaultIndex, resolveWikilink. Imports: fs from 'node:fs/promises', path from 'node:path'. Internal: const WIKILINK_RE = /\[\[([^\]|]+)(?:#([^\]|]+))?(?:\|([^\]]+))?\]\]/g (Pitfall 8 mitigation: no nested quantifiers; regex is ReDoS-safe). parseWikilinks(text): reset WIKILINK_RE.lastIndex = 0; iterate with WIKILINK_RE.exec(text); push {title: m[1].trim(), section: m[2] ? m[2].trim() : null, alias: m[3] ? m[3].trim() : null} for each match; return array. buildVaultIndex(vaultRoot): async walker — initialize `const idx = new Map()`; async walk(dir) — readdir withFileTypes; for each entry, if e.name.startsWith('.') AND e.name !== '.md' skip (skip .obsidian, .trash, but keep a file named .md if it exists for completeness); if e.isDirectory() AND e.name !== 'node_modules' AND not e.name.startsWith('.') recurse; if e.isFile() AND e.name.endsWith('.md') then `idx.set(e.name.replace(/\.md$/, '').toLowerCase(), path.join(dir, e.name))` (Pitfall 1 mitigation: index by stem.toLowerCase()); await walk(vaultRoot); return idx. resolveWikilink(index, name): return index.get(name.toLowerCase()) ?? null.

    2) daemon/vault/index.cjs (MODIFY): in the barrel module.exports, add 3 new exports — parseWikilinks: wikilink.parseWikilinks, buildVaultIndex: wikilink.buildVaultIndex, resolveWikilink: wikilink.resolveWikilink. Add `const wikilink = require('./wikilink.cjs');` at the top alongside existing config and wikilink. NOTE: rename the local `wikilink` import variable to avoid shadowing (or keep as wikilink — there is no conflict in the barrel because the keys are dotted symbols).

    3) daemon/tools/vault_search.cjs (NEW, ~250 lines). Mirror daemon/tools/code_search.cjs almost line-for-line. Imports: { rgPath } from '@vscode/ripgrep'; { spawn } from 'node:child_process'; readline from 'node:readline'; path from 'node:path'; { checkVaultAccess } from '../vault/index.cjs'. Constants: PATTERN_MAX_BYTES = 1024 * 1024; MAX_RESULTS_DEFAULT = 200; MAX_RESULTS_HARD = 1000. call(args, ctx):
       - Validate args.pattern: must be string, non-empty; > PATTERN_MAX_BYTES throws {code:'pattern_too_long'}. Reject NUL bytes (`\x00`) in pattern via regex `pattern.includes('\x00')` → throws {code:'invalid_pattern'}.
       - Validate args.glob (optional): string, NUL-byte check; > 1024 chars throws {code:'invalid_glob'}.
       - ctx.vaultRoot required (else {code:'vault_not_configured'}); ctx.registry (else skip registration).
       - Compute args.max_results: clamp to [1, MAX_RESULTS_HARD]; default MAX_RESULTS_DEFAULT.
       - Build args.argv: ['--json', '--no-messages', '--no-config', '--regexp', args.pattern, '--no-follow', '--line-buffered']; if args.glob, add '--glob', args.glob; final arg is ctx.vaultRoot (search root).
       - Spawn: const child = spawn(rgPath, argv, { stdio: ['ignore', 'pipe', 'ignore'], windowsHide: true, signal: ctx.signal }); if (ctx.registry && ctx.toolCallId) ctx.registry.registerChild(ctx.toolCallId, child); child.once('exit', () => { if (ctx.registry) ctx.registry.unregisterChild(ctx.toolCallId); });
       - Stream: const rl = readline.createInterface({ input: child.stdout }); const matches = []; let truncated = false; for await (const line of rl) { if (matches.length >= maxResults) { child.kill(); truncated = true; break; } try { const evt = JSON.parse(line); if (evt.type === 'match') { const data = evt.data; const absPath = data.path.text; const relPath = path.relative(ctx.vaultRoot, absPath).replace(/\\/g, '/'); const access = checkVaultAccess({globalDeny: ctx.globalDeny || [], vaultDeny: ctx.vaultDeny || [], vaultAllow: ctx.vaultAllow || [], relativePath: relPath}); if (!access.allowed) continue; matches.push({path: relPath, line: data.line_number, text: (data.lines && data.lines.text || '').trimEnd()}); } } catch { /* malformed JSON line — skip */ } }
       - Wait for child to exit (child-exit Promise); on non-zero exit code AND no matches AND not truncated, throw {code:'rg_failed', message: stderr or exit code}.
       - Return {matches, truncated, count: matches.length}.
       - Audit (called from main.cjs): vaultSearchAuditParams(args, successResult) = {query: args.pattern, glob: args.glob, result_count: successResult?.count ?? matches.length, truncated}.

    4) daemon/tools/vault_list.cjs (NEW, ~80 lines). Mirror daemon/tools/list_dir.cjs. Imports: fs from 'node:fs/promises'; { safePath } from './safe_path.cjs'; { checkVaultAccess } from '../vault/index.cjs'. call(args, ctx):
       - Extract args.path (default '.'); validate ctx.vaultRoot (else throw {code:'vault_not_configured'}).
       - resolved = await safePath(ctx.vaultRoot, args.path).
       - Compute relativePath = path.relative(ctx.vaultRoot, resolved).replace(/\\/g, '/'); run checkVaultAccess on relativePath; if !allowed throw {code:'glob_denied', reason, pattern}.
       - dirents = await fs.readdir(resolved, {withFileTypes: true}); map ENOENT→{code:'enoent'}, ENOTDIR→{code:'not_a_directory'}.
       - For each entry: if (!args.includeHidden && e.name.startsWith('.')) skip. Map to {name: e.name, type: e.isDirectory() ? 'dir' : e.isFile() ? 'file' : 'other', size: e.isFile() ? (e.size ?? null) : null}.
       - Sort: dirs-first then alphabetical (case-insensitive locale compare); same comparator as list_dir.
       - Return {path: relativePath, entries}.
       - Audit: vaultListAuditParams(args, successResult) = {path: successResult?.path, entryCount: successResult?.entries?.length}.

    5) daemon/tools/registry.cjs (MODIFY): TOOLS array (L15-28) gains 2 entries appended: 'vault.search' + 'vault.list' AFTER 'vault.write'. SCHEMAS (L37-168) gains 2 entries:
       - 'vault.search': {name:'vault.search', description:'ripgrep-based search across the vault. Returns matching lines with file path + line number + text. Subject to per-bot + global glob enforcement.', input_schema:{type:'object', properties:{pattern:{type:'string', description:'ripgrep regex pattern.'}, glob:{type:'string', description:'Optional ripgrep --glob filter (e.g. "*.md").'}, max_results:{type:'number', description:'Cap matches returned (default 200, max 1000).'}}, required:['pattern']}}.
       - 'vault.list': {name:'vault.list', description:'List entries in a vault directory. Sorted dirs-first then alphabetical. Subject to glob enforcement.', input_schema:{type:'object', properties:{path:{type:'string', description:'Directory path inside vault. Defaults to ".".'}, includeHidden:{type:'boolean', description:'Include dotfiles (default false).'}}, required:[]}}.
       No TOOL_FILE override needed (fileFor maps dotted→underscore, so vault.search → vault_search.cjs and vault.list → vault_list.cjs).

    6) daemon/main.cjs (MODIFY): two new audit minimization helpers, mirrors codeSearchAuditParams + execCommandAuditParams at L424-430:
       - vaultSearchAuditParams(args, successResult): {query: args.pattern, glob: args.glob ?? null, result_count: successResult?.count ?? 0, truncated: !!successResult?.truncated}.
       - vaultListAuditParams(args, successResult): {path: successResult?.path ?? args.path, entryCount: successResult?.entries?.length ?? 0}.
       In tools/call case audit append logic (~L423-430), extend the if/else if chain with two more branches AFTER the existing exec_command branch:
         else if (name === 'vault.search') auditParams = vaultSearchAuditParams(args, successResult);
         else if (name === 'vault.list') auditParams = vaultListAuditParams(args, successResult);
         else if (name === 'vault.read') auditParams = {path: successResult?.path ?? args.path, bytes: successResult?.bytes ?? 0};  // mirrors Plan 07-01 step 9(e)
         else if (name === 'vault.write') auditParams = {path: successResult?.path ?? args.path, bytesWritten: successResult?.bytesWritten ?? 0};
       Else branch (default): auditParams = args (existing behavior).

    Avoid: any changes to vault_read.cjs or vault_write.cjs (already in Plan 07-01). Avoid: exposing the in-memory vault index across tool calls (each tool resolves its own paths via fs). Avoid: a wikilink_resolve tool in this plan (it's listed as optional in RESEARCH.md and not required for OBS-01..06; defer). Avoid: ripgrep child process without --no-follow (Pitfall Open Question #2). Avoid: ripgrep without abort signal handling (loses tools/cancel support). Avoid: skipping readdir withFileTypes (loses dir-vs-file distinction).

    Validation step (REQUIRED before Task 2): after all 6 changes, run `node -e "require('./daemon/vault/index.cjs').buildVaultIndex; console.log('wikilink barrel ok');"` from D:/Claude/Grokbot. Must exit 0 with the line printed. Then run `node -e "const r = require('./daemon/tools/registry.cjs'); console.log(r.TOOLS.filter(n => n.startsWith('vault.')))"` — must print `['vault.read', 'vault.write', 'vault.search', 'vault.list']`.
  </action>
  <acceptance_criteria>
    - daemon/vault/wikilink.cjs exists; exports parseWikilinks, buildVaultIndex, resolveWikilink
    - daemon/vault/index.cjs barrel re-exports all 3 wikilink symbols alongside existing 5 (loadVaultConfig, saveVaultConfig, vaultConfigPath, checkVaultAccess, makeMatcher) = 8 total exports
    - daemon/tools/vault_search.cjs exists; exports call(args, ctx); spawns rgPath with --json --no-messages --no-config --regexp --no-follow --line-buffered; applies checkVaultAccess on each match; caps at max_results; throws pattern_too_long for > 1 MiB patterns
    - daemon/tools/vault_list.cjs exists; exports call(args, ctx); uses safePath + readdir withFileTypes + sort; applies checkVaultAccess on the listed dir's relative path; skips hidden by default
    - daemon/tools/registry.cjs TOOLS array contains 'vault.read', 'vault.write', 'vault.search', 'vault.list' (4 vault tools)
    - daemon/tools/registry.cjs SCHEMAS contains entries for all 4 vault tools with input_schema
    - daemon/main.cjs audit minimization includes vault.search (query+glob+result_count+truncated), vault.list (path+entryCount), vault.read (path+bytes), vault.write (path+bytesWritten) — verified by grep
    - `node -e "require('./daemon/vault/index.cjs').buildVaultIndex"` exits 0 (barrel resolves)
    - `node -e "console.log(require('./daemon/tools/registry.cjs').TOOLS.filter(n => n.startsWith('vault.')))"` prints the 4-element array
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && grep -l 'vault.search\|vault.list\|vaultSearchAuditParams\|vaultListAuditParams\|buildVaultIndex\|parseWikilinks\|--no-follow' daemon/tools/vault_search.cjs daemon/tools/vault_list.cjs daemon/tools/registry.cjs daemon/main.cjs daemon/vault/wikilink.cjs daemon/vault/index.cjs 2>&1</automated>
    <fails_when>grep returns empty for any of the 6 files</fails_when>
  </verify>
  <done>
    daemon/vault/wikilink.cjs created; barrel re-exports the 3 wikilink symbols. daemon/tools/vault_search.cjs created with ripgrep --no-follow streaming + checkVaultAccess filter + max_results cap + signal abort + registerChild. daemon/tools/vault_list.cjs created with safePath + readdir + sort + glob filter. registry TOOLS array extended to 4 vault entries; SCHEMAS extended with 2 new schemas. main.cjs audit minimization covers all 4 vault tools. All wired symbols present in grep output.
  </done>
</task>

<task type="auto">
  <name>Task 2: src/shared/types.ts vault_search variant + src/renderer/state/vault.ts + 3 Vault*Block components + extend MessageBlock dispatcher + 2 unit test suites (vault_search + vault_wikilink)</name>
  <files>
    - src/shared/types.ts
    - src/renderer/state/vault.ts
    - src/renderer/components/VaultReadBlock.tsx
    - src/renderer/components/VaultSearchBlock.tsx
    - src/renderer/components/VaultWriteBlock.tsx
    - src/renderer/components/MessageBlock.tsx
    - tests/unit/vault_search.test.ts
    - tests/unit/vault_wikilink.test.ts
  </files>
  <read_first>
    - src/shared/types.ts (MessageBlock union at L11-28 to extend with vault_search variant)
    - src/renderer/state/bots.ts (template for state/vault.ts ensureDaemonSubscription pattern + module-scope state + subscribers Set; lines 27-100)
    - src/renderer/components/ToolResultBlock.tsx (template for VaultReadBlock collapse pattern + isError tint + byte-count at L14-65)
    - src/renderer/components/ToolUseBlock.tsx (template for VaultSearchBlock JSON-style rendering at L14-65)
    - src/renderer/components/MessageBlock.tsx (switch dispatcher at L53-96)
    - tests/unit/code_search.test.ts (template for vault_search.test.ts rg spawn mocking pattern)
    - 07-RESEARCH.md §Pattern 4 (wikilink regex + index); §Pattern 6 (wikilink grammar)
  </read_first>
  <action>
    Seven coordinated changes:

    1) src/shared/types.ts (MODIFY): extend MessageBlock union (L11-28) with ONE new variant — `{kind: 'vault_search'; query: string; matches: Array<{path: string; line: number; text: string}>; truncated: boolean}`. Place AFTER the 'vault_write' variant that Plan 07-01 added. The 'vault_read' + 'vault_write' variants already exist from Plan 07-01.

    2) src/renderer/state/vault.ts (NEW, ~140 lines). Module-scope state + subscribers + ensureDaemonSubscription, mirrors src/renderer/state/bots.ts. Define `export interface VaultConfigState { config: VaultGlobalConfig | null; loading: boolean; error: string | null; }` (import VaultGlobalConfig from '../../shared/types'). Module-scope: `const state: VaultConfigState = { config: null, loading: false, error: null }; const subscribers = new Set<() => void>(); let subscribedToDaemon = false; let lastRefreshAt = 0; const REFRESH_MIN_INTERVAL_MS = 200;`. async function refresh(): read state; if Date.now() - lastRefreshAt < REFRESH_MIN_INTERVAL_MS return; lastRefreshAt = Date.now(); if !window.localbot) return; try { state.loading = true; state.error = null; const result = await window.localbot.vault.getConfig(); if (result?.ok) state.config = result.config ?? {rootPath:'', globalDeny:[]}; else state.error = result?.error ?? 'unknown error'; } catch (e) { state.error = (e as Error).message; } finally { state.loading = false; notify(); } function notify(): for (const s of subscribers) s(); function getState(): VaultConfigState { return state; } function subscribe(fn: () => void): () => void { subscribers.add(fn); ensureDaemonSubscription(); return () => subscribers.delete(fn); } function ensureDaemonSubscription(): if (subscribedToDaemon) return; subscribedToDaemon = true; if (!window.localbot) return; const off = window.localbot.on('vault:config:updated', () => { void refresh(); }); window.addEventListener('beforeunload', () => { off(); }, { once: true }); void refresh(); // initial fetch. Export: `export function useVaultConfig(): VaultConfigState { const [s, setS] = useState<VaultConfigState>(getState()); useEffect(() => { const off = subscribe(() => setS({...getState()})); return off; }, []); return s; }` plus `export const vaultActions = { refresh, setConfig: async (cfg: VaultGlobalConfig) => { const result = await window.localbot.vault.setConfig(cfg); if (result?.ok) await refresh(); return result; } };`.

    3) src/renderer/components/VaultReadBlock.tsx (NEW, ~90 lines). React component rendering a vault.read result. Props: `{block: Extract<MessageBlock, {kind:'vault_read'}>}`. Imports: React, useState. Mirrors ToolResultBlock pattern:
       - COLLAPSE_THRESHOLD_BYTES = 500 (matches ToolResultBlock).
       - utf8ByteLength helper (copy from ToolResultBlock).
       - sliceAtUtf8Bytes helper (copy from ToolResultBlock).
       - Component: destructure {path, content, bytes, startLine, endLine, truncated} from block; compute isSingleLine = !content.includes('\n'); compute shouldCollapse = !isSingleLine && utf8ByteLength(content) > COLLAPSE_THRESHOLD_BYTES; useState for expanded; visible = shouldCollapse && !expanded ? sliceAtUtf8Bytes(content, COLLAPSE_THRESHOLD_BYTES) + '\n\n[... truncated]' : content.
       - Render: <div className="block-vault-read"> <div className="block-header">vault.read: {path} {bytes ? `(${bytes} bytes)` : ''}</div> <pre className="block-content">{visible}</pre> {shouldCollapse && <button onClick={() => setExpanded(!expanded)}>{expanded ? 'Collapse' : `Show all ${bytes} bytes`}</button>} </div>.
       - Optional: support isError prop variant (if block has a sibling error block, render with error tint — for v1, just rely on the outer MessageBlock dispatch to pass the tool_result block alongside).

    4) src/renderer/components/VaultSearchBlock.tsx (NEW, ~110 lines). Props: `{block: Extract<MessageBlock, {kind:'vault_search'}>}`. Header: <div className="block-header">vault.search: {block.query} {block.truncated && <span className="truncated">(truncated)</span>}</div>. Body: render each match as <div className="match"> <span className="match-path">{m.path}:{m.line}</span> <pre className="match-text">{m.text}</pre> </div>. Collapse when matches.length > 20: show first 20 + "Show all N matches" button (useState expanded). Empty matches: render <div className="block-empty">No matches</div>.

    5) src/renderer/components/VaultWriteBlock.tsx (NEW, ~50 lines). Props: `{block: Extract<MessageBlock, {kind:'vault_write'}>}`. Header: <div className="block-header">vault.write: {block.path}</div>. Footer: <div className="block-footer">{block.bytesWritten} bytes written</div>. Optional content preview: if block has a `content` field (optional in type), show in <pre>; for v1, omit preview (renderer just sees the write summary).

    6) src/renderer/components/MessageBlock.tsx (MODIFY): in the switch statement (L53-96), BEFORE the `default` case, add three new cases:
       - case 'vault_read': return <VaultReadBlock block={block} />;
       - case 'vault_search': return <VaultSearchBlock block={block} />;
       - case 'vault_write': return <VaultWriteBlock block={block} />;
       Import the 3 components at the top of the file. Verify exhaustiveness check `const _exhaustive: never = block;` still compiles (the new variants must extend MessageBlock).

    7) tests/unit/vault_search.test.ts (NEW, >= 8 cases):
       - Mirror tests/unit/code_search.test.ts rg spawn mocking. Use child_process.spawn mock that returns a fake rg --json output stream (via a PassThrough or similar).
       - Case A: ripgrep finds matches; vaultRoot='/tmp/vault'; pattern='foo'; matches.push {path: 'Projects/foo.md', line: 5, text: 'foo bar'}; returns {matches, truncated:false, count:1}.
       - Case B: matches in globalDeny path are filtered out — pattern='secret'; vaultRoot has a Private/secret.md; globalDeny=['Private/**']; result.matches is empty.
       - Case C: matches in path NOT in vaultAllow are filtered out — pattern='foo'; vaultAllow=['Projects/**']; Daily/2026-09-19.md containing 'foo' is filtered; result.matches only has Projects/foo.md.
       - Case D: max_results cap — spawn returns 5 matches, max_results=2; child.kill() called; result.truncated=true; matches.length=2.
       - Case E: vault_not_configured when ctx.vaultRoot undefined.
       - Case F: pattern_too_long for > 1 MiB pattern.
       - Case G: invalid_pattern for NUL byte.
       - Case H: malformed JSON line in rg output is skipped (the loop catches the parse error and continues).

    8) tests/unit/vault_wikilink.test.ts (NEW, >= 6 cases):
       - Use createRequire + mkdtemp workspace.
       - Case A: parseWikilinks('See [[Note Name]] and [[Other Note|alias]] for details.') returns [{title:'Note Name', section:null, alias:null}, {title:'Other Note', section:null, alias:'alias'}].
       - Case B: parseWikilinks handles [[Note#Section]] and [[Note#Section|Alias]] — returns [{title:'Note', section:'Section', alias:null}] and [{title:'Note', section:'Section', alias:'Alias'}] respectively.
       - Case C: buildVaultIndex walks a mkdtemp vault tree containing Projects/foo.md, Projects/sub/bar.md, .obsidian/config.json, Daily/2026.md; returns Map with keys 'foo', 'bar', '2026' (NOT 'config' — dotfile skipped); lookup via resolveWikilink(index, 'FOO') returns the foo.md absolute path (case-fold match).
       - Case D: buildVaultIndex skips node_modules/ subdirectories.
       - Case E: resolveWikilink(index, 'nonexistent') returns null.
       - Case F: parseWikilinks returns [] for text without [[ ]] substrings.
       - Case G: parseWikilinks handles the same title twice (duplicates OK; last index wins for resolveWikilink by Map.set semantics).

    Test infrastructure: createRequire + mkdtemp + vi.fn() for ctx.registry mocks. For vault_search.test.ts, use a helper that returns a fake Readable stream writing JSON lines. Set 30s timeouts.

    Avoid: testing the Anthropic SDK or the Electron renderer (covered in Plan 07-03). Avoid: real ripgrep binary in unit tests (mock spawn). Avoid: importing src/main or src/renderer in vault_search.test.ts / vault_wikilink.test.ts (CJS modules only).
  </action>
  <acceptance_criteria>
    - src/shared/types.ts MessageBlock union contains vault_search variant (after vault_read + vault_write)
    - src/renderer/state/vault.ts exists; exports useVaultConfig hook + vaultActions object
    - src/renderer/state/vault.ts useVaultConfig returns {config, loading, error}; initial fetch happens via ensureDaemonSubscription
    - src/renderer/components/VaultReadBlock.tsx exists; renders path + content (collapsible > 500 bytes) + bytes footer
    - src/renderer/components/VaultSearchBlock.tsx exists; renders query header + matches list (collapsible > 20 matches) + truncated flag
    - src/renderer/components/VaultWriteBlock.tsx exists; renders path + bytesWritten footer
    - src/renderer/components/MessageBlock.tsx switch covers vault_read + vault_search + vault_write; exhaustiveness check compiles
    - tests/unit/vault_search.test.ts has >= 8 passing cases
    - tests/unit/vault_wikilink.test.ts has >= 6 passing cases
    - All test files use createRequire(import.meta.url) for CJS module imports
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean — proves the new MessageBlock variants + state + components all type-check)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot tests/unit/vault_search.test.ts tests/unit/vault_wikilink.test.ts 2>&1 | tail -50 && npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30</automated>
    <fails_when>non-zero vitest exit, "0 passed" or "failed" in summary, fewer than 14 total passing tests across the 2 suites, or tsc --noEmit reports any TS error</fails_when>
  </verify>
  <done>
    All 7 changes committed. Vitest exits 0 with at least 14 new passing tests across vault_search + vault_wikilink. src/shared/types.ts MessageBlock union extended with vault_search. src/renderer/state/vault.ts provides useVaultConfig hook with EVENT_VAULT_CONFIG_UPDATED subscription + initial fetch. 3 Vault*Block components render path/content/bytes/search-matches/write-summary inline. MessageBlock dispatcher covers all 3 new variants; exhaustiveness check compiles. TS build clean. Pre-existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon ripgrep child to vault filesystem | Spawns rg --no-follow so daemon safe_path is single source of truth; result matches pass through checkVaultAccess before renderer sees them. |
| Renderer state subscription | useVaultConfig subscribes via EVENT_VAULT_CONFIG_UPDATED; cannot be tricked into using stale config because REFRESH_MIN_INTERVAL_MS gates re-fetches. |
| Renderer block components | Receive only typed MessageBlock variants; cannot call vault.* IPC directly (no `window.localbot.vault.*` in component scope — actions go through vaultActions in state/vault.ts). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-10 | Information Disclosure | daemon/tools/vault_search.cjs | medium | mitigate | checkVaultAccess applied to every match before push; --no-follow prevents ripgrep from leaking matches from symlinked-out paths; tested in vault_search.test.ts case B/C |
| T-7-11 | DoS | daemon/tools/vault_search.cjs | medium | mitigate | max_results cap with hard ceiling 1000; child.kill() when cap hit; abort signal triggers child kill (matches code_search) |
| T-7-12 | DoS | daemon/vault/wikilink.cjs | low | mitigate | Regex uses negated classes [^\\]|]+, no nested quantifiers; ReDoS-safe; no per-call index build (lazy) |
| T-7-13 | Spoofing / EoP | renderer state/vault.ts | low | mitigate | Renderer never computes vaultRoot itself; only renders what the daemon resolves; refresh is throttled (REFRESH_MIN_INTERVAL_MS=200) to avoid IPC flood |
| T-7-14 | Tampering | daemon/vault/wikilink.cjs | low | mitigate | buildVaultIndex skips .obsidian, .trash, node_modules; case-fold lookup defeats case-sensitivity attacks |
| T-7-15 | Information Disclosure | src/renderer/components/Vault*Block.tsx | low | mitigate | Components render block.path / block.content from typed MessageBlock only; no prop drilling of raw ctx |

## Threat-to-Test Mapping

| Threat | Test coverage |
|--------|---------------|
| T-7-10 (search glob filter) | vault_search.test.ts cases B/C |
| T-7-11 (search cap) | vault_search.test.ts case D |
| T-7-14 (wikilink case-fold + skip) | vault_wikilink.test.ts cases C/D |
</threat_model>

<verification>
Phase-level checks after Plan 2 execution:
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot tests/unit/vault_search.test.ts tests/unit/vault_wikilink.test.ts 2>&1 | tail -30` exits 0 with >= 14 new passing tests
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot 2>&1 | tail -30` exits 0 (full Vitest suite green; Plan 07-01's 30+ tests + Plan 07-02's 14+ tests, all pre-existing tests unbroken)
- `npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30` exits 0 (TS build clean; proves MessageBlock dispatch + state + 3 components + barrel all align)
- `grep -c 'vault_search\|vault_list\|buildVaultIndex\|parseWikilinks\|vaultSearchAuditParams\|vaultListAuditParams\|VaultReadBlock\|VaultSearchBlock\|VaultWriteBlock' src/shared/types.ts src/renderer/components/MessageBlock.tsx src/renderer/state/vault.ts daemon/main.cjs daemon/vault/index.cjs daemon/tools/vault_search.cjs daemon/tools/vault_list.cjs daemon/vault/wikilink.cjs 2>&1 | awk '{ sum += $1 } END { if (sum < 20) exit 1 }'` succeeds (all symbols wired)
- `git ls-files -- daemon/tools/vault_search.cjs daemon/tools/vault_list.cjs daemon/vault/wikilink.cjs src/renderer/state/vault.ts src/renderer/components/VaultReadBlock.tsx src/renderer/components/VaultSearchBlock.tsx src/renderer/components/VaultWriteBlock.tsx tests/unit/vault_search.test.ts tests/unit/vault_wikilink.test.ts` exits 0 (8 new files tracked)
</verification>

<success_criteria>
- Phase 7 success criterion #2 (bot can read notes; can follow wikilinks within per-bot allow/deny) - PROVEN by Plan 07-01's vault_read suite + Plan 07-02's vault_wikilink suite (regex + case-fold index + hidden skip + node_modules skip).
- Phase 7 success criterion #5 (bot can search vault by query with context lines) - PROVEN by vault_search.test.ts cases A/B/C/D (happy path + global_deny filter + vaultAllow filter + max_results cap).
</success_criteria>

<output>
Create `.planning/phases/07-obsidian-integration/07-02-SUMMARY.md` when done.
</output>