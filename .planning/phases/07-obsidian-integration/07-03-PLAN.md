---
phase: 07-obsidian-integration
plan: 03
type: execute
wave: 3
depends_on: ["phase-07-obsidian-integration-07-02"]
files_modified:
  - src/renderer/components/BotSettingsPage.tsx
  - src/renderer/components/BotSettingsObsidianTab.tsx
  - src/renderer/components/VaultGlobalSettingsModal.tsx
  - src/renderer/components/App.tsx
  - src/renderer/styles/app.css
  - tests/playwright/fake-m3-server.ts
  - tests/playwright/obsidian-integration.test.ts
  - playwright.config.ts
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
  - OBS-05
  - OBS-06

estimate:
  tokens: 72000
  raw_tokens: 35000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "User can edit vault path + per-bot allow/deny glob lists from BotSettingsPage's new 5th 'Obsidian' tab; changes debounce-save (250ms) via existing bot.update IPC"
    - "User can configure the global vault root path + global deny globs from a VaultGlobalSettingsModal reached from a top-bar Vault button"
    - "Playwright E2E: fake-m3-server streams a vault.read tool_use; renderer block renders path + content + bytes; audit row carries vault-relative path only (no absolute)"
    - "Playwright E2E: fake-m3-server streams a vault.write tool_use inside Agents/<bot>/; file appears on disk under the bot's agents folder; write outside Agents/<bot>/ is refused with code:'write_outside_agents'"
    - "Playwright E2E: fake-m3-server streams a vault.search tool_use; renderer block renders match list; audit row carries {query, glob, result_count, truncated}"
  artifacts:
    - "src/renderer/components/BotSettingsObsidianTab.tsx (NEW) - vaultPath input + vaultAllow/vaultDeny textareas + 4 vault-tool allowlist checkboxes"
    - "src/renderer/components/VaultGlobalSettingsModal.tsx (NEW) - rootPath input + globalDeny textarea (newline-delimited) + Save/Cancel; uses AppModal primitive"
    - "src/renderer/components/BotSettingsPage.tsx (MODIFIED) - TabId union extends with 'obsidian'; TAB_IDS + TAB_LABELS extended; URL hash sync to obsidian; render <BotSettingsObsidianTab />"
    - "src/renderer/components/App.tsx (MODIFIED) - top-bar 'Vault' button that opens VaultGlobalSettingsModal; modal mounted near other modals"
    - "src/renderer/styles/app.css (MODIFIED) - .block-vault-read, .block-vault-search, .block-vault-write, .settings-tab-obsidian, .vault-global-modal styles"
    - "tests/playwright/fake-m3-server.ts (MODIFIED) - 3 new helpers: streamVaultReadToolUse, streamVaultWriteToolUse, streamVaultSearchToolUse"
    - "tests/playwright/obsidian-integration.test.ts (NEW) - 4-case E2E: vault.read happy + vault.write Agents/<bot>/ + vault.write outside Agents + vault.search"
    - "playwright.config.ts (MODIFIED) - extends daemon-smoke project with obsidian-integration test"
  key_links:
    - "src/renderer/components/BotSettingsObsidianTab.tsx -> useEffect debounce (250ms) -> bots/update patch {vaultPath, vaultAllow: <parsed-from-textarea>, vaultDeny: <parsed-from-textarea>} -> api.bot.update -> daemon writeConfigPatch -> ALLOWED_CONFIG_KEYS accepts new fields"
    - "src/renderer/components/VaultGlobalSettingsModal.tsx -> vaultActions.setConfig({rootPath, globalDeny: <parsed-from-textarea>}) -> api.vault.setConfig -> daemon vault/set_config -> currentVaultConfig reload -> EVENT_VAULT_CONFIG_UPDATED broadcast"
    - "src/renderer/components/App.tsx -> top-bar Vault button onClick -> setShowVaultModal(true) -> <VaultGlobalSettingsModal onClose=... />"
    - "src/renderer/components/MessageBlock.tsx switch case 'vault_read' -> <VaultReadBlock block={block} /> (Plan 07-02 wiring)"
    - "tests/playwright/fake-m3-server.ts streamVaultReadToolUse -> SSE chunks with tool_use block name='vault.read' input={path:'Projects/foo.md'} -> renderer MessageBlock dispatch renders block"
    - "tests/playwright/obsidian-integration.test.ts -> spawn daemon with LOCALBOT_USER_DATA_DIR=<tmp> -> create <tmp>/vault/Projects/foo.md + <tmp>/vault/Agents/alpha/ -> send initialize -> stream tool_use -> assert audit row + UI block"
  prohibitions:
    - "MUST NOT hardcode vault paths in tests (use mkdtemp + LOCALBOT_USER_DATA_DIR)"
    - "MUST NOT skip audit minimization assertion (verify vault-relative path only; never absolute)"
    - "BotSettingsObsidianTab MUST debounce saves (250ms) to match existing tab behavior; immediate save could race concurrent edits"
    - "VaultGlobalSettingsModal MUST validate rootPath is non-empty before save; reject non-array globalDeny entries on the renderer side too"
    - "fake-m3-server.ts streamVault*ToolUse helpers MUST follow the same SSE envelope shape as the existing streamToolUseResponse (no custom protocol)"
    - "Playwright test MUST NOT depend on real Obsidian vault path (env-based mkdtemp)"
    - "playwright.config.ts MUST add the new test to the daemon-smoke project only (not the headed project which is gated by LOCALBOT_SMOKE_OK)"
    - "App.tsx Vault button MUST be visible without selecting a bot (top-level menu item, not per-bot)"

---

# Plan 07-03 artifact additions (full list lives in Plan 07-01's Artifacts section):
#
# New files (Plan 3):
#   src/renderer/components/BotSettingsObsidianTab.tsx
#   src/renderer/components/VaultGlobalSettingsModal.tsx
#   tests/playwright/obsidian-integration.test.ts
#
# Modified files (Plan 3):
#   src/renderer/components/BotSettingsPage.tsx (5th tab 'obsidian')
#   src/renderer/components/App.tsx (top-bar Vault button + modal mount)
#   src/renderer/styles/app.css (vault settings + modal styles)
#   tests/playwright/fake-m3-server.ts (streamVaultReadToolUse + streamVaultWriteToolUse + streamVaultSearchToolUse)
#   playwright.config.ts (extend daemon-smoke project)

<objective>
Phase 7 UI + E2E slice: render the BotSettingsPage Obsidian tab (vaultPath +
per-bot allow/deny + tool allowlist checkboxes) and the VaultGlobalSettingsModal
(top-bar button), then prove the full vertical slice end-to-end via a new
Playwright suite that spawns the daemon with a temp userData, creates a fake
vault tree, streams vault.read + vault.write + vault.search tool_use blocks
via the fake-m3-server, and asserts the audit row minimization + UI block
rendering.

Purpose: close the user-facing surface so the renderer can configure vault
settings and the LLM can actually use the vault tools in a real Electron run.

Output: 2 new React components + 1 extended BotSettingsPage + 1 extended App
top-bar + 3 new fake-m3-server helpers + 1 new Playwright E2E suite + 1
extended playwright.config + styles.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-obsidian-integration/07-RESEARCH.md
@.planning/phases/07-obsidian-integration/07-PATTERNS.md
@.planning/phases/07-obsidian-integration/07-VALIDATION.md
@.planning/phases/07-obsidian-integration/07-01-PLAN.md
@.planning/phases/07-obsidian-integration/07-01-SUMMARY.md (created during execution)
@.planning/phases/07-obsidian-integration/07-02-PLAN.md
@.planning/phases/06-scheduler-notifications/06-03-PLAN.md

# Source files for direct reference
@src/renderer/components/BotSettingsPage.tsx (existing 4-tab pattern + debounced save + URL hash sync)
@src/renderer/components/App.tsx (top-bar layout + modal mount pattern)
@src/renderer/components/AppModal.tsx (primitive for VaultGlobalSettingsModal)
@src/renderer/components/NewBotModal.tsx (modal pattern reference)
@src/renderer/components/MessageBlock.tsx (extended by Plan 07-02 to dispatch vault_read/vault_search/vault_write)
@src/renderer/state/vault.ts (created in Plan 07-02; useVaultConfig hook + vaultActions)
@src/renderer/state/bots.ts (bot update pattern; trigger/cancel; updateBot)
@src/shared/types.ts (BotConfig.vaultPath + vaultAllow + vaultDeny from Plan 07-01)
@src/main/preload/index.ts (api.vault.{getConfig, setConfig} from Plan 07-01)
@tests/playwright/fake-m3-server.ts (existing streamToolUseResponse at L165-234)
@tests/playwright/daemon-tools.test.ts (template for obsidian-integration.test.ts spawn pattern)
@tests/playwright/scheduler-notification.test.ts (template for 4-case E2E pattern from Plan 06-03)
@playwright.config.ts (daemon-smoke project config)
@src/renderer/styles/app.css (existing block styles for tool_result)
</context>

<tasks>

<task type="auto">
  <name>Task 1: BotSettingsObsidianTab + VaultGlobalSettingsModal + extend BotSettingsPage 5th tab + extend App.tsx top-bar Vault button + styles</name>
  <files>
    - src/renderer/components/BotSettingsObsidianTab.tsx
    - src/renderer/components/VaultGlobalSettingsModal.tsx
    - src/renderer/components/BotSettingsPage.tsx
    - src/renderer/components/App.tsx
    - src/renderer/styles/app.css
  </files>
  <read_first>
    - src/renderer/components/BotSettingsPage.tsx (TabId union at L20-27; TAB_IDS + TAB_LABELS at L23-26; debounce save pattern at L195-200 with scheduleGeneralSave + 250ms setTimeout; persistPatch shape; parseTabFromHash + useEffect URL sync)
    - src/renderer/components/App.tsx (top-bar layout; existing modal mounts for NewBotModal + DeleteConfirmModal + ApprovalModalStack; state hooks for showModal flags)
    - src/renderer/components/AppModal.tsx (Escape + click-outside + focus trap primitive)
    - src/renderer/components/NewBotModal.tsx (text input + form submit pattern reference)
    - src/renderer/state/vault.ts (created in Plan 07-02 — useVaultConfig hook + vaultActions.setConfig; module-scope)
    - src/renderer/state/bots.ts (useBotList + updateBot pattern from Phase 4)
    - src/shared/types.ts (BotConfig.vaultPath + vaultAllow + vaultDeny from Plan 07-01)
    - src/renderer/styles/app.css (existing .block-tool-result + .block-result-error + .settings-tab classes)
  </read_first>
  <action>
    Five coordinated changes:

    1) src/renderer/components/BotSettingsObsidianTab.tsx (NEW, ~200 lines). React component for the Obsidian tab content. Props: `{bot: BotConfig; onPatch: (patch: Partial<BotConfig>) => void; saving: boolean; error: string | null}`. Internals:
       - useState for vaultPath (initialized from bot.vaultPath ?? ''), vaultAllowText (joined from bot.vaultAllow with \n), vaultDenyText (joined from bot.vaultDeny with \n).
       - useState for toolCheckboxes: { 'vault.read': true/false, 'vault.write': true/false, 'vault.search': true/false, 'vault.list': true/false } — initialized from bot.allowlist membership (defaults: all 4 vault tools opt-in if not present).
       - Debounce ref + scheduleSave() (mirror L195-200 pattern): parse vaultAllowText into string[] (split \n, trim, filter empty); same for vaultDenyText; on patch: onPatch({vaultPath: vaultPath || null, vaultAllow: parsedAllow, vaultDeny: parsedDeny, allowlist: <existing allowlist + new vault tools>}).
       - Render: <div className="settings-tab settings-tab-obsidian"> with three sections:
         (a) <section><h3>Vault path</h3><input type="text" placeholder="Leave blank to use global vault path" value={vaultPath} onChange={...} /></section>
         (b) <section><h3>Per-bot allow globs</h3><textarea placeholder="Projects/**&#10;Daily/**&#10;**/*.md" value={vaultAllowText} onChange={...} /></section>
         (c) <section><h3>Per-bot deny globs</h3><textarea placeholder="Personal/**" value={vaultDenyText} onChange={...} /></section>
         (d) <section><h3>Vault tools</h3>4 checkboxes for vault.read + vault.write + vault.search + vault.list</section>
         (e) <div className="save-status">{saving ? 'Saving...' : error ? `Error: ${error}` : 'Saved'}</div>

    2) src/renderer/components/VaultGlobalSettingsModal.tsx (NEW, ~140 lines). Modal component for global vault config. Props: `{open: boolean; onClose: () => void}`. Internals:
       - const { config, loading, error, refresh } = useVaultConfig(); const vaultActions = useVaultActions(); (or import vaultActions directly).
       - useState for rootPath (initialized from config?.rootPath ?? ''), globalDenyText (joined from config?.globalDeny with \n), saving, saveError.
       - useEffect: when config loads, populate rootPath + globalDenyText.
       - onSave: parse globalDenyText into string[] (split \n, trim, filter empty); if rootPath is empty after trim, setSaveError('rootPath is required'); else call await vaultActions.setConfig({rootPath: rootPath.trim(), globalDeny: parsed}); on success onClose(); on error setSaveError.
       - Render: <AppModal title="Obsidian vault settings" onClose={onClose}> <div className="vault-global-modal"> <section><h3>Global vault root path</h3><input type="text" placeholder="C:/Users/you/Documents/Vault" value={rootPath} onChange={...} /> <small>The Obsidian vault root. Per-bot paths override this.</small></section> <section><h3>Global deny globs</h3><textarea placeholder="Private/**&#10;Credentials/**" value={globalDenyText} onChange={...} /></section> <div className="modal-actions"><button onClick={onClose}>Cancel</button><button onClick={onSave} disabled={saving || loading}>{saving ? 'Saving...' : 'Save'}</button></div> {error && <div className="error">{error}</div>} {saveError && <div className="error">{saveError}</div>} </div> </AppModal>.

    3) src/renderer/components/BotSettingsPage.tsx (MODIFY): four changes.
       (a) Extend TabId union (L20) with `'obsidian'`. Add 'obsidian' to TAB_IDS. Add `obsidian: 'Obsidian'` to TAB_LABELS.
       (b) parseTabFromHash must recognize 'obsidian' as a valid tab.
       (c) In the JSX render where the 4 existing tabs render (general/permissions/schedule/history), add a fifth <button> + conditional render <BotSettingsObsidianTab .../> when activeTab === 'obsidian'. Pass bot + onPatch (existing persistPatch) + saving + error.
       (d) The vaultPath + vaultAllow + vaultDeny state is now handled inside BotSettingsObsidianTab; remove any inline vault field handling from BotSettingsPage itself (or leave a placeholder for back-compat).

    4) src/renderer/components/App.tsx (MODIFY): add a top-bar button + modal mount.
       (a) useState `const [showVaultModal, setShowVaultModal] = useState(false);`
       (b) In the top-bar JSX (near other buttons like settings/logout), add <button className="topbar-vault-button" onClick={() => setShowVaultModal(true)}>Vault</button>.
       (c) In the modal mount block (where NewBotModal + DeleteConfirmModal + ApprovalModalStack live), add `{showVaultModal && <VaultGlobalSettingsModal open={showVaultModal} onClose={() => setShowVaultModal(false)} />}`.
       (d) Import VaultGlobalSettingsModal at top.

    5) src/renderer/styles/app.css (MODIFY): add styles for the new components. Append at end of file:
       - .block-vault-read { /* mirror .block-tool-result + monospace pre */ }
       - .block-vault-read.block-result-error { color: #d33; }
       - .block-vault-search .match { padding: 4px 0; border-bottom: 1px solid #eee; }
       - .block-vault-search .match-path { font-family: monospace; font-size: 12px; color: #666; }
       - .block-vault-search .match-text { font-family: monospace; white-space: pre-wrap; }
       - .block-vault-write { /* similar to vault.read */ }
       - .settings-tab-obsidian section { margin-bottom: 16px; }
       - .settings-tab-obsidian input, .settings-tab-obsidian textarea { width: 100%; padding: 6px; font-family: monospace; }
       - .settings-tab-obsidian textarea { min-height: 80px; resize: vertical; }
       - .vault-global-modal section { margin-bottom: 16px; }
       - .vault-global-modal .modal-actions { display: flex; gap: 8px; justify-content: flex-end; margin-top: 16px; }
       - .topbar-vault-button { /* standard top-bar button styling */ }

    Avoid: hardcoding the vault path in components (always read from useVaultConfig). Avoid: opening the modal automatically on app start (user must click). Avoid: per-bot vaultPath being sent as empty string (use null to clear, per Pitfall 8). Avoid: missing aria-labels on the new inputs (use <label htmlFor> or aria-label).

    Validation step (REQUIRED before Task 2): after all 5 changes, run `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` from D:/Claude/Grokbot. Must exit 0 (TS build clean; new components + App.tsx wiring all type-check).
  </action>
  <acceptance_criteria>
    - src/renderer/components/BotSettingsObsidianTab.tsx exists; renders vaultPath input + vaultAllow/vaultDeny textareas + 4 vault-tool checkboxes; debounces saves via onPatch callback
    - src/renderer/components/VaultGlobalSettingsModal.tsx exists; wraps AppModal primitive; renders rootPath input + globalDeny textarea + Save/Cancel; uses vaultActions.setConfig
    - src/renderer/components/BotSettingsPage.tsx TabId union includes 'obsidian'; TAB_IDS includes 'obsidian'; URL hash sync handles 'obsidian'; renders BotSettingsObsidianTab when activeTab === 'obsidian'
    - src/renderer/components/App.tsx adds topbar-vault-button + mounts VaultGlobalSettingsModal when showVaultModal is true
    - src/renderer/styles/app.css has styles for .block-vault-read, .block-vault-search, .block-vault-write, .settings-tab-obsidian, .vault-global-modal, .topbar-vault-button
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30 && grep -l 'settings-tab-obsidian\|vault-global-modal\|topbar-vault-button\|BotSettingsObsidianTab\|VaultGlobalSettingsModal' src/renderer/components/BotSettingsPage.tsx src/renderer/components/App.tsx src/renderer/components/BotSettingsObsidianTab.tsx src/renderer/components/VaultGlobalSettingsModal.tsx src/renderer/styles/app.css 2>&1</automated>
    <fails_when>tsc --noEmit reports any TS error, or grep returns empty for any of the 5 files</fails_when>
  </verify>
  <done>
    All 5 UI changes committed. TS build clean. BotSettingsObsidianTab renders 5 sections (vaultPath + vaultAllow + vaultDeny + tool checkboxes + save status). VaultGlobalSettingsModal wraps AppModal primitive with rootPath + globalDeny + Save/Cancel. BotSettingsPage extends TabId union with 'obsidian' and routes to BotSettingsObsidianTab. App.tsx adds top-bar Vault button + mounts VaultGlobalSettingsModal. app.css has styles for all new components. Pre-existing tests + Plan 07-01/07-02 tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: fake-m3-server streamVault*ToolUse helpers + Playwright obsidian-integration.test.ts (4 cases) + extend playwright.config.ts</name>
  <files>
    - tests/playwright/fake-m3-server.ts
    - tests/playwright/obsidian-integration.test.ts
    - playwright.config.ts
  </files>
  <read_first>
    - tests/playwright/fake-m3-server.ts (existing streamToolUseResponse at L165-234; SSE envelope pattern with message_start + content_block_start + input_json_delta + content_block_stop + message_delta + message_stop; writeSse helper)
    - tests/playwright/daemon-tools.test.ts (template: spawn daemon with LOCALBOT_USER_DATA_DIR; NDJSON framing; tools/call JSON-RPC; audit JSONL read; UI block assertion)
    - tests/playwright/scheduler-notification.test.ts (template: 4-case E2E pattern from Phase 6 Plan 03)
    - playwright.config.ts (daemon-smoke project config + testDir pattern; add obsidian-integration.test.ts to the project's testMatch)
    - 07-RESEARCH.md §Validation Architecture (test framework + sample commands)
  </read_first>
  <action>
    Three coordinated changes:

    1) tests/playwright/fake-m3-server.ts (MODIFY): add 3 new helper functions that mirror streamToolUseResponse. Each helper takes (res, opts) where opts includes toolUseId (optional; default toolu_fake_<timestamp>), input (the tool input object), and any tool-specific extras. The helpers build the SSE chunk array in the same shape as streamToolUseResponse but with hardcoded tool name. Export them at the bottom of the file. ~80 lines total.
       - streamVaultReadToolUse(res, opts: { toolUseId?: string; input: { path: string; startLine?: number; endLine?: number } }): builds chunks for tool_use block name='vault.read' input=opts.input; emits tool_result on the next turn that returns {path, content, bytes}. Stream returns when SSE complete.
       - streamVaultWriteToolUse(res, opts: { toolUseId?: string; input: { path: string; content: string } }): tool_use block name='vault.write' input=opts.input; tool_result on next turn returns {path, bytesWritten}.
       - streamVaultSearchToolUse(res, opts: { toolUseId?: string; input: { pattern: string; glob?: string; max_results?: number; matches: Array<{path:string;line:number;text:string}>; truncated?: boolean } }): tool_use block name='vault.search' input={pattern, glob, max_results}; tool_result returns {matches, truncated, count}.
       Follow the exact SSE envelope shape from streamToolUseResponse (message_start → content_block_start with type:'tool_use' → input_json_delta with partial JSON → content_block_stop → message_delta → message_stop). Include tool_result in the NEXT turn (so the renderer sees tool_use first, then tool_result; that's how the LLM response is shaped).

    2) tests/playwright/obsidian-integration.test.ts (NEW, ~280 lines). Playwright test suite that exercises the full vertical slice end-to-end. 4 test cases.
       - Setup (beforeAll): create mkdtemp userDataDir + mkdtemp vaultRoot; mkdir <vaultRoot>/Projects/foo.md containing 'hello world'; mkdir <vaultRoot>/Agents/alpha/ (empty dir for now); mkdir <vaultRoot>/Private/secret.md containing 'classified'; set env LOCALBOT_USER_DATA_DIR=<userDataDir>; spawn daemon subprocess via existing helper from tests/playwright/daemon-tools.test.ts.
       - Case 1: vault.read happy path:
         * Pre-populate global vault config via direct fs.writeFileSync(<userDataDir>/vault.json, JSON.stringify({rootPath: vaultRoot, globalDeny: ['Private/**']}))
         * bots/create alpha with allowlist: ['vault.read']; bots/update alpha with vaultAllow: ['Projects/**']
         * sendMessage('Read Projects/foo.md'); fake-m3-server streams streamVaultReadToolUse with input.path='Projects/foo.md'; on tool_result the renderer MessageBlock dispatch renders VaultReadBlock with content 'hello world'
         * Assert: <div class="block-vault-read"> contains text 'hello world' + path 'Projects/foo.md'; audit JSONL line for vault.read carries {tool:'vault.read', params:{path:'Projects/foo.md', bytes:<n>}}; no absolute path in audit.
       - Case 2: vault.write inside Agents/<bot>/:
         * Same setup; bots/update alpha allowlist: ['vault.write']
         * sendMessage('Write a note to Agents/alpha/note.md with content "test"'); fake-m3-server streams streamVaultWriteToolUse with input.path='Agents/alpha/note.md', input.content='test'
         * Assert: <vaultRoot>/Agents/alpha/note.md exists with content 'test'; audit row carries {tool:'vault.write', params:{path:'Agents/alpha/note.md', bytesWritten:4}}.
       - Case 3: vault.write outside Agents/<bot>/ refused:
         * bots/update alpha with vaultAllow: ['**/*'] (allow everywhere) but allowlist: ['vault.write']
         * sendMessage('Write to Projects/foo.md'); fake-m3-server streams streamVaultWriteToolUse with input.path='Projects/foo.md'
         * Assert: error block rendered with code 'write_outside_agents'; <vaultRoot>/Projects/foo.md unchanged (not overwritten); audit row carries outcome:'error'.
       - Case 4: vault.search:
         * Pre-populate vault config (no globalDeny); bots/update alpha allowlist: ['vault.search'], vaultAllow: ['Projects/**']
         * sendMessage('Search for hello'); fake-m3-server streams streamVaultSearchToolUse with input.pattern='hello', matches=[{path:'Projects/foo.md', line:1, text:'hello world'}]
         * Assert: <div class="block-vault-search"> contains 'hello world' + path 'Projects/foo.md:1'; audit row carries {tool:'vault.search', params:{query:'hello', glob:null, result_count:1, truncated:false}}.
       - Teardown (afterAll): kill daemon; rmSync(userDataDir + vaultRoot, {recursive: true, force: true}).
       - Mirror the NDJSON framing pattern from tests/playwright/daemon-tools.test.ts; use the daemon spawn helper from there.

    3) playwright.config.ts (MODIFY): in the daemon-smoke project's testMatch array, add 'obsidian-integration.test.ts'. Verify the project's testDir points to tests/playwright. Do NOT add to the headed project (LOCALBOT_SMOKE_OK-gated) — these are pure daemon smokes.

    Avoid: any headed-Electron assertion (LOCALBOT_SMOKE_OK-gated; gate this strictly to daemon-smoke project). Avoid: depending on a real Obsidian vault (always use mkdtemp). Avoid: putting absolute paths in audit assertions (read the JSONL, parse the params.path, assert it does NOT contain the vaultRoot absolute path). Avoid: skipping the audit minimization check — every test case MUST read <userData>/audit.jsonl and verify the params shape.

    Validation step (REQUIRED): after all 3 changes, run `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts obsidian-integration.test.ts 2>&1 | tail -30` from D:/Claude/Grokbot. Exit 0 + all 4 cases passing is the gate. Then run `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` to confirm no TS regression.
  </action>
  <acceptance_criteria>
    - tests/playwright/fake-m3-server.ts exports streamVaultReadToolUse + streamVaultWriteToolUse + streamVaultSearchToolUse helpers
    - All 3 helpers follow the same SSE envelope shape as the existing streamToolUseResponse
    - tests/playwright/obsidian-integration.test.ts has 4 passing cases (vault.read happy + vault.write inside Agents + vault.write outside Agents refused + vault.search)
    - Every case asserts audit row minimization (params carries vault-relative path only; never absolute path; never rootPath)
    - playwright.config.ts daemon-smoke project includes obsidian-integration.test.ts in testMatch
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean)
    - `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts obsidian-integration.test.ts 2>&1 | tail -30` exits 0 with 4 passing cases
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts obsidian-integration.test.ts 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or "0 passed" or "failed" in summary, or fewer than 4 passing cases</fails_when>
  </verify>
  <done>
    All 3 E2E changes committed. fake-m3-server exposes 3 new vault tool_use streaming helpers matching the existing SSE envelope. obsidian-integration.test.ts has 4 passing cases covering vault.read happy + vault.write inside Agents + vault.write outside Agents refused + vault.search; every case asserts audit minimization. playwright.config.ts daemon-smoke project includes the new test. TS build clean. Full Phase 7 vertical slice proven end-to-end (config + tools + IPC + UI + E2E).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to Main IPC (vault config) | VaultGlobalSettingsModal sends via api.vault.setConfig; main proxies to daemon JSON-RPC vault/set_config; daemon validates and persists atomically. |
| Renderer to Main IPC (per-bot vault fields) | BotSettingsObsidianTab sends via bots/update; main proxies; daemon validateConfig + writeConfigPatch enforce ALLOWED_CONFIG_KEYS. |
| Renderer Vault modal to user | Modal opens only on user click (no auto-open); Cancel button works; AppModal Escape closes; modal traps focus. |
| Playwright test to real daemon | Test uses mkdtemp userData + mkdtemp vaultRoot + LOCALBOT_USER_DATA_DIR; no real user vault at risk. |
| Playwright E2E to audit log | Test reads <userData>/audit.jsonl after each call; verifies vault-relative path only; never absolute. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-16 | Spoofing | src/renderer/components/BotSettingsObsidianTab.tsx | low | mitigate | Renderer parses newline-delimited glob text into string[] before sending; daemon validateConfig enforces array-of-strings; UI shows parse errors as red border |
| T-7-17 | Tampering | src/renderer/components/VaultGlobalSettingsModal.tsx | low | mitigate | Renderer validates rootPath is non-empty before sending; daemon saveVaultConfig enforces non-string rootPath rejection |
| T-7-18 | Information Disclosure | tests/playwright/obsidian-integration.test.ts | medium | mitigate | Test reads audit JSONL after each call and asserts params.path is vault-relative (no absolute path, no rootPath leak); pattern asserted by regex `/^[A-Za-z][^/]*(\/.*)?$/` |
| T-7-19 | DoS | src/renderer/components/App.tsx | low | mitigate | Top-bar Vault button opens modal only on user click; modal is unmounted on close to free memory |
| T-7-20 | Tampering | src/renderer/styles/app.css | low | accept | CSS-only changes; no security impact; visual regression risk only |
| T-7-21 | DoS | tests/playwright/obsidian-integration.test.ts | low | mitigate | Teardown rmSync(userDataDir + vaultRoot, {recursive:true, force:true}); daemon subprocess killed in afterAll |

## Threat-to-Test Mapping

| Threat | Test coverage |
|--------|---------------|
| T-7-16 (per-bot glob parse) | obsidian-integration.test.ts case 1 (vault.read happy; audit shape) |
| T-7-17 (global config validation) | obsidian-integration.test.ts case 2 + 4 (vault.write + vault.search after setConfig) |
| T-7-18 (audit minimization) | obsidian-integration.test.ts cases 1-4 (every case reads audit JSONL) |
</threat_model>

<verification>
Phase-level checks after Plan 3 execution (full Phase 7 gate):
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot 2>&1 | tail -30` exits 0 (full Vitest suite green; Plan 07-01's 30 + Plan 07-02's 14 + Plan 07-03 has 0 new Vitest tests + all pre-existing tests unbroken; total target >= 200 passing tests)
- `npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30` exits 0 (TS build clean across all renderer + main + shared)
- `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts obsidian-integration.test.ts 2>&1 | tail -30` exits 0 with 4 passing cases
- `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts 2>&1 | tail -30` exits 0 (full Playwright daemon-smoke suite green)
- `grep -c 'BotSettingsObsidianTab\|VaultGlobalSettingsModal\|streamVaultReadToolUse\|streamVaultWriteToolUse\|streamVaultSearchToolUse\|settings-tab-obsidian\|vault-global-modal\|topbar-vault-button' src/renderer/components/BotSettingsPage.tsx src/renderer/components/App.tsx tests/playwright/fake-m3-server.ts tests/playwright/obsidian-integration.test.ts src/renderer/styles/app.css 2>&1 | awk '{ sum += $1 } END { if (sum < 15) exit 1 }'` succeeds (all symbols wired)
- `git ls-files -- src/renderer/components/BotSettingsObsidianTab.tsx src/renderer/components/VaultGlobalSettingsModal.tsx tests/playwright/obsidian-integration.test.ts` exits 0 (3 new files tracked)
</verification>

<success_criteria>
- Phase 7 success criterion #1 (user can configure vault path globally or per-bot) - PROVEN by obsidian-integration.test.ts cases 1-2 + BotSettingsObsidianTab + VaultGlobalSettingsModal (UI + IPC + daemon persistence all working)
- Phase 7 success criterion #2 (bot can read any note within per-bot allow/deny) - PROVEN by obsidian-integration.test.ts case 1 + Plan 07-01's vault_read.test.ts + Plan 07-02's vault_wikilink.test.ts
- Phase 7 success criterion #3 (bot can write only to Agents/<bot>/) - PROVEN by obsidian-integration.test.ts cases 2-3 (happy Agents + outside Agents refused) + Plan 07-01's vault_write.test.ts
- Phase 7 success criterion #4 (global deny globs block every bot) - PROVEN by obsidian-integration.test.ts case 1 (Private/** in globalDeny + vaultAllow=['Projects/**']; case 1 reads only Projects/foo.md; Private/secret.md is invisible to the bot) + Plan 07-01's vault_glob.test.ts deny-precedence cases
- Phase 7 success criterion #5 (bot can search vault by query with context lines) - PROVEN by obsidian-integration.test.ts case 4 + Plan 07-02's vault_search.test.ts

All 5 Phase 7 success criteria are now demonstrably TRUE.
</success_criteria>

<output>
Create `.planning/phases/07-obsidian-integration/07-03-SUMMARY.md` when done. After Plan 07-03 execution, run `/gsd-verify-work 7` to validate the phase against the success criteria.
</output>