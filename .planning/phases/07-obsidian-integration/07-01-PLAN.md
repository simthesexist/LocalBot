---
phase: 07-obsidian-integration
plan: 01
type: tracer
wave: 1
depends_on: ["phase-06-scheduler-notifications-06-03"]
files_modified:
  - package.json
  - package-lock.json
  - daemon/vault/config.cjs
  - daemon/vault/glob.cjs
  - daemon/vault/index.cjs
  - daemon/tools/vault_read.cjs
  - daemon/tools/vault_write.cjs
  - daemon/tools/registry.cjs
  - daemon/bots/loader.cjs
  - daemon/main.cjs
  - src/shared/types.ts
  - src/shared/ipc-channels.ts
  - src/shared/window.d.ts
  - src/main/paths.ts
  - src/main/preload/index.ts
  - src/main/ipc/vault.ts
  - src/main/ipc/index.ts
  - tests/unit/vault_config.test.ts
  - tests/unit/vault_glob.test.ts
  - tests/unit/vault_read.test.ts
  - tests/unit/vault_write.test.ts
autonomous: true
requirements:
  - OBS-01
  - OBS-02
  - OBS-03
  - OBS-04
  - OBS-05

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "User can configure a vault path per bot (bots.update patch carries vaultPath) OR globally (<userData>/vault.json via new vault.get_config / vault.set_config JSON-RPC); per-bot vaultPath overrides the global rootPath"
    - "Bot can read any note in the vault that passes the deny-wins glob pipeline (globalDeny -> vaultDeny -> vaultAllow); vaultAllow empty blocks everything; vaultAllow not matched yields not_in_allowlist; globalDeny match wins over per-bot allow"
    - "Bot can write ONLY into Agents/<bot-name>/ of the vault; any other write path is refused with code:'write_outside_agents'; safe_path realpath check is the first layer, Agents/<bot>/ containment is the second"
    - "Global deny globs (e.g., Private/**, Credentials/**) live in <userData>/vault.json and are evaluated BEFORE per-bot vaultDeny/vaultAllow; a path matching both global deny and per-bot allow is rejected with reason:'global_deny'"
    - "Vault config persists at <userData>/vault.json atomically via tmp+rename; corrupt JSON falls back to {rootPath:'', globalDeny:[]}"
    - "Every vault.read / vault.write / vault.get_config / vault.set_config audit row uses the 3-key minimization shape with vault-relative paths only (NEVER absolute paths)"
  artifacts:
    - "daemon/vault/config.cjs (NEW) exporting loadVaultConfig, saveVaultConfig, vaultConfigPath"
    - "daemon/vault/glob.cjs (NEW) exporting checkVaultAccess, makeMatcher"
    - "daemon/vault/index.cjs (NEW) barrel exporting loadVaultConfig, saveVaultConfig, vaultConfigPath, checkVaultAccess, makeMatcher"
    - "daemon/tools/vault_read.cjs (NEW) exporting call(args, ctx)"
    - "daemon/tools/vault_write.cjs (NEW) exporting call(args, ctx) with tmp+rename atomic write"
    - "daemon/tools/registry.cjs TOOLS array extended with 'vault.read' + 'vault.write'; SCHEMAS includes both; fileFor mapping unchanged (dotted->underscore rule)"
    - "daemon/bots/loader.cjs ALLOWED_CONFIG_KEYS extended with vaultPath, vaultAllow, vaultDeny; validateConfig enforces non-string entries in vaultAllow/vaultDeny arrays"
    - "daemon/main.cjs initialize case loads vault config via loadVaultConfig; tools/call ctx enriched with vaultRoot + globalDeny; new JSON-RPC cases 'vault/get_config' and 'vault/set_config'"
    - "src/shared/types.ts BotConfig extended with vaultPath?: string|null, vaultAllow?: string[], vaultDeny?: string[]; new VaultGlobalConfig + VaultConfigResult interfaces; 2 new MessageBlock variants vault_read + vault_write (vault_search variant added in Plan 07-02)"
    - "src/shared/ipc-channels.ts VAULT_GET_CONFIG + VAULT_SET_CONFIG + EVENT_VAULT_CONFIG_UPDATED constants added"
    - "src/shared/window.d.ts LocalbotApi.vault namespace added + LocalbotEvent union includes 'vault:config:updated'"
    - "src/main/paths.ts vaultConfigPath() helper added (path.join(userDataDir(), 'vault.json'))"
    - "src/main/preload/index.ts EVENT_CHANNELS Set adds EVENT_VAULT_CONFIG_UPDATED; api.vault namespace exposes getConfig + setConfig + on('vault:config:updated')"
    - "src/main/ipc/vault.ts (NEW) registerVaultHandlers wires VAULT_GET_CONFIG + VAULT_SET_CONFIG; broadcasts EVENT_VAULT_CONFIG_UPDATED on success"
    - "src/main/ipc/index.ts registerVaultHandlers invoked alongside existing registerBotHandlers/registerShellHandlers/registerNotificationHandlers"
    - "package.json + package-lock.json entry for picomatch@^4"
    - "tests/unit/vault_config.test.ts"
    - "tests/unit/vault_glob.test.ts"
    - "tests/unit/vault_read.test.ts"
    - "tests/unit/vault_write.test.ts"
  key_links:
    - "daemon/main.cjs initialize -> vault.loadVaultConfig(userDataDirState) -> stash currentVaultConfig; re-loaded on each vault/set_config so tool handler sees fresh globalDeny"
    - "daemon/main.cjs tools/call -> resolveVaultRoot(currentBot) = botCfg.vaultPath (if not '' or null) ?? currentVaultConfig.rootPath -> threaded into ctx.vaultRoot + ctx.globalDeny + ctx.vaultDeny + ctx.vaultAllow (re-read from botCfg per call, no cache, per Pitfall 4)"
    - "daemon/main.cjs vault/get_config -> vault.loadVaultConfig(userDataDirState) -> replyResult({ok:true, config})"
    - "daemon/main.cjs vault/set_config -> vault.saveVaultConfig(userDataDirState, cfg) -> re-load -> replyResult({ok:true, config})"
    - "daemon/tools/vault_read.cjs -> safePath(ctx.vaultRoot, args.path) -> checkVaultAccess({globalDeny: ctx.globalDeny, vaultDeny: ctx.vaultDeny, vaultAllow: ctx.vaultAllow, relativePath}) -> fs.readFile"
    - "daemon/tools/vault_write.cjs -> safePath(ctx.vaultRoot, args.path) -> path.relative(realpath('Agents/'+botId), resolved) check -> tmp+rename fs.writeFile (atomic)"
    - "daemon/bots/loader.cjs writeConfigPatch extends existing per-key guards: vaultPath (string|null|undefined), vaultAllow (array of strings), vaultDeny (array of strings)"
    - "src/main/ipc/vault.ts VAULT_SET_CONFIG -> callBot('vault/set_config', cfg) -> broadcast(EVENT_VAULT_CONFIG_UPDATED, {config}) -> renderer state/vault.ts subscribers refresh"
  prohibitions:
    - "MUST NOT include absolute vault paths or vault rootPath in any audit row (Pitfall 7)"
    - "MUST NOT trust renderer-supplied vault path or glob lists; daemon always resolves vaultRoot + reads botCfg + globalCfg at call-time (no cache; Pitfall 4)"
    - "vault.write MUST refuse any path whose realpath does not live under Agents/<bot>/ of the vault (Pitfall 6); the check uses path.relative(realAgents, resolved).startsWith('..') === false"
    - "Glob pipeline MUST run globalDeny FIRST, then vaultDeny, then vaultAllow (Pitfall 5)"
    - "Empty vaultAllow MUST block reads (reason:'no_allowlist')"
    - "vault.json corrupt JSON MUST fall back to {rootPath:'', globalDeny:[]} (defensive; never throws to the renderer)"
    - "MUST NOT add native deps; picomatch is pure-JS (CLAUDE.md constraint)"
    - "vault.read MUST NOT follow symlinks outside vault (safe_path realpath catches; cross-check with vault_glob deny pipeline)"
    - "renderer MUST NOT be able to override the effective vault root (daemon resolves from botCfg + globalCfg only)"
    - "existing TOOLS / SYSTEM_TOOLS / loadTool / callTool surface MUST remain backward-compatible (no breaking signature changes)"
---

# Artifacts this phase produces
# (cumulative across all 3 plans; this plan creates the daemon + IPC core)
#
# New files (Plan 1):
#   daemon/vault/config.cjs
#   daemon/vault/glob.cjs
#   daemon/vault/index.cjs
#   daemon/tools/vault_read.cjs
#   daemon/tools/vault_write.cjs
#   src/main/ipc/vault.ts
#   tests/unit/vault_config.test.ts
#   tests/unit/vault_glob.test.ts
#   tests/unit/vault_read.test.ts
#   tests/unit/vault_write.test.ts
#
# Modified files (Plan 1):
#   package.json (deps: picomatch)
#   package-lock.json (regenerated)
#   daemon/tools/registry.cjs (TOOLS + SCHEMAS additions for vault.read + vault.write)
#   daemon/bots/loader.cjs (ALLOWED_CONFIG_KEYS + per-key validation for vaultPath/vaultAllow/vaultDeny)
#   daemon/main.cjs (initialize loads vault config; tools/call ctx enriched; new JSON-RPC cases)
#   src/shared/types.ts (BotConfig fields + VaultGlobalConfig + MessageBlock variants)
#   src/shared/ipc-channels.ts (VAULT_GET_CONFIG + VAULT_SET_CONFIG + EVENT_VAULT_CONFIG_UPDATED)
#   src/shared/window.d.ts (LocalbotApi.vault namespace + LocalbotEvent union)
#   src/main/paths.ts (vaultConfigPath() helper)
#   src/main/preload/index.ts (EVENT_CHANNELS + api.vault.* namespace)
#   src/main/ipc/index.ts (registerVaultHandlers invocation)
#
# New files (Plan 2):
#   daemon/tools/vault_search.cjs
#   daemon/tools/vault_list.cjs
#   daemon/vault/wikilink.cjs (exported via index.cjs barrel)
#   src/renderer/state/vault.ts
#   src/renderer/components/VaultReadBlock.tsx
#   src/renderer/components/VaultSearchBlock.tsx
#   src/renderer/components/VaultWriteBlock.tsx
#   tests/unit/vault_search.test.ts
#   tests/unit/vault_wikilink.test.ts
#
# Modified files (Plan 2):
#   daemon/tools/registry.cjs (TOOLS + SCHEMAS additions for vault.search + vault.list)
#   daemon/main.cjs (register 2 new tools; wikilink-related ctx enrichment)
#   src/shared/types.ts (vault_search MessageBlock variant + EventEmitter helper exports)
#   src/renderer/components/MessageBlock.tsx (switch dispatch for vault_read + vault_search + vault_write)
#
# New files (Plan 3):
#   src/renderer/components/BotSettingsObsidianTab.tsx
#   src/renderer/components/VaultGlobalSettingsModal.tsx
#   tests/playwright/obsidian-integration.test.ts
#
# Modified files (Plan 3):
#   src/renderer/components/BotSettingsPage.tsx (5th tab 'obsidian')
#   src/renderer/components/App.tsx (top-bar Vault button + modal mount)
#   src/renderer/styles/app.css (vault settings + modal styles)
#   tests/playwright/fake-m3-server.ts (streamVaultReadToolUse + streamVaultWriteToolUse + streamVaultSearchToolUse)
#   playwright.config.ts (extend daemon-smoke project)

<objective>
Phase 7 tracer slice: build the daemon-side core of Obsidian vault access —
atomic userData/vault.json persistence + picomatch glob pipeline
(globalDeny -> vaultDeny -> vaultAllow) + two new tools (vault.read,
vault.write) that thread per-bot config from loader.cjs through main.cjs's
tools/call ctx — plus the IPC bridge (main + preload + shared types +
channels) so the renderer can read/write vault config end-to-end. After this
plan, the daemon can persist global vault config, evaluate the deny-wins
glob pipeline, refuse vault.write outside Agents/<bot>/, and surface both
tools to the LLM through the registry.

Purpose: prove the security-critical invariants (vaultRoot resolution,
global-deny precedence, write containment) and the IPC contract before
adding vault.search + vault.list + wikilink (Plan 2) and the renderer UI +
E2E (Plan 3).

Output: 5 new CJS modules, 1 new TypeScript IPC module, 4 unit test suites,
TypeScript build clean (proves preload + types + channels all align),
vault.read + vault.write exercised end-to-end at the JSON-RPC layer in unit
tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-obsidian-integration/07-RESEARCH.md
@.planning/phases/07-obsidian-integration/07-PATTERNS.md
@.planning/phases/07-obsidian-integration/07-VALIDATION.md
@.planning/phases/06-scheduler-notifications/06-03-PLAN.md
@.planning/phases/06-scheduler-notifications/06-03-SUMMARY.md

# Source files for direct reference
@daemon/main.cjs
@daemon/bots/loader.cjs
@daemon/bots/policy.cjs
@daemon/tools/registry.cjs
@daemon/tools/safe_path.cjs
@daemon/tools/read_file.cjs
@daemon/tools/write_file.cjs
@daemon/scheduler/index.cjs
@daemon/audit.cjs
@daemon/protocol.cjs
@src/shared/types.ts
@src/shared/ipc-channels.ts
@src/shared/window.d.ts
@src/main/paths.ts
@src/main/preload/index.ts
@src/main/ipc/bots.ts
@src/main/ipc/index.ts
@tests/unit/scheduler_persistence.test.ts
@tests/unit/bot_config.test.ts
@tests/unit/read_file.test.ts
@tests/unit/write_file.test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install picomatch + daemon/vault/{config,glob,index}.cjs + daemon/tools/{vault_read,vault_write}.cjs + extend loader.cjs ALLOWED_CONFIG_KEYS + wire registry + wire main.cjs tools/call ctx + vault JSON-RPC cases + shared types + shared channels + shared window.d.ts + paths + preload + ipc/vault.ts + ipc/index.ts</name>
  <files>
    - package.json
    - package-lock.json
    - daemon/vault/config.cjs
    - daemon/vault/glob.cjs
    - daemon/vault/index.cjs
    - daemon/tools/vault_read.cjs
    - daemon/tools/vault_write.cjs
    - daemon/tools/registry.cjs
    - daemon/bots/loader.cjs
    - daemon/main.cjs
    - src/shared/types.ts
    - src/shared/ipc-channels.ts
    - src/shared/window.d.ts
    - src/main/paths.ts
    - src/main/preload/index.ts
    - src/main/ipc/vault.ts
    - src/main/ipc/index.ts
  </files>
  <read_first>
    - daemon/main.cjs (initialize case at ~L246-280 for currentVaultConfig hook point; tools/call at L340-444 for ctx enrichment seam; case 'tools/call' ctx construction at L395-404 where workspaceRoot/botDir are threaded)
    - daemon/bots/loader.cjs (ALLOWED_CONFIG_KEYS at L19-36; validateConfig at L54-92; writeConfigPatch at L328-399; existing per-key guards at L347-363 as template for vaultPath/vaultAllow/vaultDeny guards)
    - daemon/bots/policy.cjs (getPolicy signature — Phase 4 allowlist loader the registry consults on every call)
    - daemon/tools/registry.cjs (TOOLS at L15-28; SCHEMAS at L37-168; SYSTEM_TOOLS at L35; callTool at L242-282; fileFor at L186-191)
    - daemon/tools/safe_path.cjs (safePath export — vault_read + vault_write use this verbatim)
    - daemon/tools/read_file.cjs (template for vault_read.cjs: imports + safe_path + fs.readFile + error mapping)
    - daemon/tools/write_file.cjs (template for vault_write.cjs: imports + safe_path + mkdir -p + fs.writeFile + error mapping)
    - daemon/scheduler/index.cjs (readSchedulesFile + persistSchedules at L50-100 — atomic tmp+rename template for vault config persistence)
    - daemon/audit.cjs (appendAudit signature: tool, bot, params, outcome, durationMs, error, tool_use_id)
    - daemon/protocol.cjs (sendNotification helper; replyResult/replyError pattern)
    - src/shared/types.ts (BotConfig interface at ~L331-358 to extend; MessageBlock union at L11-28 to add variants)
    - src/shared/ipc-channels.ts (CHANNELS object at L3-71 — pattern for adding VAULT_* + EVENT_VAULT_CONFIG_UPDATED)
    - src/shared/window.d.ts (LocalbotApi + LocalbotEvent type definitions — extend with vault namespace)
    - src/main/paths.ts (auditDir at L14-16 — template for vaultConfigPath helper)
    - src/main/preload/index.ts (EVENT_CHANNELS Set at L7-30; api namespace at L41-98 — pattern for adding api.vault.*)
    - src/main/ipc/bots.ts (registerBotHandlers pattern — template for registerVaultHandlers)
    - src/main/ipc/index.ts (register*Handlers invocation site)
    - 07-RESEARCH.md §Pattern 1 (ALLOWED_CONFIG_KEYS extension); §Pattern 2 (glob pipeline order); §Pattern 3 (Agents/<bot>/ write containment); §Pattern 6 (wikilink regex); §Standard Stack (picomatch + safe_path reuse)
    - 07-PATTERNS.md (file-by-file pattern map with code excerpts — direct template per file)
  </read_first>
  <action>
    Six coordinated changes:

    1) Install picomatch@^4 as the single new runtime dep. Run `npm install picomatch@^4 --save` from D:/Claude/Grokbot. Verify with `npm ls picomatch` (returns picomatch@4.x.x). package.json gains the entry; package-lock.json regenerates. No other dep changes.

    2) daemon/vault/config.cjs (NEW, ~80 lines). CJS module exporting loadVaultConfig(userDataDir), saveVaultConfig(userDataDir, cfg), vaultConfigPath(userDataDir), and __test__ seam (loadVaultConfigPath + persistQueue for tests). Internal: `vaultConfigPath(userDataDir)` returns path.join(userDataDir, 'vault.json'). `loadVaultConfig(userDataDir)` — readFileSync with utf8, JSON.parse, validate shape `{rootPath: string, globalDeny: string[]}`; on ENOENT return {rootPath:'', globalDeny:[]}; on SyntaxError OR non-string rootPath OR non-array globalDeny return the defensive default (never throw). `saveVaultConfig(userDataDir, cfg)` — validate cfg.rootPath is string (else throw {code:'invalid_vault_config'}); validate cfg.globalDeny is array of strings (else throw {code:'invalid_vault_config'}); serialize payload to JSON; writeFileSync to `<path>.<Date.now()>.tmp`; renameSync(tmp, final); best-effort unlinkSync(tmp) on failure. Wrap in a `persistQueue` promise chain so concurrent saves serialize (mirror scheduler_persistence pattern).

    3) daemon/vault/glob.cjs (NEW, ~80 lines). CJS module exporting makeMatcher(patterns) and checkVaultAccess({globalDeny, vaultDeny, vaultAllow, relativePath}). `picomatch = require('picomatch')`. `makeMatcher(patterns)` — if !Array.isArray(patterns) OR patterns.length === 0, return () => false; else return picomatch(patterns, {dot: true, nocase: false}). `checkVaultAccess` pipeline in this EXACT order (Pitfall 5 — global deny wins): (a) if globalDeny && globalDeny.length, run makeMatcher(globalDeny)(relativePath); if true, return {allowed:false, reason:'global_deny', pattern: <first matching pattern from globalDeny using picomatch.isMatch>}; (b) if vaultDeny && vaultDeny.length, same check, return {allowed:false, reason:'vault_deny', pattern: <first matching>}; (c) if !vaultAllow OR vaultAllow.length === 0, return {allowed:false, reason:'no_allowlist'}; (d) run makeMatcher(vaultAllow)(relativePath); if false return {allowed:false, reason:'not_in_allowlist'}; (e) return {allowed:true}. The 'pattern' field is computed via `patterns.find(p => picomatch.isMatch(relPath, p, {dot: true}))` to give audit logs the specific matched glob string.

    4) daemon/vault/index.cjs (NEW, ~15 lines). CJS barrel re-exporting from ./config.cjs + ./glob.cjs: module.exports = { loadVaultConfig, saveVaultConfig, vaultConfigPath, checkVaultAccess, makeMatcher }. (Plan 07-02 adds ./wikilink.cjs to the barrel.)

    5) daemon/tools/vault_read.cjs (NEW, ~80 lines). CJS tool exporting `async function call(args, ctx)`. Imports: fs from 'node:fs/promises', path from 'node:path', {safePath} from './safe_path.cjs', {checkVaultAccess} from '../vault/index.cjs'. Logic:
       - Extract args.path (string, required — else throw {code:'invalid_path'}), args.startLine (optional integer), args.endLine (optional integer).
       - Validate ctx.vaultRoot: if undefined OR empty string, throw {code:'vault_not_configured', message:'vault path not configured (per-bot or global)'} (Pitfall 8: empty string from botCfg.vaultPath: '' falls back here, which is correct).
       - Compute resolved = await safePath(ctx.vaultRoot, args.path) — throws {code:'outside_workspace'} for ../ or symlink escape (T-7-01).
       - Compute relativePath = path.relative(ctx.vaultRoot, resolved).replace(/\\/g, '/') — for audit + glob check.
       - Run checkVaultAccess({globalDeny: ctx.globalDeny || [], vaultDeny: ctx.vaultDeny || [], vaultAllow: ctx.vaultAllow || [], relativePath}); if !access.allowed, throw Object.assign(new Error(`access denied: ${access.reason}`), {code:'glob_denied', reason: access.reason, pattern: access.pattern}).
       - Read file: content = await fs.readFile(resolved, 'utf8'); bytes = Buffer.byteLength(content, 'utf8'); if ENOENT throw {code:'enoent'}; if EACCES throw {code:'eacces'}.
       - If startLine/endLine provided, slice content by lines; return {path: relativePath, content, bytes, startLine, endLine, truncated: false}. Otherwise return {path: relativePath, content, bytes, truncated: false}.
       - Audit shape (called from main.cjs tools/call, see step 7): params = {path: relativePath, bytes} (NEVER absolute path or rootPath; Pitfall 7).

    6) daemon/tools/vault_write.cjs (NEW, ~100 lines). CJS tool exporting `async function call(args, ctx)`. Imports: fs from 'node:fs/promises', path from 'node:path', {safePath} from './safe_path.cjs'. Logic:
       - Extract args.path (string, required), args.content (string, required — else throw {code:'invalid_content'}).
       - Validate ctx.vaultRoot undefined/empty → throw {code:'vault_not_configured'}. Validate ctx.bot undefined/empty → throw {code:'bot_required'} (Pitfall 6 defense).
       - Layer 1: resolved = await safePath(ctx.vaultRoot, args.path) — catches ../ + symlink escape.
       - Layer 2: agentsDir = path.join(ctx.vaultRoot, 'Agents', ctx.bot); agentsReal = await fs.realpath(agentsDir).catch(() => agentsDir); relToAgents = path.relative(agentsReal, resolved).replace(/\\/g, '/'); if relToAgents.startsWith('..') OR path.isAbsolute(relToAgents), throw {code:'write_outside_agents', message:`vault.write only allows paths inside Agents/${ctx.bot}/`}.
       - mkdir parent dirs: await fs.mkdir(path.dirname(resolved), {recursive: true}).
       - Atomic write: tmp = `${resolved}.${Date.now()}.tmp`; try { writeFile(tmp, content, utf8); rename(tmp, resolved); } catch (e) { try { unlink(tmp) } catch { /* ignore */ } throw e; }
       - Return {path: <relativePath>, bytesWritten: Buffer.byteLength(content, 'utf8')}.
       - Audit shape: params = {path: <relativePath inside Agents/>, bytesWritten}.

    7) daemon/tools/registry.cjs (MODIFY): TOOLS array gains 'vault.read' + 'vault.write' (place AFTER 'exec_command' at L27). SCHEMAS gains two entries (use vault.read/vault.write as the keys; fileFor maps them to vault_read.cjs/vault_write.cjs via the dotted->underscore rule at L190 — no TOOL_FILE override needed). Schema for vault.read: {name:'vault.read', description:'Read a note from the Obsidian vault (subject to per-bot + global glob enforcement).', input_schema:{type:'object', properties:{path:{type:'string', description:'Vault-relative path (e.g. "Projects/foo.md") or absolute path inside vault.'}, startLine:{type:'integer'}, endLine:{type:'integer'}}, required:['path']}}. Schema for vault.write: {name:'vault.write', description:'Write a note to Agents/<bot>/ of the vault. Refuses any other path.', input_schema:{type:'object', properties:{path:{type:'string', description:'Vault-relative path under Agents/<bot>/ (e.g. "Agents/alpha/note.md") or absolute path inside vault.'}, content:{type:'string', description:'Full file contents to write.'}}, required:['path','content']}}. DO NOT add to DEFAULT_POLICY (per RESEARCH.md §Architectural Responsibility Map: vault access is configured per-bot via vaultAllow/vaultDeny, not via the bot allowlist; bots opt in by editing their allowlist in the UI).

    8) daemon/bots/loader.cjs (MODIFY): extend ALLOWED_CONFIG_KEYS Set (L19-36) with three keys: 'vaultPath', 'vaultAllow', 'vaultDeny'. In validateConfig (L54-92) AFTER the scheduledPrompt check, add three guards:
       - if 'vaultPath' in cfg: must be undefined, null, or string. Anything else → throw {code:'invalid_config', message:'vaultPath must be string or null'}.
       - if 'vaultAllow' in cfg && cfg.vaultAllow !== undefined: must be array; every entry must be string. Else throw {code:'invalid_config', message:'vaultAllow must be array of strings'}.
       - if 'vaultDeny' in cfg && cfg.vaultDeny !== undefined: same shape.
       In writeConfigPatch (L328-399) BEFORE the merged= spreading, add the SAME three guards on `patch` (mirror the existing cron/notifyOnError/scheduledPrompt per-key guards at L347-363).

    9) daemon/main.cjs (MODIFY): three changes.
       (a) Add `const vault = require('./vault/index.cjs');` near top (with other requires).
       (b) Module-scope: let currentVaultConfig = null; (assigned on initialize + on every vault/set_config so tool handler sees fresh globalDeny). In initialize case AFTER userDataDirState is set, call `currentVaultConfig = vault.loadVaultConfig(userDataDirState);` (default shape {rootPath:'', globalDeny:[]} on missing/corrupt).
       (c) Helper `resolveVaultConfigForBot(botId)`: botCfg = botLoader.readConfig(userDataDirState, botId); vaultRoot = (botCfg && typeof botCfg.vaultPath === 'string' && botCfg.vaultPath.length > 0) ? botCfg.vaultPath : currentVaultConfig.rootPath; return {vaultRoot, globalDeny: currentVaultConfig.globalDeny || [], vaultDeny: (botCfg && Array.isArray(botCfg.vaultDeny)) ? botCfg.vaultDeny : [], vaultAllow: (botCfg && Array.isArray(botCfg.vaultAllow)) ? botCfg.vaultAllow : [], bot: botId}. If vaultRoot is empty/null, return {vaultRoot: undefined, ...} (the tool handler throws vault_not_configured).
       (d) In tools/call case (L395-404), BEFORE the registry.callTool call, compute `const vaultCtx = resolveVaultConfigForBot(bot);` and MERGE vaultCtx into the ctx spread: `...{...ctxBase, ...vaultCtx}`. The vault tools read ctx.vaultRoot / ctx.globalDeny / ctx.vaultDeny / ctx.vaultAllow / ctx.bot.
       (e) Audit minimization (T-7-04): for name === 'vault.read' || name === 'vault.write', auditParams = {path: successResult?.path || args.path, bytes: successResult?.bytes || successResult?.bytesWritten} (NEVER the absolute path; never rootPath; never the full args).
       (f) Two new JSON-RPC cases in the dispatch switch AFTER case 'bots/update' (or wherever the bots/* cases live): case 'vault/get_config': replyResult(id, {ok:true, config: vault.loadVaultConfig(userDataDirState)}); on error replyError(id, 'vault_get_config_failed', err.message). case 'vault/set_config': validate params shape (rootPath must be string; globalDeny must be array of strings; else replyError 'invalid_vault_config'); call vault.saveVaultConfig(userDataDirState, params); currentVaultConfig = vault.loadVaultConfig(userDataDirState); replyResult(id, {ok:true, config: currentVaultConfig}). Add audit append for both (tool: 'vault.get_config' or 'vault.set_config', params: {}, outcome + durationMs).

    10) src/shared/types.ts (MODIFY): four additive extensions.
       (a) BotConfig interface (at ~L331-358) gains three optional members: `vaultPath?: string | null;` (null = explicit "no vault for this bot"; empty string falls back to global); `vaultAllow?: string[];` (empty/undefined = blocked by default); `vaultDeny?: string[];`. Place them after `scheduledPrompt?: string;` to match the loader.cjs ordering.
       (b) New interfaces near the existing BotCreateRequest: `export interface VaultGlobalConfig { rootPath: string; globalDeny: string[]; }` and `export interface VaultConfigResult { ok: boolean; config?: VaultGlobalConfig; error?: string; }`.
       (c) MessageBlock union gains 2 variants (vault_search variant lands in Plan 07-02): `{kind: 'vault_read'; path: string; content: string; bytes: number; startLine?: number; endLine?: number; truncated: boolean}` and `{kind: 'vault_write'; path: string; bytesWritten: number}`.

    11) src/shared/ipc-channels.ts (MODIFY): add three new constants to the CHANNELS object (place AFTER the Phase 6 EVENT_NAVIGATE_TO_BOT at L67): `VAULT_GET_CONFIG: 'vault:get_config'`, `VAULT_SET_CONFIG: 'vault:set_config'`, `EVENT_VAULT_CONFIG_UPDATED: 'vault:config:updated'`. No other changes.

    12) src/shared/window.d.ts (MODIFY): in the LocalbotApi type definition (existing pattern matches `bot.*`), add a `vault: { getConfig: () => Promise<VaultConfigResult>; setConfig: (req: VaultGlobalConfig) => Promise<VaultConfigResult>; }` namespace. In the LocalbotEvent union (existing pattern matches 'bot:list:updated' | 'bot:status' | ...), add `'vault:config:updated'`. Place both after the bot event union members.

    13) src/main/paths.ts (MODIFY): add `export function vaultConfigPath(): string { return path.join(userDataDir(), 'vault.json'); }` (mirrors keyFilePath at L18-20).

    14) src/main/preload/index.ts (MODIFY): in EVENT_CHANNELS Set (L7-30), add `CHANNELS.EVENT_VAULT_CONFIG_UPDATED`. In the api namespace (L41-98), after the bot namespace at L60-70, add `vault: { getConfig: () => ipcRenderer.invoke(CHANNELS.VAULT_GET_CONFIG), setConfig: (req) => ipcRenderer.invoke(CHANNELS.VAULT_SET_CONFIG, req) }`. The `on('vault:config:updated', handler)` is automatically exposed via the existing `api.on` generic at L95-97 (no change needed).

    15) src/main/ipc/vault.ts (NEW, ~70 lines). TypeScript module exporting `registerVaultHandlers(): void`. Mirrors `src/main/ipc/bots.ts#registerBotHandlers` pattern: import { ipcMain } from 'electron'; import { CHANNELS } from '../../shared/ipc-channels'; import type { VaultConfigResult, VaultGlobalConfig } from '../../shared/types'; import { callBot } from '../daemon/spawn'; (verify the actual import path for callBot in bots.ts — match it). Local helper `function broadcast(channel: string, payload: unknown): void { for (const win of BrowserWindow.getAllWindows()) { if (!win.isDestroyed()) win.webContents.send(channel, payload); } }`. Two ipcMain.handle registrations:
       - CHANNELS.VAULT_GET_CONFIG → callBot('vault/get_config', {}) → return {ok: result?.ok === true, config: result?.config as VaultGlobalConfig | undefined, error: result?.error}; on catch → return {ok: false, error: (err as Error).message}.
       - CHANNELS.VAULT_SET_CONFIG → validate req shape (typeof req.rootPath === 'string' + Array.isArray(req.globalDeny) + every entry is string); if invalid return {ok: false, error: 'invalid_vault_config'}; on success callBot('vault/set_config', req); if result?.ok broadcast(CHANNELS.EVENT_VAULT_CONFIG_UPDATED, {config: result.config}); return result shape.

    16) src/main/ipc/index.ts (MODIFY): import { registerVaultHandlers } from './vault'; add `registerVaultHandlers();` invocation alongside the existing registerBotHandlers / registerShellHandlers / registerNotificationHandlers calls.

    Avoid: any renderer-side state changes (Plan 2). Avoid: opening the renderer BotSettingsPage or preload vault button in this plan. Avoid: any changes to the existing audit JSONL shape (the new vault tools append the 3-key shape with vault-relative path; the existing tests don't depend on vault-specific keys). Avoid: registering vault.* in DEFAULT_POLICY (per RESEARCH.md: vault access is per-bot allowlist-only; not added to defaults). Avoid: importing picomatch in any renderer file (renderer never sees the raw globs — only the configured bot's view via API responses).

    Validation step (REQUIRED before Task 2): after all 16 changes, run `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` from D:/Claude/Grokbot. Exit 0 (TS build clean) is the gate. Then run `npm ls picomatch 2>&1 | grep -E '^picomatch@'` — must return a single picomatch@4.x.x line. Then run `node -e "require('./daemon/vault/index.cjs')"` — must not throw.
  </action>
  <acceptance_criteria>
    - `npm ls picomatch` exits 0 with output containing `picomatch@4.`
    - package.json has `"picomatch": "^4"` under dependencies
    - daemon/vault/config.cjs exists; exports loadVaultConfig, saveVaultConfig, vaultConfigPath
    - daemon/vault/glob.cjs exists; exports checkVaultAccess, makeMatcher
    - daemon/vault/index.cjs exists; barrel re-exports the 5 symbols
    - daemon/tools/vault_read.cjs exists; exports call(args, ctx)
    - daemon/tools/vault_write.cjs exists; exports call(args, ctx); uses tmp+rename atomic write; uses path.relative for Agents/<bot>/ containment
    - daemon/tools/registry.cjs TOOLS array contains 'vault.read' and 'vault.write'
    - daemon/tools/registry.cjs SCHEMAS contains entries for vault.read + vault.write with input_schema
    - daemon/bots/loader.cjs ALLOWED_CONFIG_KEYS contains 'vaultPath', 'vaultAllow', 'vaultDeny'
    - daemon/bots/loader.cjs validateConfig + writeConfigPatch enforce non-string entries in vaultAllow/vaultDeny and non-string vaultPath (throws {code:'invalid_config'})
    - daemon/main.cjs initialize case calls vault.loadVaultConfig and stashes currentVaultConfig
    - daemon/main.cjs tools/call case resolves vaultRoot from botCfg.vaultPath (if non-empty) else currentVaultConfig.rootPath; threads {vaultRoot, globalDeny, vaultDeny, vaultAllow, bot} into ctx
    - daemon/main.cjs tools/call audit row for vault.read/vault.write carries ONLY {path: <relative>, bytes/bytesWritten}; NEVER absolute path or rootPath
    - daemon/main.cjs has 'vault/get_config' + 'vault/set_config' JSON-RPC cases; vault/set_config reloads currentVaultConfig after save
    - src/shared/types.ts BotConfig extends with vaultPath?, vaultAllow?, vaultDeny?
    - src/shared/types.ts exports VaultGlobalConfig + VaultConfigResult interfaces
    - src/shared/types.ts MessageBlock union gains 'vault_read' and 'vault_write' variants (vault_search lands in Plan 07-02)
    - src/shared/ipc-channels.ts contains VAULT_GET_CONFIG, VAULT_SET_CONFIG, EVENT_VAULT_CONFIG_UPDATED
    - src/shared/window.d.ts LocalbotApi extends with vault.{getConfig, setConfig}; LocalbotEvent union includes 'vault:config:updated'
    - src/main/paths.ts exports vaultConfigPath()
    - src/main/preload/index.ts EVENT_CHANNELS includes EVENT_VAULT_CONFIG_UPDATED; api.vault namespace exists
    - src/main/ipc/vault.ts exists; exports registerVaultHandlers; broadcasts EVENT_VAULT_CONFIG_UPDATED on setConfig success
    - src/main/ipc/index.ts invokes registerVaultHandlers() alongside existing handlers
    - `node -e "require('./daemon/vault/index.cjs')"` exits 0 (barrel resolves; picomatch loads)
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Claude/Grokbot && npm ls picomatch 2>&1 | grep -E '^picomatch@' && grep -l 'loadVaultConfig\|saveVaultConfig\|checkVaultAccess\|vault_not_configured\|write_outside_agents' daemon/vault/config.cjs daemon/vault/glob.cjs daemon/main.cjs daemon/tools/vault_read.cjs daemon/tools/vault_write.cjs 2>&1 && npx --prefix D:/Claude/Grokbot tsc --noEmit -p . 2>&1 | tail -30</automated>
    <fails_when>npm ls missing picomatch, grep returns empty for any of the 5 files, or tsc --noEmit reports any TS error</fails_when>
  </verify>
  <done>
    picomatch@^4 installed. All 5 new CJS modules + 1 new TS module created. ALLOWED_CONFIG_KEYS + validateConfig + writeConfigPatch extended. registry TOOLS + SCHEMAS extended. main.cjs initialize loads vault config; tools/call ctx enriched with vaultRoot + globalDeny + vaultDeny + vaultAllow + bot; 2 new JSON-RPC cases added. shared/types.ts BotConfig + VaultGlobalConfig + VaultConfigResult + 2 MessageBlock variants added. shared/ipc-channels.ts VAULT_* constants added. shared/window.d.ts LocalbotApi.vault namespace added. paths.ts vaultConfigPath() helper added. preload api.vault + EVENT_CHANNELS extended. src/main/ipc/vault.ts registerVaultHandlers wires the IPC. src/main/ipc/index.ts invokes it. TS build clean. picomatch loads. All wired symbols present in grep output.
  </done>
</task>

<task type="auto">
  <name>Task 2: Unit tests - vault_config + vault_glob + vault_read + vault_write (4 suites, >= 30 passing cases)</name>
  <files>
    - tests/unit/vault_config.test.ts
    - tests/unit/vault_glob.test.ts
    - tests/unit/vault_read.test.ts
    - tests/unit/vault_write.test.ts
  </files>
  <read_first>
    - tests/unit/scheduler_persistence.test.ts (template: createRequire + mkdtemp workspace + CJS module loading + corrupt JSON handling + concurrent persistQueue test)
    - tests/unit/bot_config.test.ts (template: validateConfig per-key guard tests; extend with vaultPath/vaultAllow/vaultDeny cases)
    - tests/unit/read_file.test.ts (template: mkdtemp + vi.mock electron + safe_path behavior tests)
    - tests/unit/write_file.test.ts (template: atomic write + containment)
    - daemon/vault/config.cjs (just created in Task 1)
    - daemon/vault/glob.cjs (just created in Task 1)
    - daemon/tools/vault_read.cjs (just created in Task 1)
    - daemon/tools/vault_write.cjs (just created in Task 1)
    - daemon/tools/safe_path.cjs (existing — vault tools reuse this)
    - daemon/bots/loader.cjs (just extended in Task 1 — test the new validation guards)
    - 07-VALIDATION.md row 07-01-01 + 07-01-02 + 07-01-03 + 07-01-04 (test map + Wave 0 requirements)
    - 07-RESEARCH.md §Common Pitfalls 1-8 (each pitfall needs at least one test case)
  </read_first>
  <action>
    Create 4 new unit test suites. Total target: >= 30 passing cases.

    1) tests/unit/vault_config.test.ts (NEW, >= 8 cases):
       - Use createRequire(import.meta.url) + mkdtemp workspace.
       - Case A: loadVaultConfig on non-existent file returns {rootPath:'', globalDeny:[]} (no throw).
       - Case B: saveVaultConfig({rootPath: 'C:/vault', globalDeny: ['Private/**']}) writes <userData>/vault.json atomically; loadVaultConfig reads back the same object.
       - Case C: corrupt JSON ('not json {{{') in vault.json does NOT crash loadVaultConfig; returns the defensive default {rootPath:'', globalDeny:[]}.
       - Case D: saveVaultConfig with typeof cfg.rootPath !== 'string' (e.g. number) throws {code:'invalid_vault_config'}; on-disk file unchanged.
       - Case E: saveVaultConfig with cfg.globalDeny = ['Private/**', 42] (non-string entry) throws {code:'invalid_vault_config'}; on-disk file unchanged.
       - Case F: concurrent saves (Promise.all of 5 saveVaultConfig calls with different configs) all succeed; final file is valid JSON and contains the last persisted shape (Pitfall 12 analog: persistQueue serializes).
       - Case G: atomic write — read <userData>/vault.json mid-write (custom fs.writeFileSync spy that pauses between writeFileSync and renameSync) shows file is either old content or new content, never partial bytes.
       - Case H: loadVaultConfig after a partial disk failure (corrupt shape: {rootPath: 123, globalDeny: 'Private/**'} — non-string rootPath + non-array globalDeny) falls back to default rather than throwing.

    2) tests/unit/vault_glob.test.ts (NEW, >= 10 cases):
       - Import checkVaultAccess + makeMatcher from the barrel; createRequire pattern.
       - Case A: globalDeny match wins — checkVaultAccess({globalDeny:['Private/**'], vaultDeny:[], vaultAllow:['**/*'], relativePath:'Private/secret.md'}) returns {allowed:false, reason:'global_deny', pattern:'Private/**'}.
       - Case B: vaultDeny match after globalDeny passes — checkVaultAccess({globalDeny:[], vaultDeny:['Daily/**'], vaultAllow:['**/*'], relativePath:'Daily/2026-09-19.md'}) returns {allowed:false, reason:'vault_deny', pattern:'Daily/**'}.
       - Case C: vaultAllow not matched after both denies pass — checkVaultAccess({globalDeny:[], vaultDeny:[], vaultAllow:['Projects/**'], relativePath:'Daily/2026-09-19.md'}) returns {allowed:false, reason:'not_in_allowlist'}.
       - Case D: vaultAllow empty (or undefined) blocks everything — checkVaultAccess({globalDeny:[], vaultDeny:[], vaultAllow:[], relativePath:'Projects/foo.md'}) returns {allowed:false, reason:'no_allowlist'}.
       - Case E: path matches both globalDeny AND vaultAllow — globalDeny wins (Pitfall 5). RelativePath='Private/notes.md'; globalDeny=['Private/**']; vaultAllow=['**/*']. Result: {allowed:false, reason:'global_deny'}.
       - Case F: dotfile in vault — relativePath='.obsidian/workspace.json'; vaultAllow=['**/*']; checkVaultAccess returns {allowed:true} (because {dot:true} is passed).
       - Case G: case-sensitive matching on POSIX paths — relativePath='Projects/Foo.md'; vaultAllow=['Projects/**']; result: {allowed:true}.
       - Case H: brace-expansion — vaultAllow=['Projects/{foo,bar}/**']; relativePath='Projects/bar/baz.md'; result: {allowed:true}.
       - Case I: makeMatcher with non-array input returns () => false (defensive); makeMatcher with empty array returns () => false (no patterns = match nothing).
       - Case J: order of evaluation — globalDeny checked FIRST even when vaultDeny would also match. Use a relativePath that matches both lists; assert reason is 'global_deny' (not 'vault_deny'). This is the deny-wins precedence invariant.

    3) tests/unit/vault_read.test.ts (NEW, >= 6 cases):
       - mkdtemp vault root; mkdir Projects/ + Daily/ + Private/ subdirs; write Projects/foo.md, Daily/2026-09-19.md, Private/secret.md.
       - Mock safe_path? NO — use real safe_path with the mkdtemp vaultRoot (which is what the daemon does at runtime).
       - Call vaultRead directly with `{ vaultRoot: tmp, globalDeny, vaultDeny, vaultAllow, bot: 'alpha', toolCallId: 'test', registry: undefined }`. (The ctx signature comes from registry.callTool which spreads additional fields; mirror that shape.)
       - Case A: reads a vault-relative file — vaultRoot={tmp}, vaultAllow=['Projects/**'], path='Projects/foo.md' returns {content: '...', bytes: <n>, path: 'Projects/foo.md'}.
       - Case B: rejects global_deny match — vaultRoot={tmp}, globalDeny=['Private/**'], vaultAllow=['**/*'], path='Private/secret.md' throws {code:'glob_denied', reason:'global_deny', pattern:'Private/**'}.
       - Case C: rejects when vaultAllow is empty — vaultRoot={tmp}, vaultAllow=[], path='Projects/foo.md' throws {code:'glob_denied', reason:'no_allowlist'}.
       - Case D: rejects path outside vaultRoot (../escape) — vaultRoot={tmp}, vaultAllow=['**/*'], path='../etc/passwd' throws {code:'outside_workspace'}.
       - Case E: vault_not_configured when ctx.vaultRoot is undefined — vaultRoot=undefined, vaultAllow=['**/*'], path='Projects/foo.md' throws {code:'vault_not_configured'}.
       - Case F: ENOENT for non-existent vault-relative file — vaultRoot={tmp}, vaultAllow=['Projects/**'], path='Projects/missing.md' throws {code:'enoent'}.
       - Case G (optional): startLine/endLine slicing — vaultRoot={tmp}, write a 10-line file, vaultAllow=['Projects/**'], path='Projects/foo.md', startLine=3, endLine=5 returns {content: 'line3\nline4\nline5', ...}.

    4) tests/unit/vault_write.test.ts (NEW, >= 6 cases):
       - mkdtemp vault root; mkdir Agents/ subdir.
       - Call vaultWrite directly with the same ctx shape as vault_read tests.
       - Case A: writes inside Agents/<bot>/ — vaultRoot={tmp}, bot='alpha', path='Agents/alpha/note.md', content='hello'. Verify file exists at <tmp>/Agents/alpha/note.md and content matches.
       - Case B: refuses write outside Agents/<bot>/ — vaultRoot={tmp}, bot='alpha', path='Projects/foo.md', content='x' throws {code:'write_outside_agents'}.
       - Case C: refuses write to a different bot's folder — vaultRoot={tmp}, bot='alpha', path='Agents/beta/note.md', content='x' throws {code:'write_outside_agents'} (Pitfall 6: cross-bot isolation).
       - Case D: refuses path with `..` escape — vaultRoot={tmp}, bot='alpha', path='Agents/alpha/../../etc/passwd' throws either {code:'outside_workspace'} (if realpath catches it via safe_path layer 1) or {code:'write_outside_agents'} (if safe_path resolves to inside vault but outside Agents). Document the actual outcome in the test.
       - Case E: atomic write — mid-write failure leaves no partial file. Use a custom fs.writeFileSync spy that throws after writing 5 bytes; verify <tmp>/Agents/alpha/note.md does NOT exist (only the .tmp file might exist; check that the .tmp is cleaned up).
       - Case F: creates parent directories recursively — vaultRoot={tmp}, bot='alpha', path='Agents/alpha/sub/dir/note.md', content='deep'. Verify <tmp>/Agents/alpha/sub/dir/note.md exists.
       - Case G: vault_not_configured when ctx.vaultRoot is undefined — vaultRoot=undefined, bot='alpha', path='Agents/alpha/note.md', content='x' throws {code:'vault_not_configured'}.
       - Case H: invalid_content (non-string) — bot='alpha', path='Agents/alpha/note.md', content={not:'a string'} throws {code:'invalid_content'}.
       - Case I (optional): bot_required when ctx.bot is undefined — vaultRoot={tmp}, bot=undefined, path='Agents/alpha/note.md', content='x' throws {code:'bot_required'}.

    Test infrastructure: spawn daemon is NOT needed (these are pure-function unit tests of the tool modules + vault submodules); use createRequire + mkdtemp + real safe_path. Set 30s timeouts. For Case G in vault_write.test.ts (atomic write), use vi.spyOn(fs, 'writeFileSync').mockImplementationOnce(...) to throw mid-write. For Case F in vault_config.test.ts (concurrent saves), use Promise.all of 5 saveVaultConfig calls; assert all resolve and the final file content matches the LAST queued write (because the persistQueue serializes).

    Avoid: any test that imports from src/main or src/renderer (those are TypeScript / Electron-coupled). Avoid: tests that depend on the Anthropic SDK. Avoid: real cron waits (no cron in Phase 7). Avoid: testing the daemon JSON-RPC layer in this plan (that's covered by Plan 07-03's Playwright E2E; here we test the modules in isolation).
  </action>
  <acceptance_criteria>
    - All 4 test files exist on disk
    - vault_config.test.ts has >= 8 passing cases (default + round-trip + corrupt + invalid shapes + concurrent + atomic + partial shape)
    - vault_glob.test.ts has >= 10 passing cases (global deny wins + vault deny after + allow not matched + empty allow + dotfile + case-sensitive + brace-expansion + non-array defensive + empty defensive + order invariant)
    - vault_read.test.ts has >= 6 passing cases (happy path + global deny + empty allow + outside escape + vault_not_configured + ENOENT)
    - vault_write.test.ts has >= 6 passing cases (happy Agents + outside Agents + cross-bot + .. escape + atomic + mkdir recursive + vault_not_configured + invalid_content)
    - All 4 files use createRequire(import.meta.url) for CJS module imports
    - Test files do NOT import from src/main or src/renderer
    - Total new passing tests: >= 30
    - `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean — proves the new shared/types.ts additions do not break existing Phase 1-6 code)
  </acceptance_criteria>
  <verify>
    <automated>npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot tests/unit/vault_config.test.ts tests/unit/vault_glob.test.ts tests/unit/vault_read.test.ts tests/unit/vault_write.test.ts 2>&1 | tail -50</automated>
    <fails_when>non-zero exit, or "0 passed" or "failed" in summary, or fewer than 30 total passing tests across the 4 suites</fails_when>
  </verify>
  <done>
    All 4 test files committed. Vitest exits 0 with at least 30 passing tests across the 4 suites. Daemon core end-to-end (vault.json persistence + deny-wins glob pipeline + vault.read safe_path + vault.write Agents/<bot>/ containment + atomic tmp+rename + per-key config validation) is provably correct without the renderer being involved. picomatch@^4 is installed. TypeScript build clean (proves the IPC contract is wired correctly). Pre-existing tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer to Main IPC | Renderer requests vault config + sets vault config via VAULT_GET_CONFIG / VAULT_SET_CONFIG. Main proxies to daemon JSON-RPC; main never reads/writes vault.json directly (SEC-01). |
| Main to Daemon JSON-RPC | Main's spawn.ts proxies vault/get_config + vault/set_config. Daemon is the only writer of <userData>/vault.json (atomic tmp+rename). |
| LLM (Anthropic SDK) to Daemon | The LLM calls tools via tools/call {name:'vault.read', arguments:{path}} or {name:'vault.write', arguments:{path, content}}. Daemon resolves botCfg + globalCfg at call-time (no cache), runs safe_path + checkVaultAccess + Agents/<bot>/ containment check before any fs call. |
| Daemon to vault filesystem | safe_path realpath ensures the requested path lives under vaultRoot; vault.write additionally requires Agents/<bot>/ containment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-7-01 | Tampering / EoP | daemon/tools/vault_read.cjs | high | mitigate | safe_path realpath + ancestor walk; outside_workspace thrown for any ../ or symlink escape; tested in vault_read.test.ts case D |
| T-7-02 | Information Disclosure | daemon/vault/glob.cjs | high | mitigate | Deny-wins pipeline (globalDeny FIRST then vaultDeny then vaultAllow); tested in vault_glob.test.ts cases A/E/J |
| T-7-03 | Tampering | daemon/tools/vault_write.cjs | high | mitigate | Agents/<bot>/ containment via path.relative(realAgents, resolved) check + safe_path layer 1; cross-bot isolation; tested in vault_write.test.ts cases B/C |
| T-7-04 | Information Disclosure | daemon/main.cjs audit append | medium | mitigate | vault.read/vault.write audit rows carry ONLY vault-relative path (never absolute vaultRoot); matches Phase 4 T-P4-22 + Phase 6 T-P6-19 minimization; verified by grep + manual review |
| T-7-05 | Tampering / DoS | daemon/vault/config.cjs | medium | mitigate | Atomic tmp+rename; corrupt JSON falls back to default {rootPath:'', globalDeny:[]}; non-string rootPath + non-array globalDeny rejected with invalid_vault_config; tested in vault_config.test.ts cases C/D/E/H |
| T-7-06 | DoS | daemon/main.cjs tools/call ctx | medium | mitigate | Per-call botCfg read (no cache) — vault config edits take effect immediately (Pitfall 4 mitigation); tool handler never trusts a cached value |
| T-7-07 | Spoofing / EoP | renderer -> daemon | high | mitigate | Daemon always resolves vaultRoot = botCfg.vaultPath ?? globalCfg.rootPath; renderer cannot override; renderer sends vault config via IPC but daemon validates and persists |
| T-7-08 | Tampering | daemon/tools/vault_read.cjs | medium | mitigate | safe_path realpath defeats symlinks pointing outside vault; vault.read never returns content from outside the resolved realpath |
| T-7-09 | Tampering | daemon/tools/vault_write.cjs | high | mitigate | tmp + rename atomic write + mkdir -p parent dirs + Agents/<bot>/ containment; mid-write failure leaves no partial file; tested in vault_write.test.ts case E |
| T-7-SC | Tampering | npm install picomatch | low | accept | picomatch is a well-known pure-JS package; `npm install picomatch@^4` pulls from npm registry; [OK] verdict from RESEARCH.md Package Legitimacy Audit |

## Threat-to-Test Mapping

| Threat | Test coverage |
|--------|---------------|
| T-7-01 (path traversal) | vault_read.test.ts case D + vault_write.test.ts case D |
| T-7-02 (deny precedence) | vault_glob.test.ts cases A/E/J |
| T-7-03 (cross-bot write isolation) | vault_write.test.ts case C |
| T-7-04 (audit minimization) | vault_config.test.ts (audit shape implicit) + grep gate in verify |
| T-7-05 (config persistence integrity) | vault_config.test.ts cases C/D/E/G/H |
| T-7-09 (atomic write) | vault_write.test.ts case E |
</threat_model>

<verification>
Phase-level checks after Plan 1 execution:
- `npm ls picomatch 2>&1 | grep -E '^picomatch@'` exits 0 (dependency installed)
- `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0 (TS build clean; daemon is CJS so no TS check)
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot tests/unit/vault_config.test.ts tests/unit/vault_glob.test.ts tests/unit/vault_read.test.ts tests/unit/vault_write.test.ts 2>&1 | tail -30` exits 0 with >= 30 new passing tests
- `npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot 2>&1 | tail -30` exits 0 (full Vitest suite still green; pre-existing tests unbroken)
- `grep -c 'loadVaultConfig\|saveVaultConfig\|checkVaultAccess\|vault_not_configured\|write_outside_agents' daemon/vault/config.cjs daemon/vault/glob.cjs daemon/main.cjs daemon/tools/vault_read.cjs daemon/tools/vault_write.cjs | awk '{ sum += $1 } END { if (sum < 20) exit 1 }'` succeeds (all symbols wired)
- `git ls-files -- daemon/vault/config.cjs daemon/vault/glob.cjs daemon/vault/index.cjs daemon/tools/vault_read.cjs daemon/tools/vault_write.cjs src/main/ipc/vault.ts tests/unit/vault_config.test.ts tests/unit/vault_glob.test.ts tests/unit/vault_read.test.ts tests/unit/vault_write.test.ts` exits 0 (10 new files tracked)
</verification>

<success_criteria>
- Phase 7 success criterion #1 (user can configure vault path globally or per-bot) - PARTIAL: daemon accepts vaultPath per-bot via bots/update + globalConfig via vault/set_config; renderer-side Obsidian tab + VaultGlobalSettingsModal land in Plan 3.
- Phase 7 success criterion #2 (bot can read any note within per-bot allow/deny) - PROVEN by vault_read.test.ts cases A/B/C + vault_glob.test.ts pipeline ordering cases A/B/C.
- Phase 7 success criterion #3 (bot can write only to Agents/<bot>/) - PROVEN by vault_write.test.ts cases A/B/C/D (happy path + outside Agents + cross-bot + .. escape).
- Phase 7 success criterion #4 (global deny globs block every bot) - PROVEN by vault_glob.test.ts cases A/E (global deny wins) + vault_read.test.ts case B (integration).
- Phase 7 success criterion #5 (bot can search vault by query) - DEFERRED to Plan 07-02 (vault.search tool).
</success_criteria>

<output>
Create `.planning/phases/07-obsidian-integration/07-01-SUMMARY.md` when done.
</output>