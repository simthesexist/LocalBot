---
phase: 05-shell-exec-with-approval
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - daemon/main.cjs
  - tests/playwright/fake-m3-server.ts
  - playwright.config.ts
  - tests/unit/exec_audit.test.ts
  - tests/unit/exec_denylist_defense.test.ts
  - tests/playwright/exec-command-approval.test.ts
autonomous: true
requirements:
  - TOOL-06
  - SEC-03
  - UI-04

estimate:
  tokens: 70000
  raw_tokens: 32000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Audit line for exec_command NEVER contains the full command string or stdout/stderr text - only the 6 minimization fields"
    - "Denylist check runs in daemon main.cjs tools/call branch AS WELL AS inside exec_command.cjs#call - defense in depth"
    - "End-to-end Playwright smoke: fake M3 emits exec_command -> modal pops up -> user clicks Allow -> daemon spawns -> stdout streams -> exit -> audit line is minimized"
    - "Approval timeout: a modal left open for 5 minutes auto-denies on the daemon side (proven by a Playwright test using a shorter timeout injection)"
  artifacts:
    - "daemon/main.cjs tools/call branch re-checks matchesDangerous before dispatch (defense in depth)"
    - "tests/unit/exec_audit.test.ts (NEW) - verifies execCommandAuditParams minimization invariants"
    - "tests/unit/exec_denylist_defense.test.ts (NEW) - verifies denylist runs in main.cjs even if exec_command.cjs has a bug"
    - "tests/playwright/fake-m3-server.ts extended with streamExecCommandToolUse"
    - "tests/playwright/exec-command-approval.test.ts (NEW) - E2E smoke"
    - "playwright.config.ts daemon-smoke project extended"
  key_links:
    - "Playwright exec-command-approval.test.ts spawns fake-m3, drives the renderer via Playwright APIs, observes DOM updates"
    - "fake-m3-server streamExecCommandToolUse emits a tool_use block for exec_command followed by a benign echo command"
    - "exec_audit.test.ts reads <userData>/audit/<today>.jsonl and asserts each exec_command line has ONLY the 6 fields"
  prohibitions:
    - "Audit line MUST NEVER include the full command, stdout, or stderr text (T-P5-08)"
    - "Playwright test MUST NOT use a real M3 API key - it MUST point at the fake-m3-server"
    - "Denylist defense-in-depth MUST NOT introduce a race (the second check must produce the same denylist_blocked error)"
    - "Playwright test MUST NOT depend on a desktop machine for the daemon-smoke variant (it's headless-only)"

# Artifacts this plan produces
# New: tests/unit/exec_audit.test.ts, tests/unit/exec_denylist_defense.test.ts,
#       tests/playwright/exec-command-approval.test.ts
# Modified: daemon/main.cjs (defense-in-depth denylist check + audit minimization patches if not already in Plan 1),
#           tests/playwright/fake-m3-server.ts (streamExecCommandToolUse extension),
#           playwright.config.ts (daemon-smoke project extended with exec-command-approval test)

<objective>
Plan 3 closes the verification loop on the trust boundary: (1) audit minimization is enforced by a unit test that reads the JSONL audit log, (2) denylist defense-in-depth is enforced by a unit test that monkey-patches exec_command.cjs to be empty and verifies the denylist still triggers from main.cjs, and (3) an end-to-end Playwright smoke drives the whole stack from fake M3 to daemon to renderer to modal click to stdout streaming.

Purpose: provide evidence that the security model from RESEARCH.md holds under realistic conditions, and surface any regressions before merge.
Output: 3 new test files + 1 daemon patch + fake-m3 extension, all green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-shell-exec-with-approval/05-RESEARCH.md
@.planning/phases/05-shell-exec-with-approval/05-VALIDATION.md
@.planning/phases/05-shell-exec-with-approval/05-01-PLAN.md
@.planning/phases/05-shell-exec-with-approval/05-02-PLAN.md

# Source files
@daemon/main.cjs
@daemon/audit.cjs
@daemon/exec/denylist.cjs
@daemon/tools/exec_command.cjs
@tests/playwright/fake-m3-server.ts
@playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit minimization verification + denylist defense-in-depth + 2 unit test suites + daemon patch</name>
  <files>
    - daemon/main.cjs
    - tests/unit/exec_audit.test.ts
    - tests/unit/exec_denylist_defense.test.ts
  </files>
  <read_first>
    - daemon/main.cjs (tools/call branch at ~L265-322; execCommandAuditParams helper added in Plan 1; per-bot allowlist gate in registry.callTool)
    - daemon/audit.cjs (appendAudit signature; JSONL line format per day)
    - daemon/exec/denylist.cjs (matchesDangerous already imported by exec_command.cjs)
    - tests/unit/exec_command.test.ts (template: __setSpawnForTest__ seam; createRequire for CJS)
    - 05-VALIDATION.md row 5-03-01 + 5-03-03 (audit minimization + defense-in-depth requirements)
  </read_first>
  <action>
    1) daemon/main.cjs (MODIFY): in the tools/call dispatch AFTER the per-bot allowlist check BUT BEFORE registry.callTool, add a defense-in-depth denylist re-check. If the command matches GLOBAL_DENYLIST, write the audit line (using execCommandAuditParams) and reply with error code 'denylist_blocked'. This is redundant with the check inside exec_command.cjs but ensures even a hypothetical bypass of the per-bot allowlist (e.g. someone adds exec_command to default allowlist) does not let dangerous commands through. Also verify the existing execCommandAuditParams call from Plan 1 is wired correctly - if Plan 1's appendAudit already uses execCommandAuditParams for exec_command, no additional change needed.

    2) tests/unit/exec_audit.test.ts (NEW, minimum 8 cases):
       - Spawn real daemon, issue 3 tools/call exec_command invocations: 'echo hello' (allow-once), 'echo world' (allow-always), and a denylist match.
       - Read <userDataDir>/audit/<today>.jsonl after each invocation.
       - For each audit line where tool === 'exec_command':
         a. assert keys.length === 6 (the 6 minimized fields)
         b. assert keys include exactly: ['command_redacted', 'commandLength', 'approvedBy', 'exitCode', 'stdoutBytes', 'stderrCount']
         c. assert NO key named 'command', 'stdout', 'stderr', 'output', 'input', 'params' is present
         d. assert command_redacted.length <= 80
         e. assert commandLength === original command length (NOT redacted length)
         f. assert approvedBy is one of 'user-once' | 'user-always' | 'denied' | 'denylist_blocked' | 'approval_timeout'
         g. assert stdoutBytes is a non-negative integer; for echo hello it should be >= 6
         h. assert exitCode is 0 for echo hello; assert denylist line has no exitCode (null)
       - Pattern: createRequire to load daemon/audit.cjs; use the audit reading helper that tests/unit/bot_crud.test.ts uses.

    3) tests/unit/exec_denylist_defense.test.ts (NEW, minimum 5 cases):
       - Verify defense-in-depth: monkey-patch the exec_command module's matchesDangerous call by setting __setDenylistForTest__ to return hit:false for everything (simulating a broken exec_command.cjs). The main.cjs denylist re-check (added in step 1) MUST still catch 'rm -rf /', 'sudo *', 'curl * | bash', 'Remove-Item -Recurse C:\\', 'del /s /q C:\\Windows'.
       - The test launches a daemon subprocess (mirror exec_approve.test.ts pattern) and sends 5 commands via tools/call. Each must reply with error code 'denylist_blocked' WITHOUT spawning.
       - Also verify: after the patch is removed (clear via __setDenylistForTest__), benign commands like 'echo hello' still go through the full approval flow (regression check).
       - Pattern: sendRequest with method 'tools/call' params {name:'exec_command', arguments:{command:...}}.

    Avoid: changing the audit JSONL schema (other tests depend on it). Do NOT add a denylist 'dry-run' mode in production code. Do NOT log the full command anywhere in main.cjs even for debug purposes - use the redacted slice.
  </action>
  <acceptance_criteria>
    - daemon/main.cjs tools/call branch has a defense-in-depth matchesDangerous check before registry.callTool
    - daemon/main.cjs tools/call branch uses execCommandAuditParams for exec_command audit lines
    - tests/unit/exec_audit.test.ts exists with >= 8 cases asserting the 6-field minimization invariant
    - tests/unit/exec_denylist_defense.test.ts exists with >= 5 cases proving defense-in-depth holds when exec_command.cjs is monkey-patched
    - Neither test file leaks full command text or stdout/stderr text in any assert
  </acceptance_criteria>
  <verify>
    <automated>npx --prefix D:/Claude/Grokbot vitest run --root D:/Claude/Grokbot tests/unit/exec_audit.test.ts tests/unit/exec_denylist_defense.test.ts 2>&1 | tail -50</automated>
    <fails_when>non-zero exit, or "0 passed" or "failed" in summary, or fewer than 13 total passing tests across the 2 suites</fails_when>
  </verify>
  <done>
    daemon/main.cjs patched with defense-in-depth denylist check. 2 new unit test suites committed. Vitest exits 0 with at least 13 passing tests. The audit minimization invariant is provably enforced: no test can pass while a full command or stdout text appears in the JSONL log.
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright E2E smoke - fake M3 streamExecCommandToolUse extension + exec-command-approval test + playwright.config update</name>
  <files>
    - tests/playwright/fake-m3-server.ts
    - tests/playwright/exec-command-approval.test.ts
    - playwright.config.ts
  </files>
  <read_first>
    - tests/playwright/fake-m3-server.ts (existing streamEditFileToolUse + streamReadFileToolUse helpers; SSE protocol)
    - tests/playwright/ (any existing *.test.ts for the daemon-smoke project pattern)
    - playwright.config.ts (daemon-smoke project definition: headless, uses fake-m3-server, custom env)
    - 05-VALIDATION.md row 5-03-02 (E2E smoke requirements)
  </read_first>
  <action>
    1) tests/playwright/fake-m3-server.ts (MODIFY): export streamExecCommandToolUse(command, options): AsyncGenerator<SSEEvent>. Mirrors the existing streamEditFileToolUse shape: yields a tool_use block {name:'exec_command', input:{command, cwd:options.cwd, timeoutMs:options.timeoutMs ?? 30000}} then ends. Use the SSE event protocol already in use (message_start, content_block_start, content_block_delta, content_block_stop, message_stop). Do NOT yield a tool_result - the daemon handles that internally and emits it via the tools/call reply.

    2) tests/playwright/exec-command-approval.test.ts (NEW): the E2E smoke. Headless Playwright (no desktop needed for daemon-smoke project). Pattern:
       - beforeAll: launch fake-m3-server on a free port; start the Electron app via playwright's _electron.launch() with env {ANTHROPIC_BASE_URL: `http://localhost:${fakeM3Port}`, LOCALBOT_USER_DATA_DIR: <tmp>, ELECTRON_DISABLE_GPU:1}; wait for the first BrowserWindow.
       - Test cases (>= 5):
         a. The happy path: type a chat message; fake-m3 streams a tool_use for exec_command {command:'echo hello-from-bot'}; assert a modal with class .approval-modal appears within 5s; click the Allow Once button; assert shell_stream MessageBlock appears with stdout containing 'hello-from-bot'; assert exit 0 footer; assert audit line written with the 6 minimized fields.
         b. The deny path: type a chat message; fake-m3 streams exec_command {command:'echo secret'}; click Deny; assert NO shell_stream block appears; assert error toast or inline error text.
         c. The always-allow path: click Always Allow; assert <userDataDir>/always-allow/<bot>.json has the command; type a second message with the same command; assert NO modal this time; assert shell_stream block appears immediately.
         d. The denylist path: fake-m3 streams exec_command {command:'rm -rf /'}; assert NO modal appears (denylist short-circuit); assert error inline; assert audit line with approvedBy='denylist_blocked'.
         e. The cancel path: fake-m3 streams exec_command {command:'sleep 60'}; click Allow Once; assert shell_stream block shows 'running' badge; click Cancel; assert exit code != 0 footer within 5s; assert child process is dead.
       - afterAll: close Electron app; kill fake-m3-server; rmSync(userDataDir, recursive, force).

    3) playwright.config.ts (MODIFY): extend the daemon-smoke project to include tests/playwright/exec-command-approval.test.ts. The existing config likely uses a glob like 'tests/playwright/*.test.ts' - verify and add the new file. Do NOT change the headed project's config (the headed shell-approval-headed.test.ts remains gated by LOCALBOT_SMOKE_OK=1).

    Avoid: introducing real Anthropic API calls (must use fake-m3-server). Avoid: relying on user interaction with the modal beyond what Playwright clicks (no manual steps). Do NOT use `npx playwright codegen` - hand-write the selectors per UI-SPEC.md class names.
  </action>
  <acceptance_criteria>
    - tests/playwright/fake-m3-server.ts exports streamExecCommandToolUse streaming the tool_use block via SSE
    - tests/playwright/exec-command-approval.test.ts has >= 5 E2E cases (happy, deny, always-allow, denylist, cancel)
    - E2E test launches Electron via _electron.launch + fake-m3-server, exercises the full stack
    - Denylist case proves NO modal appears (T-P5-09 enforcement)
    - Always-allow case proves persistence + short-circuit on second invocation
    - Cancel case proves the AbortController propagates to killChildTree within 5s
    - playwright.config.ts daemon-smoke project includes the new test
    - E2E test does NOT depend on a desktop machine (no headed browser)
  </acceptance_criteria>
  <verify>
    <automated>npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts exec-command-approval.test.ts 2>&1 | tail -30</automated>
    <fails_when>non-zero exit, or "0 passed" or "failed" in summary, or any test timeout, or fewer than 4 passing tests (the platform-specific cases may be skipped)</fails_when>
  </verify>
  <done>
    fake-m3-server extended with streamExecCommandToolUse. exec-command-approval E2E test committed. playwright.config.ts includes the new test in the daemon-smoke project. Playwright exits 0 with >= 4 passing cases. The full security model (denylist + approval gate + audit minimization + cancel) is exercised end-to-end without a real M3 key or a desktop machine.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Daemon main.cjs vs exec_command.cjs | Defense in depth: the same matchesDangerous check runs in BOTH places. A bug in one is caught by the other. |
| Audit log to disk | daemon/audit.cjs appends JSONL lines to <userData>/audit/<YYYY-MM-DD>.jsonl. The minimization in execCommandAuditParams is the LAST line of defense for not leaking secrets into the audit log. |
| Renderer to fake M3 | E2E test points the app at fake-m3-server on localhost - the real Anthropic API is never reached. |
| Playwright to Electron | _electron.launch() drives the app via DevTools Protocol. The modal click goes through real DOM events. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-14 | Information Disclosure | daemon/main.cjs audit.appendAudit | high | mitigate | execCommandAuditParams enforces 6-field minimization; exec_audit.test.ts verifies the invariant by reading the JSONL log |
| T-05-15 | Tampering | daemon/tools/exec_command.cjs | high | mitigate | Defense-in-depth denylist check in main.cjs catches commands even if exec_command.cjs#call is bypassed; exec_denylist_defense.test.ts proves this by monkey-patching |
| T-05-16 | Tampering | tests/playwright/exec-command-approval.test.ts | medium | mitigate | Uses fake-m3-server on localhost - never hits real Anthropic API |
| T-05-17 | Denial of Service | Playwright _electron.launch + Electron | low | accept | Test timeouts cap at 60s per case; afterEach cleanup terminates Electron |

## Threat-to-Test Mapping

| Threat | Test coverage |
|--------|---------------|
| T-05-14 (audit minimization) | exec_audit.test.ts (>= 8 cases reading JSONL log + asserting 6-field schema) |
| T-05-15 (defense-in-depth) | exec_denylist_defense.test.ts (>= 5 cases with monkey-patched exec_command.cjs) |
| T-05-16 (no real M3 leak) | exec-command-approval.test.ts (env points to fake-m3-server; verified by URL assertion) |
| T-05-17 (test DoS) | All E2E cases have explicit 30s timeout |
</threat_model>

<verification>
Phase-level checks after Plan 3 execution:
- `npm --prefix D:/Claude/Grokbot run build 2>&1 | tail -20` exits 0
- `npm --prefix D:/Claude/Grokbot test 2>&1 | tail -30` exits 0 with >= 220 passing tests (baseline 138 + Plan 1 ~36 + Plan 2 ~19 + Plan 3 ~13 = ~206 minimum)
- `npx --prefix D:/Claude/Grokbot playwright test --config D:/Claude/Grokbot/playwright.config.ts exec-command-approval.test.ts 2>&1 | tail -20` exits 0 with >= 4 passing tests
- `grep -c "matchesDangerous\|execCommandAuditParams" daemon/main.cjs | awk '{ if ($1 < 2) exit 1 }'` succeeds (both wired)
- `git ls-files -- tests/unit/exec_audit.test.ts tests/unit/exec_denylist_defense.test.ts tests/playwright/exec-command-approval.test.ts` exits 0 (3 new files tracked)
</verification>

<success_criteria>
ALL FOUR Phase 5 success criteria from ROADMAP.md are now PROVEN:
- #1 Approval modal pops up with Allow Once / Always Allow / Deny: PROVEN by shell_approval_modal.test.ts (Plan 2) + exec-command-approval.test.ts case A (Plan 3)
- #2 Globally dangerous commands auto-denied before modal: PROVEN by exec_denylist.test.ts (Plan 1) + exec_approve.test.ts case E (Plan 1) + exec-command-approval.test.ts case D (Plan 3)
- #3 stdout/stderr streams inline in chat: PROVEN by shell_stream_block.test.ts (Plan 2) + exec-command-approval.test.ts case A (Plan 3)
- #4 Always Allow persists per-bot: PROVEN by exec_alwaysAllow.test.ts (Plan 1) + exec-command-approval.test.ts case C (Plan 3)
</success_criteria>

<output>
Create `.planning/phases/05-shell-exec-with-approval/05-03-SUMMARY.md` when done.
</output>

