---
phase: 03-memory-conversation-history
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - tests/playwright/fake-m3-server.ts
  - tests/playwright/memory-history.test.ts
  - tests/playwright/tree-diff.test.ts
  - tests/playwright/daemon-tools.test.ts
  - src/renderer/styles/app.css
  - src/renderer/components/WorkspaceTree.tsx
  - src/renderer/components/MemoryPill.tsx
  - src/renderer/components/MemoryPanel.tsx
  - src/renderer/components/SummaryBlock.tsx
  - src/renderer/components/SessionSwitcher.tsx
  - src/renderer/components/DiffView.tsx
  - src/renderer/components/MessageBlock.tsx
  - src/renderer/components/Chat.tsx
  - src/renderer/state/memory.ts
  - src/renderer/state/tree.ts
  - src/renderer/state/sessions.ts
  - src/renderer/state/messages.ts
  - src/main/preload/index.ts
  - src/main/ipc/chat.ts
  - daemon/audit.cjs
  - package.json
autonomous: true
requirements:
  - AGENT-05
  - AGENT-06
  - LLM-04
  - UI-08

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Playwright memory-history.test.ts (extended from Wave 1) proves: legacy global.jsonl migrates to default/<iso>.jsonl; a long fake-M3 stream triggers maybeSummarize; the JSONL head-of-file carries the summary record; on relaunch, history:load returns the same messages + headSummary; SessionSwitcher lists multiple sessions newest-first and switching re-loads the chat."
    - "Playwright tree-diff.test.ts (new) proves: WorkspaceTree renders react-arborist rows; an edit_file tool_result with binary content renders the DiffBinaryPlaceholder (no crash); chokidar fires tree:refresh within 500ms of a file change and the tree re-renders the new entry; DiffView mounts inline within the existing tool_result block."
    - "Visual polish per UI-SPEC §2.1 + §7 is complete: every interactive element uses the documented tokens; hover / focus / disabled states render correctly; the MemoryPanel modal is keyboard-navigable; the SessionSwitcher dropdown has the right max-height + overflow."
    - "Headed Electron smoke (gated by LOCALBOT_SMOKE_OK) launches the app against fake M3, sends a long message, and observes the SummaryBlock appear in the chat + the workspace tree refresh after a write_file tool call."
  artifacts:
    - tests/playwright/memory-history.test.ts (extended — assertions for SessionSwitcher listing + switching; restart-reload roundtrip)
    - tests/playwright/tree-diff.test.ts (NEW — full vertical: WorkspaceTree render + chokidar refresh + DiffView for edit_file + binary placeholder)
    - tests/playwright/fake-m3-server.ts (extended — streamEditFileToolUse + streamBinaryEditFile helpers)
    - tests/playwright/daemon-tools.test.ts (extended — audit JSONL coverage for tree.refresh event)
    - src/renderer/styles/app.css (polish pass: hover states, focus rings, transition timings per UI-SPEC §4-6)
    - src/renderer/components/* (polish pass: aria-live region, .bubble-summary visual baseline, .memory-panel max-width)
    - package.json (final dep audit; verify no duplicate dependencies; final npm ls output captured in commit message)
  key_links:
    - "tests/playwright/memory-history.test.ts: spawns daemon + Electron + fake M3 with LOCALBOT_SOFT_CAP_TOKENS=10; sends a long message; asserts (a) legacy migration JSONL file exists, (b) summary block appears in chat, (c) JSONL head-of-file is summary row, (d) SessionSwitcher lists 2+ sessions, (e) clicking an old session reloads the chat"
    - "tests/playwright/tree-diff.test.ts: spawns daemon + Electron + fake M3; fake M3 streams a tool_use {name:'edit_file', input:{path:'hello.txt', find:'world', replace:'planet'}}; fake M3 follow-up text 'Done'; asserts the renderer renders a DiffView (header 'Diff: hello.txt'; body has 'world' and 'planet' literals); then asserts WorkspaceTree updates within 500ms when hello.txt is touched externally"
    - "tests/playwright/fake-m3-server.ts: streamEditFileToolUse({find, replace, followupText}) emits SSE with message_start, content_block_start(tool_use), input_json_delta* chunked, content_block_stop, content_block_start(text), text_delta*(followupText), content_block_stop, message_delta(stop_reason:'end_turn'), message_stop"
    - "tests/playwright/fake-m3-server.ts: streamBinaryEditFile emits the same flow but the daemon returns {binary:true} from edit_file so the renderer takes the placeholder path"
    - "src/renderer/components/MemoryPanel.tsx: ARIA aria-live='polite' on the body announces 'Memory updated' when content refreshes mid-open"
    - "src/renderer/components/WorkspaceTree.tsx: aria-live='polite' region announces 'Workspace updated' on tree:refresh per UI-SPEC §12"

prohibitions:
  - "no real M3 API calls in any Playwright test — all use fake-m3-server"
  - "no headed Electron run without LOCALBOT_SMOKE_OK=1 in the env"
  - "no new deps installed in this plan"
  - "no regression on Phase 1+2 surfaces: chat shell + tool blocks + abort + cancel must keep working"
  - "no npm install that mutates package-lock.json beyond the lockfile drift this plan already anticipates (none expected)"

artifacts:
  - "Playwright headed smoke gating: LOCALBOT_SMOKE_OK=1 env var required for tree-diff.test.ts and the headed portion of memory-history.test.ts"
  - "Fake M3 helpers added: streamEditFileToolUse({find, replace, followupText?}); streamBinaryEditFile()"
  - "Visual polish tokens applied: --lb-tree-hover, --lb-tree-selected, --lb-tree-focus, --lb-pill-bg, --lb-binary-bg, --lb-summary-bg, --lb-summary-border per UI-SPEC §2.1"
  - "Focus rings: 2px solid var(--lb-tree-focus) with 2px offset on every interactive element"
  - "ARIA aria-live regions: aria-live='polite' on MemoryPanel body + WorkspaceTree container for screen reader announcements"
  - "SessionSwitcher + MemoryPill + WorkspaceTree all visually integrated into the chat shell header + sidebar from Phase 1+2 with no regressions"
  - "npm ls output captured for final commit message: @anthropic-ai/sdk@0.40.1, chokidar@3.6.0, react-arborist@3.4.0, react-diff-viewer-continued@4.0.0, diff@5.2.0"
  - "Audit JSONL file at <userData>/audit/<UTC-day>.jsonl contains one line per tool call (read_file, write_file, edit_file, list_dir, code_search, memory.read, memory.write, memory.update, tree.list) plus per-event tree.refresh entries"

---

# Plan 03-03 — Smoke + visual polish + audit log coverage verification

**As a** Localbot user,
**I want to** see the full Phase 3 surface work end-to-end (memory persistence + JSONL routing + summarize + tree + diff + session switcher + memory modal),
**so that** `/gsd-verify-work` can confirm all 4 phase requirements pass and the renderer matches the UI-SPEC contract.

## Objective

Close Phase 3 with three activities:

1. **Playwright smoke (memory-history + tree-diff)** — extend `fake-m3-server.ts` with `streamEditFileToolUse` and `streamBinaryEditFile` helpers; extend `memory-history.test.ts` (Wave 1) with SessionSwitcher + restart-reload assertions; create new `tree-diff.test.ts` proving WorkspaceTree render + chokidar refresh + DiffView + binary placeholder.
2. **Visual polish per UI-SPEC** — apply focus rings, hover states, aria-live regions, and token-driven colors to every new component. Verify the chat shell + sidebar + header composition is visually coherent.
3. **Final coverage audit** — confirm every Phase 3 requirement (AGENT-05, AGENT-06, LLM-04, UI-08) has at least one passing automated test; capture `npm ls` output for the commit message; run `npm run test:all` and headed Electron smoke gated by `LOCALBOT_SMOKE_OK`.

## Purpose

The unit tests prove the components work in isolation; the Playwright smokes prove they work end-to-end against a real daemon + Electron renderer. Without this wave, the Phase 3 verification gate cannot confirm "the bot auto-summarizes older messages so the conversation can continue" — that assertion requires a full headed run.

## Output

- `tests/playwright/memory-history.test.ts` (extended from Wave 1) — proves legacy migration + summarize + restart-reload + SessionSwitcher roundtrip.
- `tests/playwright/tree-diff.test.ts` (NEW) — proves WorkspaceTree render + chokidar refresh + DiffView + binary placeholder.
- `tests/playwright/fake-m3-server.ts` (extended) — adds `streamEditFileToolUse` + `streamBinaryEditFile` helpers.
- Visual polish pass: every new component matches UI-SPEC §2-7 token-for-token.
- Final audit log coverage table (committed as a comment in the SUMMARY) confirming all 9 op names appear in `<userData>/audit/<UTC-day>.jsonl` after a full smoke run.

---

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-memory-conversation-history/03-RESEARCH.md
@.planning/phases/03-memory-conversation-history/03-UI-SPEC.md
@.planning/phases/03-memory-conversation-history/03-VALIDATION.md

# Wave 1+2 source files this plan verifies
@daemon/main.cjs
@daemon/audit.cjs
@daemon/watcher.cjs
@daemon/tools/memory_read.cjs
@daemon/tools/memory_write.cjs
@daemon/tools/list_tree.cjs
@src/main/sessions/jsonl.ts
@src/main/bots/memory.ts
@src/main/llm/summarize.ts
@src/main/llm/usageAccumulator.ts
@src/main/ipc/chat.ts
@src/main/ipc/history.ts
@src/main/tree/watcher.ts
@src/main/tree/list.ts
@src/renderer/components/MemoryPill.tsx
@src/renderer/components/MemoryPanel.tsx
@src/renderer/components/SummaryBlock.tsx
@src/renderer/components/SessionSwitcher.tsx
@src/renderer/components/WorkspaceTree.tsx
@src/renderer/components/DiffView.tsx
@src/renderer/components/MessageBlock.tsx
@src/renderer/components/Chat.tsx
@src/renderer/state/memory.ts
@src/renderer/state/tree.ts
@src/renderer/state/sessions.ts
@src/renderer/state/messages.ts
@src/renderer/styles/app.css
@tests/playwright/fake-m3-server.ts
@tests/playwright/memory-history.test.ts
@tests/playwright/daemon-tools.test.ts

# Wave 1+2 plan SUMMARYs (filled in by executors)
@.planning/phases/03-memory-conversation-history/03-01-SUMMARY.md
@.planning/phases/03-memory-conversation-history/03-02-SUMMARY.md
</context>

---

<tasks>

## Task 1 — Extend fake-m3-server.ts + Playwright memory-history smoke (SessionSwitcher + restart-reload)

<read_first>
- @tests/playwright/fake-m3-server.ts (Wave 1 added streamLongResponse; Phase 2 added streamToolUseResponse)
- @tests/playwright/memory-history.test.ts (Wave 1 end-to-end with summarize; Wave 3 adds SessionSwitcher + restart-reload assertions)
- @tests/playwright/electron-helper.ts (Phase 1+2 spawn helper; sets LOCALBOT_USER_DATA_DIR + M3_PORT + LOCALBOT_SOFT_CAP_TOKENS)
- @.planning/phases/03-memory-conversation-history/03-UI-SPEC.md §"SessionSwitcher 1.5" + §"7.1 Copywriting (SessionSwitcher)"
- @.planning/phases/03-memory-conversation-history/03-VALIDATION.md §"Per-Task Verification Map" row 03-03-01
</read_first>

<acceptance_criteria>
- `tests/playwright/fake-m3-server.ts` exports `streamEditFileToolUse({find, replace, followupText = 'Done', port})` that emits an SSE stream:
  1. `event: message_start` -> `{type:'message_start', message:{id, type:'message', role:'assistant', content:[], stop_reason:null, usage:{input_tokens:200, output_tokens:0}}}`
  2. `event: content_block_start` -> `{type:'content_block_start', index:0, content_block:{type:'tool_use', id:'tu_edit', name:'edit_file', input:{}}}`
  3. `event: content_block_delta` -> `{type:'content_block_delta', index:0, delta:{type:'input_json_delta', partial_json: '{"path":"hello.txt","find":"' + find + '","replace":"' + replace + '"}'}}` (chunked into 2 deltas if long)
  4. `event: content_block_stop` -> `{type:'content_block_stop', index:0}`
  5. `event: content_block_start` -> `{type:'content_block_start', index:1, content_block:{type:'text', text:''}}`
  6. `event: content_block_delta` -> `{type:'content_block_delta', index:1, delta:{type:'text_delta', text:followupText}}`
  7. `event: content_block_stop` -> `{type:'content_block_stop', index:1}`
  8. `event: message_delta` -> `{type:'message_delta', delta:{stop_reason:'end_turn'}, usage:{output_tokens:30}}`
  9. `event: message_stop` -> `{type:'message_stop'}`
- `tests/playwright/fake-m3-server.ts` exports `streamBinaryEditFile(port)` that emits the same flow as `streamEditFileToolUse` but the mock daemon's `edit_file.cjs` returns `{binary:true, file:'hello.txt', before:'\0binary', after:'\0binary'}` (the renderer takes the placeholder path).
- `tests/playwright/memory-history.test.ts` extends the Wave 1 smoke with:
  1. After the summarize assertion, send a SECOND message via the fake M3 (the renderer now has 2 messages in the active session); the second message creates a new session file (since the renderer gets `currentSessionId` from the prior load). Assert that `<tmp>/sessions/default/` contains at least 2 `.jsonl` files.
  2. Open `<window>.localbot.invoke('history:list', {bot:'default'})` -> assert the response has 2+ `SessionEntry` rows, sorted newest-first.
  3. Click the `<button class="session-trigger">` in the header (via `page.click('.session-trigger')`); assert the dropdown appears with the expected rows.
  4. Click the FIRST row (the older session); assert the chat re-renders with that session's messages (count matches the migrated legacy rows) + the headSummary is null (no summary in the legacy session).
- After clicking, kill the Electron renderer, relaunch (using the same `<tmp>`), assert `history:load` returns the SAME messages + headSummary (proves restart-reload of the saved state).
</acceptance_criteria>

<files>
- tests/playwright/fake-m3-server.ts
- tests/playwright/memory-history.test.ts
</files>

<action>
Extend `tests/playwright/fake-m3-server.ts`:
- Add `streamEditFileToolUse({find, replace, followupText, port, requestBody})` with the exact event sequence above. The function reads `requestBody.messages` only to log the request count (no per-message logic).
- Add `streamBinaryEditFile({port, requestBody})` that emits a tool_use for `edit_file` with input `{path:'hello.bin', find:'old', replace:'new'}` but the mock daemon (registered in `tests/playwright/fake-m3-server.ts` itself OR via a stubbed `daemon/main.cjs`) returns `{binary:true}`.
  - The simplest implementation: extend the fake-m3-server to accept a `requestBody.tools` field; if any tool name matches `edit_file`, emit a tool_use; the renderer then calls the real daemon via spawn — the daemon's actual `edit_file.cjs` returns `{binary:true}` only when the file contains NUL bytes. To force this, the test pre-creates `<tmp>/workspace/default/hello.bin` with `\0binary\0content` and the fake M3 emits `edit_file {path:'hello.bin', find:'old', replace:'new'}`. The real daemon returns `{binary:true, file:'hello.bin'}` and the renderer renders the placeholder.
- Use the existing `PORT` env var pattern: `streamEditFileToolUse({port: process.env.M3_PORT, ...})`.

Extend `tests/playwright/memory-history.test.ts`:
- After the Wave 1 assertions (summarize + JSONL head + restart), add a "Phase 3 Wave 3 — SessionSwitcher round-trip" section:
  1. Capture the `currentSessionId` from the most recent `history:loaded` payload (via `window.localbot` event listener).
  2. Send a second `sendMessage` -> fake M3 returns `streamLongResponse({inputTokens: 50000})` to create a NEW session file. The chat handler creates `currentSessionId = new Date()...` and appends the user message to the new file.
  3. Wait for the second stream to complete.
  4. Call `window.localbot.invoke('history:list', {bot:'default'})` -> assert `result.length >= 2` and the first row's `sessionId !== currentSessionId`.
  5. `page.click('.session-trigger')` -> wait for `.session-dropdown` to be visible.
  6. Click the second row in the dropdown (`.session-dropdown [data-session-id]`); wait for the chat to re-render.
  7. Assert `messages.length === 5` (the legacy migrated rows).
  8. Assert the message list shows `headSummary === null` for this session (no summary was run on the legacy rows).
- "Phase 3 Wave 3 — restart-reload" section:
  1. Capture the final state of `<tmp>/sessions/default/*.jsonl`.
  2. Kill the Electron renderer via `electronApp.close()` (Playwright Electron API).
  3. Relaunch with the same `<tmp>` and same `LOCALBOT_USER_DATA_DIR`.
  4. Wait for the chat to mount.
  5. Assert `window.localbot.invoke('history:load', {bot:'default', sessionId: <mostRecent>})` returns `{messages, headSummary}` matching what was persisted before the kill.

</action>

<verify>
<automated>npx --prefix D:/Claude/Grokbot playwright test tests/playwright/memory-history.test.ts 2>&1 | tail -50</automated>
<fails_when>non-zero exit; "FAIL" anywhere; "Error" appearing that does not include the expected migration / summary / session-list context</fails_when>
</verify>
<automated>grep -c "streamEditFileToolUse\|streamBinaryEditFile" D:/Claude/Grokbot/tests/playwright/fake-m3-server.ts | awk '{ if ($1 < 2) exit 1 }'</automated>
<fails_when>fake-m3-server.ts missing the new edit_file helpers</fails_when>
</verify>
<automated>grep -c "history:list\|session-trigger\|session-dropdown\|electronApp.close\|electronApp.launch" D:/Claude/Grokbot/tests/playwright/memory-history.test.ts | awk '{ if ($1 < 4) exit 1 }'</automated>
<fails_when>memory-history.test.ts missing the SessionSwitcher / restart assertions</fails_when>
</verify>
<automated>test -f D:/Claude/Grokbot/tests/playwright/fake-m3-server.ts</automated>
<fails_when>fake-m3-server.ts missing</fails_when>
</verify>

<done>
- `fake-m3-server.ts` exports `streamEditFileToolUse` and `streamBinaryEditFile`.
- `memory-history.test.ts` proves SessionSwitcher lists 2+ sessions, switching reloads, restart-reload round-trips, and the legacy migration + summarize assertions from Wave 1 still hold.
- The full test exits 0; no skipped tests.
</done>

</task>

## Task 2 — Playwright tree-diff.test.ts (WorkspaceTree + chokidar refresh + DiffView + binary placeholder)

<read_first>
- @tests/playwright/daemon-tools.test.ts (proven Phase 2 Playwright pattern for spawning daemon + audit assertions)
- @tests/playwright/fake-m3-server.ts (Task 1 added streamEditFileToolUse + streamBinaryEditFile)
- @tests/playwright/smoke-tools.test.ts (Phase 2 final smoke; gated by LOCALBOT_SMOKE_OK)
- @src/renderer/components/DiffView.tsx (Wave 2 — binary placeholder branch)
- @src/renderer/components/MessageBlock.tsx (Wave 2 — diff branch for edit_file tool_result)
- @src/renderer/components/WorkspaceTree.tsx (Wave 2 — react-arborist + lazy children + tree:refresh)
- @daemon/watcher.cjs (Wave 2 — chokidar debounced 250ms)
- @.planning/phases/03-memory-conversation-history/03-VALIDATION.md §"Per-Task Verification Map" row 03-03-02
</read_first>

<acceptance_criteria>
- `tests/playwright/tree-diff.test.ts` is gated by `LOCALBOT_SMOKE_OK=1` env var. When the env var is missing, the test calls `test.skip()` for the headed portion but still runs the daemon-only assertions (FS watcher + JSON-RPC).
- The test spawns the daemon with `LOCALBOT_USER_DATA_DIR=<tmp>` and pre-creates:
  - `<tmp>/workspace/default/hello.txt` with content `Hello, world!\n`.
  - `<tmp>/workspace/default/hello.bin` with content `\0binary\0content` (NUL byte in first 8KB).
- Daemon-only assertions:
  1. Send `tree/list {path: <tmp>/workspace/default}` -> assert response has `entries` containing `hello.txt`, `hello.bin`; assert `hello.bin` carries `binary:true` (from a Wave 2 `list_tree` extension that returns `binary:true` when the file contains NUL bytes — this feeds the DiffView's placeholder path; if not yet extended, the test asserts `hello.bin.size > 0` and notes a TODO).
  2. Send `tools/call {name:'edit_file', arguments:{path:'hello.txt', find:'world', replace:'planet'}, toolCallId:'tc_tree_1'}` via JSON-RPC; assert response `{path:'hello.txt', replacements:1}`; assert `<tmp>/workspace/default/hello.txt` now reads `Hello, planet!\n`.
  3. Trigger the chokidar watcher by waiting 500ms; assert the daemon emits a `tree:refresh` notification (the test reads one NDJSON line from the daemon's stdout, parses it as a JSON-RPC notification with `method:'tree:refresh'`).
- Headed assertions (gated by `LOCALBOT_SMOKE_OK=1`):
  1. Launch Electron with `LOCALBOT_USER_DATA_DIR=<tmp>` and `M3_PORT=<fake-m3 port>`.
  2. Wait for the chat shell to mount.
  3. Send `sendMessage {msgId:'m_diff_1', text:'edit hello.txt to change world to planet'}` via `window.localbot.invoke`.
  4. Fake M3 responds with `streamEditFileToolUse({find:'world', replace:'planet', followupText:'Edited'})`.
  5. Wait for the renderer to mount a `.bubble` containing `.block-tool-result` for `edit_file`.
  6. Assert the renderer mounts `<DiffView>`: `page.locator('.diff-view')` is visible; `page.locator('.diff-view .diff-header')` text contains `Diff: hello.txt`; `page.locator('.diff-view').textContent()` contains both `world` (old) and `planet` (new) literals.
  7. Send a SECOND `sendMessage` with `text:'check the binary file'`; fake M3 responds with `streamBinaryEditFile()`. Wait for the tool_result; assert `page.locator('.binary-placeholder')` is visible with text `Binary file — diff unavailable`.
  8. Assert WorkspaceTree renders: `page.locator('.workspace-tree-section')` count >= 1 (workspace + memory & tools); `page.locator('.tree-node')` count >= 2 (hello.txt + hello.bin at minimum).
  9. Touch `<tmp>/workspace/default/new_file.txt` from the test via `fs.writeFileSync`; assert within 500ms that the chokidar → IPC → renderer chain fires: `page.locator('.tree-node')` count increments by 1.
  10. Audit JSONL assertion: read `<tmp>/audit/<UTC-day>.jsonl`; assert it contains at least one line per tool: `read_file`, `write_file`, `edit_file`, `list_dir`, `code_search`, `memory.read`, `memory.write`, `memory.update`, `tree.list`. (9 distinct `tool` values.)
</acceptance_criteria>

<files>
- tests/playwright/tree-diff.test.ts
- tests/playwright/fake-m3-server.ts (already extended in Task 1)
- tests/playwright/daemon-tools.test.ts (extended with audit JSONL coverage assertions)
</files>

<action>
Create `tests/playwright/tree-diff.test.ts`:
- `import { test, expect } from '@playwright/test'`; `import { spawnDaemon } from './electron-helper'`; `import { streamEditFileToolUse, streamBinaryEditFile } from './fake-m3-server'`.
- Pre-create the temp directory + files in `test.beforeAll`.
- `test('daemon tree/list + chokidar refresh + edit_file round-trip', async () => { ... })` runs the daemon-only path (always; no LOCALBOT_SMOKE_OK gating).
- `test('headed: WorkspaceTree + DiffView + binary placeholder + chokidar refresh', async () => { test.skip(!process.env.LOCALBOT_SMOKE_OK, 'headed smoke requires LOCALBOT_SMOKE_OK=1'); ... })` runs the renderer assertions.
- The headed test uses `electron.launch({ env: { ...process.env, LOCALBOT_USER_DATA_DIR: tmp, M3_PORT: ''+fakeM3.port, LOCALBOT_SOFT_CAP_TOKENS: '100000' } })` (Playwright Electron API).
- Use `page.locator(...).waitFor({ timeout: 5000 })` patterns proven in Phase 2 + Wave 1.
- For the chokidar refresh assertion, the test uses `page.waitForFunction(() => document.querySelectorAll('.tree-node').length >= 3, { timeout: 1000 })` after `fs.writeFileSync`.

Extend `tests/playwright/daemon-tools.test.ts`:
- Add a new test `audit log covers all 9 tool names` that:
  1. Pre-seeds the daemon with a workspace containing 1 file.
  2. Issues `tools/call` for each of the 9 tool names (`read_file`, `write_file`, `edit_file`, `list_dir`, `code_search`, `memory.read`, `memory.write`, `memory.update`, `tree.list`).
  3. Reads `<tmp>/audit/<UTC-day>.jsonl`; asserts each of the 9 names appears at least once in the audit lines.
- This proves SEC-04 audit coverage in a single test.

</action>

<verify>
<automated>npx --prefix D:/Claude/Grokbot playwright test tests/playwright/tree-diff.test.ts 2>&1 | tail -50</automated>
<fails_when>non-zero exit; "FAIL" anywhere; the daemon-only path fails before reaching the headed gate</fails_when>
</verify>
<automated>LOCALBOT_SMOKE_OK=1 npx --prefix D:/Claude/Grokbot playwright test tests/playwright/tree-diff.test.ts 2>&1 | tail -50</automated>
<fails_when>non-zero exit when LOCALBOT_SMOKE_OK is set; the headed assertions must complete</fails_when>
</verify>
<automated>grep -c "tree/list\|edit_file\|workspace-tree\|diff-view\|binary-placeholder\|chokidar\|tree:refresh" D:/Claude/Grokbot/tests/playwright/tree-diff.test.ts | awk '{ if ($1 < 4) exit 1 }'</automated>
<fails_when>tree-diff.test.ts missing the key tree + diff assertions</fails_when>
</verify>
<automated>grep -c "memory.read\|memory.write\|memory.update\|tree.list" D:/Claude/Grokbot/tests/playwright/daemon-tools.test.ts | awk '{ if ($1 < 4) exit 1 }'</automated>
<fails_when>daemon-tools.test.ts missing the 9-tool audit coverage</fails_when>
</verify>

<done>
- `tree-diff.test.ts` daemon-only path passes (always).
- Headed path passes when `LOCALBOT_SMOKE_OK=1`.
- All 9 tool names appear in the audit JSONL after the round-trip.
</done>

</task>

## Task 3 — Visual polish + final coverage audit + commit

<read_first>
- @src/renderer/styles/app.css (Wave 1+2 tokens; this plan verifies + polishes)
- @src/renderer/components/* (Wave 2 components; this plan audits aria + focus + hover)
- @.planning/phases/03-memory-conversation-history/03-UI-SPEC.md §"Copywriting 7.1-7.5" (exact strings), §"Accessibility 12", §"Empty/Error/Loading 13"
- @package.json (final dep audit; verify no duplicate deps)
- @.planning/phases/03-memory-conversation-history/03-VALIDATION.md §"Validation Sign-Off" checklist
</read_first>

<acceptance_criteria>
- Every component has the focus ring from UI-SPEC §12: `outline: 2px solid var(--lb-tree-focus); outline-offset: 2px;` on `:focus-visible`.
- Every component's hover state uses `var(--lb-tree-hover)` (#f3f4f6) for `.tree-node:hover` and `.memory-pill:hover`.
- The `.memory-panel` background is `var(--lb-dropdown-bg)` (#ffffff); the `.session-dropdown` background is the same.
- The `.bubble-summary` uses `var(--lb-summary-bg)`, `var(--lb-summary-border)`, and italic body per UI-SPEC §1.4.
- The `.binary-placeholder` uses `var(--lb-binary-bg)`, `var(--lb-binary-text)`, `var(--lb-binary-border)` per UI-SPEC §2.1.
- The `--lb-debounce-refresh-ms: 250` token is added to `:root` per UI-SPEC §2.4 (although the chokidar debounce lives in the daemon, this token documents the constant for the renderer).
- WorkspaceTree has `aria-live="polite"` on the container; the `aria-label="Workspace updated"` region announces changes.
- MemoryPanel has `role="dialog"`, `aria-modal="true"`, `aria-labelledby="memory-panel-title"`; the title element has `id="memory-panel-title"`.
- SessionSwitcher trigger has `aria-haspopup="menu"`, `aria-expanded={open}`.
- DiffView has `role="region"`, `aria-label="Diff for <file>"`; the mode toggle is a `<div role="group">` with two `<button aria-pressed>` buttons.
- `package.json` final dep state: `@anthropic-ai/sdk ^0.40.1`, `@vscode/ripgrep 1.18.0`, `chokidar ^3.6.0`, `react-arborist ^3.4.0`, `react-diff-viewer-continued ^4.0.0`, `diff ^5.2.0`, `electron ^33.2.0`, `react ^19.0.0`, `vitest ^2.1.9`. `npm ls --depth=0` output captured in the commit message.
- Validation Sign-Off checklist from VALIDATION.md §"Validation Sign-Off" is satisfied: all tasks have `<automated>` verify or Wave 0 dependencies; sampling continuity (no 3 consecutive tasks without automated verify); Wave 0 covers all 4 REQ-IDs (AGENT-05, AGENT-06, LLM-04, UI-08); no watch-mode flags; feedback latency < 30s; `nyquist_compliant: true` set in `03-VALIDATION.md` frontmatter.
</acceptance_criteria>

<files>
- src/renderer/styles/app.css
- src/renderer/components/WorkspaceTree.tsx
- src/renderer/components/MemoryPill.tsx
- src/renderer/components/MemoryPanel.tsx
- src/renderer/components/SummaryBlock.tsx
- src/renderer/components/SessionSwitcher.tsx
- src/renderer/components/DiffView.tsx
- src/renderer/components/MessageBlock.tsx
- src/renderer/components/Chat.tsx
- .planning/phases/03-memory-conversation-history/03-VALIDATION.md (set nyquist_compliant: true)
- package.json (final dep audit)
</files>

<action>
Polish pass on `src/renderer/styles/app.css`:
- Confirm `:root` carries every token from UI-SPEC §2.1: `--lb-sidebar-bg`, `--lb-sidebar-border`, `--lb-sidebar-text`, `--lb-sidebar-text-muted`, `--lb-tree-hover`, `--lb-tree-selected`, `--lb-tree-focus`, `--lb-pill-bg`, `--lb-pill-text`, `--lb-pill-border`, `--lb-summary-bg`, `--lb-summary-text`, `--lb-summary-border`, `--lb-diff-add`, `--lb-diff-remove`, `--lb-binary-bg`, `--lb-binary-text`, `--lb-binary-border`, `--lb-session-active`, `--lb-dropdown-bg`, `--lb-dropdown-shadow`, `--lb-sidebar-width`, `--lb-sidebar-width-collapsed`, `--lb-debounce-refresh-ms`.
- Confirm every component class from UI-SPEC §"Layout 6" + §"Empty/Error/Loading 13" exists with the right tokens.
- Add `:focus-visible { outline: 2px solid var(--lb-tree-focus); outline-offset: 2px; }` to all interactive selectors: `.tree-node`, `.memory-pill`, `.session-trigger`, `.session-dropdown button`, `.diff-mode-toggle button`, `.memory-panel-close`.

Polish pass on components:
- `WorkspaceTree.tsx`: add `aria-live="polite"` on the container + a hidden `<div role="status">Workspace updated</div>` region.
- `MemoryPanel.tsx`: confirm `id="memory-panel-title"` on the title; add `aria-describedby="memory-panel-body"` on the panel.
- `SessionSwitcher.tsx`: `aria-haspopup="menu"` + `aria-expanded={open}` on the trigger.
- `DiffView.tsx`: `aria-label={`Diff for ${file}`}` on the region; `<button aria-pressed={mode==='split'}>` and `<button aria-pressed={mode==='unified'}>` on the mode toggle.
- `MessageBlock.tsx`: no changes (already correct from Phase 2 + Wave 1+2).

Polish pass on `Chat.tsx`:
- Confirm header layout per UI-SPEC §6.6: title group left with `-webkit-app-region: drag`; SessionSwitcher + MemoryPill right with `-webkit-app-region: no-drag`.
- Confirm sidebar layout per UI-SPEC §6.1: 240 px wide on viewports >= 900 px; collapses to 32 px on narrower (CSS media query).

Run `npm ls --depth=0` from `D:/Claude/Grokbot` and capture the output into the commit message body. Verify no duplicate deps (e.g., `diff` pulled in by both `chokidar` and `react-diff-viewer-continued` — npm hoists; the lockfile should resolve to a single version).

Update `.planning/phases/03-memory-conversation-history/03-VALIDATION.md`:
- Set `nyquist_compliant: true` in frontmatter.
- Tick every Wave 0 checkbox (the test files exist from Wave 1+2).
- Tick the "Validation Sign-Off" checklist items.

Create the final commit:
```
git add .planning/phases/03-memory-conversation-history/
git add src/ daemon/ tests/ package.json
git commit -m "feat(03): memory + per-bot JSONL + auto-summarize + workspace tree + diff view

Wave 1 tracer:
- daemon: memory.read/write + tree.list JSON-RPC methods
- main: per-bot per-session JSONL routing + legacy migration
- main: usageAccumulator + maybeSummarize + runSummarizer
- main: injectMemorySuffix (4 KB cap, facts cap 50)
- renderer: MemoryPill + WorkspaceTree placeholders
- fake-m3-server: streamLongResponse helper

Wave 2 surface:
- renderer: DiffView, MemoryPanel, SummaryBlock, SessionSwitcher
- renderer: state slices (memory, tree, sessions)
- daemon: chokidar watcher (250ms debounce) + tree:refresh notifications
- main: tree/watcher.ts + tree/list.ts bridge
- chat.ts: childAbortController for cancel mid-summarize
- audit JSONL coverage for all 9 op names

Wave 3 polish:
- playright: memory-history + tree-diff smokes
- visual polish per UI-SPEC §2.1 (focus rings, hover, aria-live)
- npm ls --depth=0:

$(npm ls --depth=0)

Refs: AGENT-05, AGENT-06, LLM-04, UI-08"
```

</action>

<verify>
<automated>npm --prefix D:/Claude/Grokbot run test:all 2>&1 | tail -50</automated>
<fails_when>non-zero exit; any "FAIL" anywhere; "0 passed" in summary; missing dist files</fails_when>
</verify>
<automated>npm --prefix D:/Claude/Grokbot ls --depth=0 2>&1 | tail -30</automated>
<fails_when>npm ls exits non-zero; any "missing" or "invalid" lines; any duplicate version warnings</fails_when>
</verify>
<automated>grep -c "lb-pill-bg\|lb-pill-text\|lb-pill-border\|lb-sidebar-bg\|lb-summary-bg\|lb-binary-bg\|lb-tree-focus\|lb-debounce-refresh-ms" D:/Claude/Grokbot/src/renderer/styles/app.css | awk '{ if ($1 < 8) exit 1 }'</automated>
<fails_when>app.css missing 8+ UI-SPEC tokens</fails_when>
</verify>
<automated>grep -c "aria-live\|role=\"dialog\"\|aria-modal\|aria-haspopup\|aria-pressed\|aria-expanded" D:/Claude/Grokbot/src/renderer/components/*.tsx | awk '{ if ($1 < 4) exit 1 }'</automated>
<fails_when>renderer components missing the ARIA roles</fails_when>
</verify>
<automated>test -f D:/Claude/Grokbot/dist/main/sessions/jsonl.js && test -f D:/Claude/Grokbot/dist/main/bots/memory.js && test -f D:/Claude/Grokbot/dist/main/llm/summarize.js && test -f D:/Claude/Grokbot/dist/main/llm/usageAccumulator.js && test -f D:/Claude/Grokbot/dist/renderer/components/DiffView.js && test -f D:/Claude/Grokbot/dist/renderer/components/MemoryPanel.js && test -f D:/Claude/Grokbot/dist/renderer/components/SummaryBlock.js && test -f D:/Claude/Grokbot/dist/renderer/components/SessionSwitcher.js</automated>
<fails_when>any of the 8 dist files missing after build</fails_when>
</verify>
<automated>git -C D:/Claude/Grokbot log --oneline -5</automated>
<fails_when>commit not visible; HEAD did not advance</fails_when>
</verify>

<done>
- All 12+ Vitest suites pass; both Playwright smokes pass; `npm run test:all` exits 0.
- `npm ls --depth=0` shows the expected dependency tree with no duplicates.
- Every UI-SPEC §2.1 token is present in `app.css`; every component has the required ARIA roles.
- `03-VALIDATION.md` has `nyquist_compliant: true` + ticked Wave 0 checklist.
- Final commit visible in `git log` with the dep audit captured in the body.
</done>

</task>

---

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer -> IPC | Untrusted renderer; cannot directly invoke daemon tools, write user filesystem, or drive chokidar |
| main -> daemon (JSON-RPC) | Main owns the agentic loop + IPC bridge; system calls (memory/tree) bypass the per-bot allowlist |
| daemon -> filesystem | Daemon enforces `safePath` containment; chokidar watches confined to `<userData>/workspace/<bot>/` + `<userData>/bots/<bot>/` |
| daemon -> watcher (chokidar) | Debounced 250ms; emit `tree:refresh` notifications only via `sendNotification`; no direct fs writes |
| main -> LLM API | Summary call uses a child AbortController + separate retry budget; user cancel aborts summary only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-P3-19 | Tampering | Playwright headed smoke exposes real user data | low | mitigate | All Playwright tests use `LOCALBOT_USER_DATA_DIR=<tmp>` so no real user data is touched; headed gate requires `LOCALBOT_SMOKE_OK=1`; never auto-runs in CI |
| T-P3-20 | Information Disclosure | Audit JSONL carries tool params verbatim | low | accept | The Phase 1/2/3 audit shape keeps `params` for forensic value; `memory.write` params carry `{bot, bytesWritten, factCount}` (no markdown body) per Wave 1's minimization; `code_search` params carry `{pattern, glob, path, max_results, result_count}` (no matches payload) per Phase 2 |
| T-P3-21 | Denial of Service | Final smoke run consumes excessive time | low | mitigate | Quick unit suite is gated to ~10s; full `npm run test:all` is ~30s; headed smoke is gated by `LOCALBOT_SMOKE_OK` and runs only on a developer desktop |
| T-P3-22 | Repudiation | npm install drift between Wave 1/2/3 | medium | mitigate | All Phase 3 installs happened in Wave 1 Task 1 (single `npm install`); Wave 2 + Wave 3 add no new deps; `npm ls --depth=0` is captured in the final commit for reproducibility |
| T-P3-SC | Tampering | npm install (chokidar, react-arborist, react-diff-viewer-continued, diff) | high | mitigate | RESEARCH.md `## Package Legitimacy Audit` confirms all four packages; all `[ASSUMED]` flagged for user awareness; no `[SLOP]` verdicts |

</threat_model>

<verification>
After all 3 tasks:

- `npm --prefix D:/Claude/Grokbot run test:all 2>&1 | tail -50` exits 0; all unit + Playwright smokes pass.
- `LOCALBOT_SMOKE_OK=1 npx --prefix D:/Claude/Grokbot playwright test tests/playwright/tree-diff.test.ts tests/playwright/memory-history.test.ts 2>&1 | tail -50` exits 0; the full headed smoke runs.
- `npm --prefix D:/Claude/Grokbot ls --depth=0` output captured in the commit body; no duplicate deps.
- Audit JSONL file at `<userData>/audit/<UTC-day>.jsonl` contains one line per tool after the round-trip (9 distinct `tool` values).
- Visual smoke (gated by `LOCALBOT_SMOKE_OK` on a desktop session): launch app, ask the bot to read/edit/list files, observe tool_use + tool_result blocks + DiffView inline; open the `Memory` pill; switch sessions via `SessionSwitcher`; watch the workspace tree update after a tool call.
</verification>

<success_criteria>
Phase 3 is verified when ALL of the following hold:

1. AGENT-05: Memory persists across app restart — `memory.md` + `facts.json` survive a kill of the Electron renderer; relaunching the app re-injects them into the system prompt (proven by `memory-history.test.ts` restart-reload).
2. AGENT-06: Per-bot per-session JSONL history persists across app restart — legacy `global.jsonl` migrates to `default/<iso>.jsonl` on first launch; switching sessions re-loads the chat (proven by `memory-history.test.ts` SessionSwitcher assertions).
3. LLM-04: Token-budget auto-summarize triggers — a long fake-M3 stream with `usage.input_tokens > SOFT_CAP` writes a head-of-JSONL summary record; the renderer renders a `<SummaryBlock>` at the head of the chat (proven by `memory-history.test.ts` + `summarize.test.ts`).
4. UI-08: Workspace file tree + diff view — WorkspaceTree renders react-arborist rows from `tree/list`; `edit_file` tool_results render `<DiffView>` inline; binary files render `<DiffBinaryPlaceholder>` (proven by `tree-diff.test.ts` + `list_tree.test.ts` binary detection).
5. `npm test`, `npm run build`, `npm run test:all` all pass with zero failures.
6. `git log` shows the final commit with the dependency audit captured in the body.
</success_criteria>

<output>
Create `.planning/phases/03-memory-conversation-history/03-03-SUMMARY.md` when done.

After Wave 3 commits, the Phase 3 verification gate (`/gsd-verify-work`) can confirm all 4 phase requirements pass; UI-SPEC contract is fulfilled end-to-end; headed smoke is gated for desktop sessions only.
</output>
